# 스타포지 러시 v3 — 2차 검수 Q3 반영: 성공 경로 봇 `planBoss` 와 검사 SD-7~9 (2026-09-17)

저장소 `E:\workspace\claude\neon-fleet\worktrees\v3-lastwar`(브랜치 `claude/starforge-v3`, HEAD `33bde94` 위 **미커밋** 작업). 기존 `rush/*`·`rush.html`·`tests/rush-*.test.mjs` **무수정**(`git diff` 0줄로 확인). 커밋·푸시·배포 없음.

**한 줄 요약.** 2차 검수가 지적한 "봇 미달"을 전투를 약하게 만들어 덮지 않고, **조작을 포함한 성공 경로가 존재하는지**를 따로 재는 봇과 검사를 추가했다. 결과: **어려움 S1·S2·S3 와 극한 S1·S3 은 완주 가능**하고(검수의 어려움 S2 5명·극한 S1 10명과 생존 수까지 일치), **극한 S2 만 여전히 미달**이다. 규칙·배치·배수 표·기본 난이도는 한 줄도 바뀌지 않았다.

---

## 1. 무엇을 왜 했나

2차 검수(`newmode/v3/review2/01_GPT_2차_검수결과.md` §4 Q3)의 요구는 셋이었다.

1. 서 있는 봇을 이기게 하려고 **전투 전체를 약하게 만들지 말 것**.
2. 완주 요구를 '정예 도달'로 바꿔 놓고 **원래 요구까지 충족됐다고 보고하지 말 것**.
3. 검사를 세 층으로 나누고, **조작을 포함한 성공 경로 검사를 별도로 추가할 것**. 실패 봇 결과는 그대로 남길 것.

그래서 이번 작업은 **더하기만** 했다. 기존 `SD-2`·`SD-3`('정예 도달' 검사)은 이름·목적·단언 그대로 두고, 그 옆에 성공 경로 검사 `SD-7`을 새로 잠갔다. 밸런스 수치(`rush3/balance.js`)·코스 배치(`rush3/stages.js`)·전투 규칙(`rush3/combat.js`)은 열지도 않았다.

## 2. 봇 정의 — `planBoss`

`tests/lib/rush3-policies.mjs` 에 정책 하나를 추가했다.

```js
export function botPlanBoss(run) {
  return run.boss ? run.boss.x : botPlan(run);
}
```

- **보스 등장 전**: 기존 계획 봇 `botPlan` 과 **완전히 같다**(구간별 목표 x 스크립트, 무작위 없음).
- **보스 등장 후**: `pointerX = run.boss.x` — 정예의 현재 x 를 따라 조준한다.
- **이동은 실제 STEP 의 이동 속도 제한을 그대로 받는다.** 순간이동하지 않고, **탄 회피를 최적화하지도 않는다**(부채꼴을 피하려고 옆으로 비키는 판단이 없다).
- 2차 검수 스크립트 `review2/review-probe.mjs` 의 `bossFollow` 변형과 같은 조작이다(그쪽은 `package/` 하위 경로로 import 하는 점만 다르다).
- **`POLICIES` 목록에는 넣지 않았다.** 기존 24판 표(`V3-SIM-POLICY`)와 27판 표(`V3-SIM-DIFF SD-0~6`)의 판 수·의미를 그대로 두기 위해서다. 성공 경로 검사만 이 정책을 따로 부른다.
- 규칙 모듈(`combat`/`gates`/`supply`/`squad`/`weapons`)은 열지 않았다. `Math.random`·`rng`·`balance` 를 새로 import 한 곳도 없다.

## 3. 검수 표와 우리 실행값 — 나란히

검수가 실은 세 조건과 우리 실행 결과다. **생존 병력 수까지 같다.**

| 조건 | 2차 검수 독립 실험(보스 위치를 따라 조준) | 우리 실행(`SD-7` 진단 출력) | 일치 |
|---|---|---|---|
| 어려움 S2 | **5명 생존 성공** | **완주 · 생존 5명** (peak 21 · 소총 · 정예 잔여 0 · 61.3초) | 같음 |
| 극한 S1 | **10명 생존 성공** | **완주 · 생존 10명** (peak 14 · 기관총 · 정예 잔여 0 · 44.7초) | 같음 |
| 극한 S2 | 실패, 정예 hp **201** 남음 | **실패**, 정예 hp **201** / 528 남음 (66.9초) | 같음 |

검수는 이 세 조건만 실었다. 나머지 세 판(어려움 S1·S3, 극한 S3)은 우리 쪽에서만 돌린 값이며 아래 §4 에 있다.

## 4. 전체 결과표 — **봇 결과이지 사람의 성공률이 아니다**

`node --test tests/rush3-difficulty.test.mjs` 의 `SIM-BOSS` 진단 출력 그대로(판당 상한 14,400 STEP = 4분, 각 판은 결정적 1판).

| 난이도 | 스테이지 | 결과 | 생존 | 최고(peak) | 무기 | 정예 잔여 hp | 피격 손실 | 접촉 손실 | 시간 |
|---|---|---|---:|---:|---|---:|---:|---:|---:|
| 어려움 | S1 | **완주** | 14 | 14 | 기관총 | 0 | 0 | 0 | 41.4초 |
| 어려움 | S2 | **완주** | 5 | 21 | 소총 | 0 | 17 | 2 | 61.3초 |
| 어려움 | S3 | **완주** | 70 | 78 | 기관총 | 0 | 8 | 1 | 61.4초 |
| 극한 | S1 | **완주** | 10 | 14 | 기관총 | 0 | 4 | 0 | 44.7초 |
| 극한 | S2 | **실패**(정예전 전멸) | 0 | 21 | 소총 | **201** / 528 | 22 | 2 | 66.9초 |
| 극한 | S3 | **완주** | 57 | 78 | 기관총 | 0 | 18 | 4 | 64.7초 |

### 4-1. 같은 판을 `plan`(정예전에도 계획 경로 그대로)으로 돌린 대조값

| 난이도 | 스테이지 | `plan` | `planBoss` | 뒤집혔나 |
|---|---|---|---|---|
| 어려움 | S1 | 완주 11명 | 완주 14명 | — |
| 어려움 | S2 | **실패**(정예 122 잔존) | **완주 5명** | **예** |
| 어려움 | S3 | 완주 71명 | 완주 70명 | — |
| 극한 | S1 | **실패**(정예 11 잔존) | **완주 10명** | **예** |
| 극한 | S2 | 실패(정예 321 잔존) | 실패(정예 201 잔존) | 아니오(줄기만 함) |
| 극한 | S3 | 완주 63명 | 완주 57명 | — |

**정예전에서 보스를 따라 조준하는 것만으로 어려움 S2 와 극한 S1 의 결과가 실패 → 완주로 뒤집힌다.** 즉 그 두 판의 실패 원인은 코스 배치나 배수 표가 아니라 **정예전 조작**이었다. 최고 병력(peak)은 여섯 판 모두 두 봇이 같다 — 정예는 코스 끝에 나오므로 이 조작은 성장 축을 건드리지 않는다(검사 `SD-9` 가 잠근다).

### 4-2. 한계 — 이 표로 말할 수 없는 것

- **사람의 성공률이 아니다.** 난이도·스테이지마다 **한 판의 결정적 시뮬**이고 분포도 표본도 아니다. 사람이 몇 번 만에 이기는지, 재미있는지는 이 표에 없다.
- 이 표가 말하는 것은 하나다: **어려움 3스테이지와 극한 S1·S3 에는 "이길 수 있는 조작"이 실제로 존재한다.** 따라서 이 판들을 '불가능한 코스'로 보고 배수 표를 낮출 근거는 없다.
- **극한 S2 는 여전히 미달이다.** 검수도 "단순 조준 변형으로는 성공을 입증하지 못했다"고 적었고 우리 실행도 같다(정예 hp 201 잔존). 감추지 않고 기록으로 남긴다.
- `planBoss` 는 탄 회피를 하지 않는 봇이다. 사람은 부채꼴을 옆으로 비킬 수 있으므로 **봇 실패 = 사람 실패가 아니다**(반대로 봇 성공도 사람의 쉬움을 뜻하지 않는다).

## 5. 추가한 검사 3건

| ID | 무엇을 잠그나 |
|---|---|
| `V3-SIM-DIFF SD-7` | **성공 경로(잠금)** — `planBoss` 가 어려움 S1·S2·S3 와 극한 S1 을 `won === true`·생존 병력 > 0 으로 완주한다. 더해서 **어려움 S2 생존 5명·극한 S1 생존 10명**이 2차 검수 표와 같은 값임을 단언한다. 여섯 판 전부 `t.diagnostic` 으로 출력한다. |
| `V3-SIM-DIFF SD-8` | **기록(실패 허용)** — 극한 S2·S3 는 완주를 요구하지 않는다. 다만 상한 안에서 종료·정예 등장까지 도달·**지더라도 정예전에서만 진다**(코스 중간 전멸이면 배치 문제이므로 실패)를 확인하고, 완주 여부와 정예 잔여 hp 를 기록한다. 극한 S3 가 완주한 사실도 진단에 남긴다. |
| `V3-SIM-DIFF SD-9` | **오염 방지** — `planBoss` 는 보스 등장 전까지 `plan` 과 같은 판이다. 보통 S1 은 판 전체가 deepEqual 이고, 여섯 판(어려움·극한 × S1~S3) 모두 `peak` 이 `plan` 과 같다. 같은 입력열 결정성도 확인한다. |

기존 `SD-2`·`SD-3`('정예 도달')은 **이름·목적·단언을 그대로 유지**했다. 단언을 약화한 곳은 없다.

## 6. 검사 수치(실제 출력)

```
node --test tests/rush3-*.test.mjs   → ℹ tests 216 · pass 216 · fail 0 (duration_ms 8452)
node --test tests/rush-*.test.mjs    → ℹ tests 32  · pass 32  · fail 0
```

**213 → 216**(신규 3건 = `SD-7`·`SD-8`·`SD-9`). 기존 213건은 단언 변경 없이 그대로 통과한다. 기존 게임 회귀 `tests/rush-*.test.mjs` 3파일 32건도 통과하며, `git diff -- rush/ rush.html tests/rush-*.test.mjs` 는 0줄이다.

## 7. 변경 파일

| 파일 | 내용 |
|---|---|
| `tests/lib/rush3-policies.mjs` | `botPlanBoss` 추가 · `pickX` 에 `case 'planBoss'` 추가. `POLICIES` 목록은 8종 그대로(주석으로 이유 명시) |
| `tests/rush3-difficulty.test.mjs` | 끝에 `SD-7`·`SD-8`·`SD-9` 추가. 기존 `DIFF-1~6`·`SD-0~5` 무수정 |
| `newmode/v3/build3/difficulty-report.md` | §8 추가(원래 미달 기록 §2·§3 은 그대로 두고 `planBoss` 결과표를 덧붙임) |
| `newmode/v3/DESIGN_v3_stage1.md` | r3.5 — 머리말 한 줄 · §3-8 '성공 경로 봇' 문단 · §8 `V3-SIM-DIFF` 검증 항목에 SD-7~9 |
| `newmode/v3/build5/bot-report.md` | 이 보고서 |
| `newmode/v3/build5/planboss-probe.mjs` | 같은 수치를 표로만 뽑는 스크립트(`node newmode/v3/build5/planboss-probe.mjs`) |

**바뀌지 않은 것**: `rush3/*` 전부(규칙·배치·밸런스·셸·렌더) · `rush/*` · `rush.html` · `tests/rush-*.test.mjs`.

## 8. 손대지 않은 이사 결정 대기 항목

지시대로 아래 셋은 현재 동작 그대로 두었다. 이번 발견이 이 결정들을 바꿀 근거로 쓰이지 않도록 명시한다.

1. **기본 난이도(극한 유지)** — 타이틀 초기 선택 `DEFAULT_PICK_DIFFICULTY = 'brutal'` 그대로. `planBoss` 가 극한 S1·S3 을 완주했다는 사실은 "극한이 적절한 첫 화면"이라는 증명이 아니다(봇 결과이지 신규 이용자의 학습 가능성이 아니다).
2. **랜덤 길 재도전 시드 정책** — `main.js` 의 시드 조립 그대로.
3. **확정 −10 게이트의 존폐·외형** — `balance.js` 의 `trapGate` 그대로.

## 9. 이사님이 보실 것(2줄)

1. **어려움 S2** 를 직접 플레이해 보세요. 봇도 "정예를 따라가며 쏘기"만으로 5명 남기고 이겼습니다 — 사람이 부채꼴을 비킬 수 있다는 점까지 더하면 여유가 더 있다는 뜻입니다.
2. **극한 S2 만 남았습니다.** 이 판은 여전히 봇으로 성공을 못 만들었습니다. 이사님이 직접 이겨 보신 기록이 있다면 그것이 가장 확실한 증거이고, 없다면 다음 라운드에서 배치가 아니라 **정예전 구간**을 따로 들여다보겠습니다.
