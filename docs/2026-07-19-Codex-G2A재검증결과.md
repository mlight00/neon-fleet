The measured happy path may pass, but low-damage runs can still omit scheduled bosses, and every play-mode completion crashes while constructing the result UI.

Full review comments:

- [P1] Queue every overlapping boss event ??E:\workspace\claude\neon-fleet\js\main.js:883-883
  When a boss survives across two later `bossStart` times, each event overwrites `cl.pendingBoss`; after the active boss dies, only the most recent pending boss is spawned and an earlier regional boss is permanently skipped. This can occur in low-damage play because enrage still requires hits, so use a FIFO collection rather than a single slot.

- [P2] Build the campaign result UI arguments correctly ??E:\workspace\claude\neon-fleet\js\main.js:921-921
  Whenever campaign25 finishes in `play` mode, this passes three positional arguments to `ui.showCoreLoopResult`, which expects one object shaped like `{ snap, build, ... }`. It therefore destructures the metrics snapshot itself, leaves its local `snap` undefined, and throws when reading `snap.damageByWeapon`, preventing every play-mode result screen from rendering.
