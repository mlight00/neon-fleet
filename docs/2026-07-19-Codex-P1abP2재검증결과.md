The adaptive boss logic does not cap actual post-BREAK damage and changes STAGGER behavior while provisional HP is active. Both issues undermine the boss timing and interaction guarantees targeted by the patch.

Full review comments:

- [P1] Apply the cap after B22's BREAK multiplier ??E:\workspace\claude\neon-fleet\js\main.js:596-596
  When B22 is in BREAK, this wrapper charges `_dmgSec` for `d`, but `rawHit` then reaches `NeonArbiter.hitByBullet`, which applies the 1.25횞 `breakDamageMult` before reducing HP. Actual HP loss therefore exceeds the per-second budget; charge play that repeatedly triggers BREAK, especially after calibration uses `minRemain`, can kill the boss before the intended 45-second floor. Enforce the budget using actual HP loss or after boss-specific modifiers.

- [P2] Preserve STAGGER progress across HP calibration ??E:\workspace\claude\neon-fleet\js\main.js:586-586
  During every calibrated B22 fight, this temporary `maxHp` is also the denominator in `NeonArbiter.staggerFromDamage` (`maxHp * 0.05`). For the first 11 seconds the threshold uses `_provMax`, then calibration replaces `maxHp` without rescaling `_staggerDmg`; normal damage can therefore produce no incremental STAGGER during sampling and then jump several levels on the next hit, or lose progress already consumed under the old threshold. Keep calibration HP separate from this mechanic or rescale the accumulator consistently.
