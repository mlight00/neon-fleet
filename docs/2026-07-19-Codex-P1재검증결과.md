The recalibration omits direct boss-damage paths and can undo legitimate player damage. Enemy purging also leaves stale homing and resonance targets, causing projectiles to chase removed objects.

Full review comments:

- [P1] Track all boss damage before recalibrating HP ??E:\workspace\claude\neon-fleet\js\main.js:637-637
  When a player lands a charge lance or lance echo during the first 11 seconds, those paths call `bo.hitByBullet` directly from `Squad` and never increment `_sampleDmg` or `_totalDmg`. This assignment therefore restores the lance damage at calibration and derives the new HP from projectile-only DPS, visibly healing the boss and invalidating the intended single-target measurement; boss damage accounting needs to cover every damage path.

- [P2] Mark purged enemies dead before splicing them ??E:\workspace\claude\neon-fleet\js\main.js:567-567
  If a homing missile or seeker mark targets a residual enemy when the boss spawns, splicing that enemy leaves its `dead` flag false and skips `resonEnemyRemoved`. `HomingMissile.update` consequently keeps steering existing missiles?봞nd newly fired seeker missiles while the stale mark remains active?봳oward the detached object instead of retargeting the boss; purge through the normal removal lifecycle or explicitly invalidate these targets.
