# G2-C (세 번째 슬롯 fleet 시스템, §7.2) — Codex 검증 완료 요약

- **일자**: 2026-07-19
- **브랜치**: `claude/neon-fleet-overhaul-phase-c-core-loop`
- **검증**: codex-bridge (`-Review -Base a54ac70`, read-only, gpt-5.6-sol)
- **결과**: **2라운드 만에 클린 통과** (지적 2 → 0, P1 없음)
- **최종 HEAD**: `50061ca` (origin push, master 미병합·미배포)

## 무엇을 만들었나 (G2-C)

§7.2 + T3 캐리어("함대 시스템 슬롯과 **전투기 편대**")대로, 세 번째 슬롯을 **자율 전투기 편대**(주포 복제 아님)로 구현.

- **해금**: 디렉터 사건 `fleetSlot`(780s / 13분) → `equipCampaignFleet` (그 전 `fleetTelegraph` 660s에 예고)
- **동작**: 전투기 3기가 기함 앞 편대 대형(V형) 유지 + 상하 부유. 매 프레임 **위쪽 사거리 내 최근접 적을 스스로 조준**해 발사(잡몹 우선, 없으면 보스)
- **격리**: `sq.fleet.ships` 전용 배열 — `w.entities`에 안 넣어 적 판정·Apex·플레이어탄 충돌과 무간섭
- **집계 구분**: 볼트 `sourceWeaponId 'fleet'` → `damageByWeapon.fleet` 버킷(공명 미충전)
- **화면 구분**: HUD 세 번째 슬롯 칩 + 결과 패널 '함대·전투기' 행 → 세 슬롯(주무기·보조·함대) 전부 구분(§7.2)
- 파일: `balance.gate2.fleet`(설정), `main.js`(equip/tick/target/draw + 사건 배선 + HUD 데이터 + startCampaign25 리셋), `render.js`(HUD 칩), `ui.js`(결과 행), `run-metrics.js`(fleetSlot 마일스톤)

## 라운드별 Codex 지적 → 수정

### 1차 (커밋 `50061ca`, P2×1 + P3×1)
- **[P2] 하향 볼트 무한 잔류**: 표적이 전투기보다 아래(+20px)까지 허용 → 하향 볼트 생성. `Bullet.update`는 화면 위/좌우 이탈만 제거하고 **아래로 나간 볼트는 안 지움** → 25분 내내 `w.bullets` 누적. → **표적을 위쪽(y < 전투기 y) 적만으로 한정**(잡몹·보스 모두) → 볼트 항상 상향 → 상단서 소멸. 효율 영향 무시 가능(적이 대부분 전투기 위)
- **[P3] Gate 1에 못 여는 슬롯 표시**: `drawCoreLoopHud`가 Gate 1·Gate 2 공유인데 Gate 1(`?coreLoopTest`)에선 함대가 Gate 2 전용이라 영영 못 여는 "함대 슬롯 (미해금)" 상시 표시 → **함대 행 전체를 `d.fleetSupported`(=`!!r.campaign25`) 게이트** → Gate 1 미표시

### 2차 = 클린
> "The fleet system is consistently wired through campaign events, combat, rendering, metrics, and result reporting. All 395 tests pass, and no actionable regression was identified."

## 최종 검증 (본인 재측정)

- **자동 테스트 395개 통과**, `node --check` clean, 콘솔 무오류
- **25분 헤드리스 integrationCheck PASS**: 지역 보스 TTK **32.3 / 32.2 / 32.2 / 32.1 / 47 / 62.2초** — 전투기 피해가 780s 후 avgDps에 반영돼도 전부 대역
- **슬롯 해금 마일스톤 780s**, 전투기 피해 32,675(**약 6.7% 지분**)
- 결과 패널 DOM: 함대 행 포함 피해 비율 **100% 정합**
- 회귀 방지 테스트 `gate2-wiring` G2-11 신설

## 미검증(위임)

- 전투기 **시각 렌더**(`drawCampaignFleet` 삼각 아군기): `__NF.run`이 stale라 콘솔에서 함대 강제 활성화 불가 → 표준 캔버스 코드 + 무오류 렌더 루프 + Codex 정적검토로 갈음, 실제 시각은 이사 플레이테스트에 위임

## 향후 확장 여지

- `fleet.systemId`는 확장 가능하게 설계 — 이후 **기뢰·지원 빔·방어 드론** 같은 다른 함대 시스템을 같은 슬롯에 추가할 수 있음(현재는 전투기 편대 1종)
