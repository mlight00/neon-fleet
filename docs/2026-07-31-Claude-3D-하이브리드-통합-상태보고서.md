# 네온 함대 실제 3D 하이브리드 — 통합 상태보고서

- 작성일: 2026-07-31, **갱신 2026-08-01**(Codex 재검토 `2026-08-01-…재검토결과.md` P2 3건 반영)
- 브랜치: `claude/neon-fleet-analytics-stage1` (**푸시·배포·업로드 없음**, 로컬 커밋만)
- 이전 문서 `2026-07-30-…AURORA-재구현-완료보고.md` 는 시간순 **작업 일지**로 격하 — 수치·상태는 본 문서가 단일 진실.
- 표기: 〔자동〕=테스트/스크립트 실측, 〔브라우저〕=로컬 Chrome 실측, 〔육안〕=사람 판독, 〔추론〕=미실측 추정.

## A. 현재 적용 (기본 동작 — `?chase3d=1`)

| 항목 | 상태 |
|---|---|
| 공개 기본 URL(플래그 없음) | **3D 비활성** — three·hero 렌더러·모델·소품 팩토리 로드 0(8/1 리소스 실측). 잔류는 플래그 파서(chase3d-config 3.5KB)와 순수 상수(chase3d-prop-defs 0.7KB)뿐 — 8/1 재검토가 지적한 38.9KB 정적 체인(aurora-geometry+props) 절단〔브라우저〕 |
| 3D 레이어(=`chase3d=1`) | 기함 AURORA + 크리처 B1(소형)·B2(중형)·B4(저격수)만 3D. 그 외 전부 기존 2D |
| 기함 스왑 | t=0.5 하드 컷(겹침 0·공백 0, HW-02 수학 증명) + 워프 플래시. 180% 정착 배율 제거(줌 4단계 1.0/1.5/2.0/2.2) |
| 카메라 | 부분 추종 0.42(기함이 화면에서 실제 이동, 3D 트럭 정합), 기함 통과 개체 depth 1.6 외삽 퇴장 |
| 배경 | 추적 시 세로 별·먼지·삼각 파편·항적 소등 → 소실점 방사 워프 별(결정적 110) |
| 스웜 배치 | 2.5D 투영 화면좌표·크기를 unproject 정합 — 위치·원근이 2D 세계와 픽셀 정합. 종당 InstancedMesh 3(=+3 draw) |
| 정보 보존 | 변이(엘리트) 개체는 2D 유지(링·표식), 초과분 2D 폴백, 피격핵·판정·입력·규칙 완전 불변 |
| 실패 복귀 | WebGL context loss → `degraded` → 2D 게임 계속〔브라우저, Codex 독립 재검증 일치〕 |

## B. 개발 전용 (기본 OFF)

| 플래그 | 내용 |
|---|---|
| `chase3dProps=1` | 탄 3종·픽업 5종 소품 3D — **이사 실기 "엉망" + Codex "가독성 A/B 통과 전 2D 유지" 판정으로 기본 OFF**. 8/1 재검토 반영으로 OFF 는 **GPU 자원 생성까지 OFF**: 실측 OFF=programs 5·geometries 25·propCounts null ↔ ON=6·33·활성(pbullet 34 등)〔브라우저〕. 코드·테스트는 유지 |
| `chase3dLab=aurora·b1·b2·b4·swarm` | 모델 검사실(고정 카메라·시드) |
| `chase3dTest=1` | ⚠ **LEGACY 구형 조립식 장면** — 최신 hero 가 아님(오버레이 첫 줄에 경고 명시). 최신 QA 는 `?chase3d=1` 실게임으로 |

## C. 최신 통합 계측 (2026-07-31, 데스크톱 Chrome 682×1100, 소품 OFF 기본 구성)〔브라우저〕

| 지표 | 값 | 비고 |
|---|---|---|
| 프레임 p50 / p95 | **0.90 / 1.9 ms** | update+draw+3D 전체, 900프레임 |
| 프레임 p99 / worst | 11.2 / 18.8 ms | 스폰 웨이브·GC 순간 스파이크(예산 16.7ms, p99 이내) |
| programs | **6** (3회 측정 동일) | 플레이 중 셰이더 증가 0. ※구 보고서의 3 은 AURORA 단독 시점 |
| geometries / textures | **33 / 28** | 100↔220% **20회 왕복 후 불변**(누수 0) |
| draw calls / tris | 11 / 21,564 | AURORA 8 + B1(2기 시점) 3 |
| 프리웜 | 495 ms | 출격 전 1회(전투 중 컴파일 히치 0) |
| 진단 | `__NF.chase3dInfo()` | b1/b2/b4Count·propCounts 노출(Codex P2 반영) |

모델 예산〔자동〕: AURORA LOD0 18,264 / B1 1,650 / B2 1,934 / B4 1,118 (enemy ≤3,000 계약). IoU: 0.8666 / 0.8711 / 0.8677 / 0.8611(전부 ≥0.82).

## D. Codex 검토 반영 내역 (2026-07-31)

| 지적 | 조치 |
|---|---|
| P1 작업 미커밋(105 untracked) | **로컬 체크포인트 커밋 8개**로 분류 보존(core/models/stage1/wiring/tests/docs). scratch·dist 는 gitignore. 푸시는 금지 유지 |
| P1 chase3dTest=legacy 혼동 | 오버레이 첫 줄 경고 명시(임시 완화). **hero 결정 장면(chase3dHeroTest) 신설은 출시 전 필수 항목으로 이관** |
| P1 최종 통합 계측 부재 | §C 로 재계측 완료(구 수치 3/16/19 폐기) |
| P2 보고서 모순 | 본 통합 문서 신설, 구 문서는 일지 격하 |
| P2 `_in3d` 게임 객체 오염 | **WeakMap sidecar** 로 교체(HW-12 회귀: 소스 전체에 `_in3d` 필드 부재 증명) |
| P2 진단 부족 | info() 에 b4Count·propCounts 추가 |
| 소품 3D 시각 이득 작음 | **기본 OFF(2D 복귀)** — 이사 지시와 일치. `chase3dProps=1` A/B 전용 |
| 신규 모델 중단 | 준수 — 본 배치는 정리·계측만(신규 모델 0) |

## D-2. Codex 재검토(8/1) 반영 내역

| 지적 | 조치 |
|---|---|
| P2-1 소품 OFF 가 표시만 OFF(자원은 생성) | `createChase3D(canvas, { hero, propsEnabled })` — false 면 지오·재질·InstancedMesh·instanceColor 자체를 미생성. 실측 OFF: programs 6→**5**, geometries 33→**25**(-8), propCounts **null**. HW-12 에 게이트 회귀 추가 |
| P2-2 기본 URL 에 3D 코드 38.9KB 정적 로드 | 순수 상수를 `chase3d-prop-defs.js`(의존 0, 0.7KB)로 분리 — main 은 defs 만 정적 import, 팩토리(→aurora-geometry 30.9KB)는 플래그 뒤 renderer 만 로드. 기본 URL 리소스 실측: aurora·props·three·모델·렌더러 **0개**. HW-12 에 정적 import 회귀 추가 |
| P2-3 커밋 메시지≠실제 diff (`d91ffcc`) | 인정 — 해당 커밋은 수정하지 않고(이력 보존), **본 배치부터 커밋 메시지는 그 커밋의 diff 만 기술** |
| P3 scratch 잔존 | 불용 프로브 2개(`scratch_b1_probe`·`scratch_geo_probe`) 삭제. QA 서버 2개(`scratch_nocache_srv`·`scratch_capsrv2`)는 세션 QA 에 계속 사용 중이라 유지(gitignore 상태) |

## E. 미검증 (정직 고지)

- 실제 rAF 지속 FPS(pane rAF 정지 → 스텝 계측으로 대체). 실기기(진짜 모바일 하드웨어) 미측정.
- CrazyGames `dist` ZIP 의 hero 런타임 실행(빌드 검사·크기만 확인).
- B1 다수전(6기+) 가독성, AURORA 반응로 크기(Codex 화면 조언 — 축소 10~20% 권고) — **이사 실기 확인 후 조정 예정**.
- 850 테스트는 계약·수학·배선 검증이지 "실플레이 850상황"이 아님(실플레이는 위 브라우저 실측·이사 실기).

## F. 출시 전 필수 (남은 순서 — Codex 권고 채택)

1. hero 결정 검증 장면 `chase3dHeroTest=1` 신설(AURORA+B1/B2/B4+loss+4단계+좌중우 한 화면) / legacy 는 `chase3dLegacy=1` 로 개명
2. ~~실기기 QA~~ → **완료(8/1)**〔이사 실기〕: 이사 Android 폰, Cloudflare 임시 터널 경유(테스트 후 즉시 폐쇄), **섹터 1 완주 — 끊김 없음·발열 없음·터치 정상·사망 후 재출격 정상**. 계측기 없는 체감 평가이나 실사용자 기준 통과.
3. ~~CrazyGames dist 실행 QA~~ → **완료(8/1)**〔브라우저〕: dist 로컬 서빙에서 부팅·영어 UI(Vulcan 카드)·1클릭 전투 진입·220% hero·**B1 4+B2 1+B4 1 동시 3D 표시**(`_dist-enemies3d.png`)·소품 OFF 자원 격리(programs 5/geo 25/propCounts null — 소스와 동일)·**사망→항로 화면 전환→재진입 재전투 3D 정상**(rendered true·degraded false·programs 불변, `_dist-death/retry.png`)·콘솔 error 0·request failure 0. *한계: 항로 노드 2탭 확정의 DOM 클릭 자동화가 스크립트에선 state 전환을 못 시켜 재진입은 dev 훅 경로로 검증 — 손 터치 재도전은 실기기 QA(2번)에서 함께 확인.*
4. ~~가독성 A/B~~ → **이사 실기 판정으로 대체(8/1, §G)**: 방향 확정 — "후미 모드는 전부 제대로 3D". 단 품질 기준 미달이던 구 소품 3D(기각분)는 폐기하고 **고품질 재구현**으로 진행.
5. AURORA 반응로 축소(10~20%)·상판 무늬 완화, B1 다수전 크기 감쇠 — 이사 확인 후
6. (별도 승인 시) 기본 URL 활성화 여부 결정

## G. 이사 실기 피드백 백로그 (8/1 실기기 테스트 직후 — "나중에 반영")

1. **아군 함선 차별화**: 순양함과 드론의 디자인을 다르게 — **드론은 더 단순화**(현재 둘이 비슷하게 읽힘). 위계: 기함(대·정교) > 순양함(중) > 드론(소·단순).
2. **후미 모드 전면 3D 화**: 기함·순양함·드론뿐 아니라 **발칸·레이저·유도미사일·드론 발사체·적·적 발사체·배지·크리스탈 등 전부** 3D 로. 
   - 구 소품 3D 가 기각된 원인(다이아 컨페티·정보 전달 부족)을 감안해, 이번에는 크리처 파이프라인 수준의 품질 기준(원본 그림 기반 형상·발광 설계·근접 판독성)으로 종별 재구현.
   - 레이저는 3D 빔 표현(단순 스트릭 대체) 설계 필요. 남은 적(터렛·위버·B3·보스 등)도 이 범위에 포함 — 종별 순차.

**신규 3D 제작은 §G 백로그 승인 순서에 따라 재개(코덱스 "중단" 판정은 이사 실기 판정으로 갱신됨). 단 품질 관문(검사실·IoU·실전 캡처·이사 확인)은 유지.**

### G-1 진행 기록 — 아군 드론·순양함 3D (8/1 구현 완료, 이사 확인 대기)

- **파이프라인**: 구 소품 기각 교훈대로 창작 조형 금지 — 원본 스프라이트 `assets/styleC/A1.png`(드론)·`A2.png`(순양함)를 알파 디지타이즈(64행 런, fixture `tests/fixtures/allies-planform.json`)해 반폭 프로파일 로프트. 모듈 상수=fixture 일치를 AL-00 테스트로 보증(단일 진실).
- **모델**: 드론 180tri(원본 코쿤+델타 실루엣, 콕핏·쌍발 엔진 glow만 — "단순화" 계약 ≤200tri) / 순양함 532tri(어깨 장갑함+측면 포드+함교+은 패널 trim+콕핏 세로창·쌍발 엔진 glow, ≤800tri). 위계: 기함(금 트림) > 순양함(은 패널) > 드론(무패널). 3점 가중 스무딩으로 디지타이즈 지그재그의 표면 밴딩 제거, flatShading 패널감, emissive 미광(실전 어두운 조명에서 아군 식별).
- **배선**: `Squad.escortsFor3D`(draw 동일 수식) → main 수집(projectPoint 정합) → renderer 인스턴스 스웜. t≥0.5에서 2D 본체 blit만 스킵(hideEscorts — HP바·플래시 링·무리 라벨 유지), 150% 이하는 기존 2D 그대로(실측 확인).
- **실측 교정 2건**: ① 검사실에서 타이틀 state가 overlay를 되살려 검사 대상을 덮음 → lab 모드 `#stage>*:not(#game3d)` 고정 숨김 ② 첫 배선에서 아군이 거꾸로 비행(엔진이 전방) → 지오 z 반전(노즈 +z=소실점, 감김·노멀 복구) 후 정상.
- **검증**: 855/855(AL-00~04 신설 포함), 실전 220% 실측 droneCount 2·cruiserCount 1·t 1(draws 13·tri 19,156 — 기함 단독 대비 +5 draw), dist 재빌드. 캡처: `docs/qa/chase3d-opus5-aurora/allies-lab-top.png`(검사실 위계 전시)·`allies-play-final.png`(실전 220%)·`allies-play-150-2d.png`(2D 폴백)·`allies-drone-zoom.png`(순양함 확대). 커밋 d57e0e2.
- **대기**: 이사 실기 확인(`?chase3d=1` 220%) → 다음 §G-2(무기 발사체 3종).

### G-1 재작업 기록 (8/1 저녁 — 이사 "2D 드론과 너무 다르고 기함 대비 단순" 반영, 커밋 2e1d955)

- **원인 진단 2건**: ① 참조 원본 오류 — 구 `styleC/A1.png`(게임이 안 쓰는 그림)를 디지타이즈했었음. 게임 2D 는 `art2-webp/styleC/A1.webp`(257×512, aspect 0.492 길쭉한 델타 인터셉터)·`A2.webp`(478×512)를 로드 ② 재질 격차 — 크리처는 절차 텍스처인데 아군만 민무늬 단색.
- **재구축**: planform 을 실제 webp 재디지타이즈(유도 규칙=행 최외곽, A2 는 날개+측면 나셀 한 장·틈은 텍스처 음영·포드 박스 폐지) + **원본 그림을 그대로 albedo 로**(정점 (x,z)→그림 (u,v) 투영 UV, CanvasTexture 로 투명부를 헐 색으로 채워 굽기, 종별 재질 분리). 드론 192tri·순양함 484tri.
- **결과**: 상면에서 2D 스프라이트와 동일한 패널·금 스파인·청록 라인이 3D 입체 위에 얹힘 — "2D 와 같은 배가 3D 로 서 있는" 목표 달성. 검사실 `allies-lab-top-tex.png` / 실전 220% `fleetlab-220-tex.png`(드론 49기+순양함 2척). 856/856·dist 재빌드.
- **확인 경로**: `?chase3d=1&fleetlab=1`(함대 완비 즉시 전투 — 정상 초반은 호위가 xN 라벨이라 관찰 불가, §G-1 이전부터의 기존 규칙) / 검사실 `?chase3d=1&chase3dLab=allies&labView=top`.

### G-2 진행 기록 — 무기 발사체 3종 3D (8/1 밤 구현 완료·이사 확인 대기, 커밋 cbe6573)

- **이사 승인 방식 그대로**: 2D 가 blit 하는 원본 그림(`nf2_proj_vulcan/homing/laser_base.webp`)을 납작 방추 로프트(각 72tri)에 planform UV 로 입히고, 합성도 2D 와 동일한 가산(AdditiveBlending — 'lighter'). 투명부는 가산 0 이라 알파 처리 자체가 불필요.
- **실측 교정 2건**: ① 진화색 instanceColor 틴트가 원본 주황 탄두를 초록으로 왜곡 → 발사체는 흰색(원색). 진화 구분은 후속(진화별 텍스처 3장) 과제로 명시 ② 레이저 랜스가 캔버스 여백까지 입혀져 뚱뚱 → 반폭 0.55 캡으로 그림 중앙(크리스탈)만.
- **레이저 처리 결정**: §G 백로그의 "3D 빔 설계" 대신 **원본 랜스 발사체 그대로**(2D 도 랜스 그림을 날림 — §G-1 교훈 '원본 우선'). 공명 레일만 2D 전용 렌더 유지.
- **배선**: 레이저 수집 추가(kind 'laser'), PROP_KEYS 9종(laser 캡 48), fleetlab 은 소품 3D 자동 ON. 856/856·dist 재빌드.
- **캡처**: `g2-vulcan-220.png`(주황 탄두 로켓 탄막)·`g2-laser-220.png`(청록 크리스탈 랜스)·`g2-homing-220.png`(은색 4핀 미사일). 모두 fleetlab 220% 실측.
- **대기**: 이사 실기 확인 → §G-3(배지·크리스탈·보급선).

### G-3 진행 기록 — 픽업류 3D (8/1 밤 구현 완료·이사 확인 대기, 커밋 6e3d8fc)

- **같은 방식**: 파일이 있는 3종은 원본 그대로(크리스탈=C1.png **가산**으로 발광 halo 보존 / 보급선=C5.png / 캡슐=C2.png 알파 컷아웃), 파일이 없는 절차 벡터 2종(POW 배지·코인)은 **2D draw 와 동일한 모양·색을 캔버스에 구워** 텍스처로.
- **지오**: 세로 카드 프리즘(12tri) — 'spin' y 축 회전에서 앞뒤 그림+두께감, 알파가 실루엣대로 컷아웃. 구 파라메트릭(옥타·토러스 등— 소품 기각의 원인) 전면 폐지.
- **함정 예방**: 텍스처 재질 인스턴스색=흰색(§G-2 에서 배운 틴트 왜곡 함정).
- **실측**: 크리스탈(파일 텍스처 대표, `g3-pickups-220.png` — '+3' 라벨 유지)·코인(굽기 텍스처 대표, `g3-pickups2-220.png`). pod·pow·capsule 은 동일 파이프라인 공유 — 자연 스폰이 드물어 개별 실측은 이사 실기에서 확인. 856/856·dist 재빌드.
- **§G 진행 현황**: ①아군 ✔ ②무기 발사체 ✔ ③픽업 ✔ — 남은 것 ④적탄(ebullet 원본화)·터렛·위버·B3·보스 종별 순차.

### G-3 재조형 기록 (8/2 — 이사 "2D 이미지 회전은 3D 가 아니다" 반영, 커밋 c5630f8)

- **8/2 순수 조형 재설계(커밋 47dfcd1)**: 이사 "크리스탈이 애매하게 깨지고 수송선 어설픔 — 2D 이어붙이지 말고 3D 완전 재작업. 적 발사체·적들도 마찬가지" → 픽업 그림 텍스처 전면 폐기. createPickupParts 멀티 파트(지오+재질 순수 조형): crystal=결정면 쌍뿔+백열 코어(104tri)/pod=팔각 드럼+리브 8+발광 창(160tri)/coin=금속 원반+금 코어/pow=육각+버튼/capsule=알약+발광 밴드. **원칙 확정: §G-4(적탄·적)도 그림 부착 금지, 순수 조형.** 실측 g3r2-final-220.png. 856/856.
- **8/2 품질 상향(커밋 5065486)**: 이사 "2000년대 초반 3D 그래픽 같다" 기각 → 원인=저폴리 프리미티브+단색 재질. 크리처급 파이프라인 적용: 소품용 절차 텍스처 포지(forgePropTex — perlin/fbm/heightToNormal, 결정적)+PBR. 크리스탈=결정면 로프트(지터·트위스트)+내부 결·백열 emissive 그라데이션+IBL 반사(과노출 1회 교정) / 수송선=챔퍼 팔각 셸+패널·리벳 노멀맵+리브·프레임·해치·발광 창 / 코인=문양 양각+금 PBR / POW=베벨 육각 / 캡슐=금속 캡+유리 몸통+발광 코어. 예산: 탄 ≤200 유지·픽업 파트합 ≤600(cap 8~48). 확대 실측 g3r3-crystal-zoom2.png.
- (기록) 1차 재조형: 카드 프리즘(얇은 박스 앞뒤 그림)은 판때기였음을 인정 — 전 픽업을 실제 조형으로: crystal=팔각 쌍뿔 보석 커트(세로 랜스형, 원본 그림 원통 감기, 96tri) / pod=두툼한 팔각 컨테이너(깊이 0.55) / coin=옆면 금테 원반 / pow=육각 프리즘 / capsule=팔각 프리즘. 재질·수집·배치 불변. 실측 g3r-*.png. 856/856.

### fleetlab 수정 기록 (8/2, 이사 실기 제보 2건 — 커밋 e9d9049)

- 증상: 막대 게이트 재등장 + (구버전) 모듈 선택 후 진행 불능. 원인: ①fleetlab 이 정상 진입의 installGate1(공명 reson) 미설치 → 게이트·캡슐·모듈 드래프트의 구형 규칙 부활(스킵 조건이 전부 !reson) ②devDirect 에서 enterSectorMap 조기 반환 → 노드 완료가 무효화되는 무한 정지.
- 수정: startFleetLab 에 installGate1+resonSetLoadout(정상 플레이 동일 상태), completeNode 는 devDirect 면 다음 열 노드로 직행(관찰 루프). 실측: 게이트 0·노드 자동 연결(track→flythrough→track)·보상 1회. 856/856.
- ⚠️일반 사용자 영향 없음 — 정상 플레이는 reson 이 항상 설치되고 devDirect 도 아님. fleetlab/bosslab 전용 경로 결함이었음.
