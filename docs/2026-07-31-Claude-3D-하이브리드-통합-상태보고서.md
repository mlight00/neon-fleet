# 네온 함대 실제 3D 하이브리드 — 통합 상태보고서

- 작성일: 2026-07-31, **갱신 2026-08-01**(Codex 재검토 `2026-08-01-…재검토결과.md` P2 3건 반영)
- 브랜치: `claude/neon-fleet-analytics-stage1` (**푸시·배포·업로드 없음**, 로컬 커밋만)
- 이전 문서 `2026-07-30-…AURORA-재구현-완료보고.md` 는 시간순 **작업 일지**로 격하 — 수치·상태는 본 문서가 단일 진실.
- 표기: 〔자동〕=테스트/스크립트 실측, 〔브라우저〕=로컬 Chrome 실측, 〔육안〕=사람 판독, 〔추론〕=미실측 추정.

## A. 현재 적용 (기본 동작 — `?chase3d=1`)

| 항목 | 상태 |
|---|---|
| 공개 기본 URL(플래그 없음) | **3D 비활성** — three·hero 렌더러·모델·소품 팩토리 로드 0(8/1 리소스 실측). 잔류는 플래그 파서(chase3d-config 3.5KB)와 순수 상수(chase3d-prop-defs 0.7KB)뿐 — 8/1 재검토가 지적한 38.9KB 정적 체인(aurora-geometry+props) 절단〔브라우저〕 |
| 3D 레이어(=`chase3d=1`) | 기함 AURORA + 크리처 B1(소형)·B2(중형)·B4(저격수)만 3D. 그 외 전부 기존 2D |
| 기함 스왑 | t=0.5 하드 컷(겹침 0·공백 0, HW-02 수학 증명) + 워프 플래시. 180% 정착 배율 제거(줌 4단계 1.0/1.5/2.0/2.2) |
| 카메라 | 부분 추종 0.42(기함이 화면에서 실제 이동, 3D 트럭 정합), 기함 통과 개체 depth 1.6 외삽 퇴장 |
| 배경 | 추적 시 세로 별·먼지·삼각 파편·항적 소등 → 소실점 방사 워프 별(결정적 110) |
| 스웜 배치 | 2.5D 투영 화면좌표·크기를 unproject 정합 — 위치·원근이 2D 세계와 픽셀 정합. 종당 InstancedMesh 3(=+3 draw) |
| 정보 보존 | 변이(엘리트) 개체는 2D 유지(링·표식), 초과분 2D 폴백, 피격핵·판정·입력·규칙 완전 불변 |
| 실패 복귀 | WebGL context loss → `degraded` → 2D 게임 계속〔브라우저, Codex 독립 재검증 일치〕 |

## B. 개발 전용 (기본 OFF)

| 플래그 | 내용 |
|---|---|
| `chase3dProps=1` | 탄 3종·픽업 5종 소품 3D — **이사 실기 "엉망" + Codex "가독성 A/B 통과 전 2D 유지" 판정으로 기본 OFF**. 8/1 재검토 반영으로 OFF 는 **GPU 자원 생성까지 OFF**: 실측 OFF=programs 5·geometries 25·propCounts null ↔ ON=6·33·활성(pbullet 34 등)〔브라우저〕. 코드·테스트는 유지 |
| `chase3dLab=aurora·b1·b2·b4·swarm` | 모델 검사실(고정 카메라·시드) |
| `chase3dTest=1` | ⚠ **LEGACY 구형 조립식 장면** — 최신 hero 가 아님(오버레이 첫 줄에 경고 명시). 최신 QA 는 `?chase3d=1` 실게임으로 |

## C. 최신 통합 계측 (2026-07-31, 데스크톱 Chrome 682×1100, 소품 OFF 기본 구성)〔브라우저〕

| 지표 | 값 | 비고 |
|---|---|---|
| 프레임 p50 / p95 | **0.90 / 1.9 ms** | update+draw+3D 전체, 900프레임 |
| 프레임 p99 / worst | 11.2 / 18.8 ms | 스폰 웨이브·GC 순간 스파이크(예산 16.7ms, p99 이내) |
| programs | **6** (3회 측정 동일) | 플레이 중 셰이더 증가 0. ※구 보고서의 3 은 AURORA 단독 시점 |
| geometries / textures | **33 / 28** | 100↔220% **20회 왕복 후 불변**(누수 0) |
| draw calls / tris | 11 / 21,564 | AURORA 8 + B1(2기 시점) 3 |
| 프리웜 | 495 ms | 출격 전 1회(전투 중 컴파일 히치 0) |
| 진단 | `__NF.chase3dInfo()` | b1/b2/b4Count·propCounts 노출(Codex P2 반영) |

모델 예산〔자동〕: AURORA LOD0 18,264 / B1 1,650 / B2 1,934 / B4 1,118 (enemy ≤3,000 계약). IoU: 0.8666 / 0.8711 / 0.8677 / 0.8611(전부 ≥0.82).

## D. Codex 검토 반영 내역 (2026-07-31)

| 지적 | 조치 |
|---|---|
| P1 작업 미커밋(105 untracked) | **로컬 체크포인트 커밋 8개**로 분류 보존(core/models/stage1/wiring/tests/docs). scratch·dist 는 gitignore. 푸시는 금지 유지 |
| P1 chase3dTest=legacy 혼동 | 오버레이 첫 줄 경고 명시(임시 완화). **hero 결정 장면(chase3dHeroTest) 신설은 출시 전 필수 항목으로 이관** |
| P1 최종 통합 계측 부재 | §C 로 재계측 완료(구 수치 3/16/19 폐기) |
| P2 보고서 모순 | 본 통합 문서 신설, 구 문서는 일지 격하 |
| P2 `_in3d` 게임 객체 오염 | **WeakMap sidecar** 로 교체(HW-12 회귀: 소스 전체에 `_in3d` 필드 부재 증명) |
| P2 진단 부족 | info() 에 b4Count·propCounts 추가 |
| 소품 3D 시각 이득 작음 | **기본 OFF(2D 복귀)** — 이사 지시와 일치. `chase3dProps=1` A/B 전용 |
| 신규 모델 중단 | 준수 — 본 배치는 정리·계측만(신규 모델 0) |

## D-2. Codex 재검토(8/1) 반영 내역

| 지적 | 조치 |
|---|---|
| P2-1 소품 OFF 가 표시만 OFF(자원은 생성) | `createChase3D(canvas, { hero, propsEnabled })` — false 면 지오·재질·InstancedMesh·instanceColor 자체를 미생성. 실측 OFF: programs 6→**5**, geometries 33→**25**(-8), propCounts **null**. HW-12 에 게이트 회귀 추가 |
| P2-2 기본 URL 에 3D 코드 38.9KB 정적 로드 | 순수 상수를 `chase3d-prop-defs.js`(의존 0, 0.7KB)로 분리 — main 은 defs 만 정적 import, 팩토리(→aurora-geometry 30.9KB)는 플래그 뒤 renderer 만 로드. 기본 URL 리소스 실측: aurora·props·three·모델·렌더러 **0개**. HW-12 에 정적 import 회귀 추가 |
| P2-3 커밋 메시지≠실제 diff (`d91ffcc`) | 인정 — 해당 커밋은 수정하지 않고(이력 보존), **본 배치부터 커밋 메시지는 그 커밋의 diff 만 기술** |
| P3 scratch 잔존 | 불용 프로브 2개(`scratch_b1_probe`·`scratch_geo_probe`) 삭제. QA 서버 2개(`scratch_nocache_srv`·`scratch_capsrv2`)는 세션 QA 에 계속 사용 중이라 유지(gitignore 상태) |

## E. 미검증 (정직 고지)

- 실제 rAF 지속 FPS(pane rAF 정지 → 스텝 계측으로 대체). 실기기(진짜 모바일 하드웨어) 미측정.
- CrazyGames `dist` ZIP 의 hero 런타임 실행(빌드 검사·크기만 확인).
- B1 다수전(6기+) 가독성, AURORA 반응로 크기(Codex 화면 조언 — 축소 10~20% 권고) — **이사 실기 확인 후 조정 예정**.
- 850 테스트는 계약·수학·배선 검증이지 "실플레이 850상황"이 아님(실플레이는 위 브라우저 실측·이사 실기).

## F. 출시 전 필수 (남은 순서 — Codex 권고 채택)

1. hero 결정 검증 장면 `chase3dHeroTest=1` 신설(AURORA+B1/B2/B4+loss+4단계+좌중우 한 화면) / legacy 는 `chase3dLegacy=1` 로 개명
2. ~~실기기 QA~~ → **완료(8/1)**〔이사 실기〕: 이사 Android 폰, Cloudflare 임시 터널 경유(테스트 후 즉시 폐쇄), **섹터 1 완주 — 끊김 없음·발열 없음·터치 정상·사망 후 재출격 정상**. 계측기 없는 체감 평가이나 실사용자 기준 통과.
3. ~~CrazyGames dist 실행 QA~~ → **완료(8/1)**〔브라우저〕: dist 로컬 서빙에서 부팅·영어 UI(Vulcan 카드)·1클릭 전투 진입·220% hero·**B1 4+B2 1+B4 1 동시 3D 표시**(`_dist-enemies3d.png`)·소품 OFF 자원 격리(programs 5/geo 25/propCounts null — 소스와 동일)·**사망→항로 화면 전환→재진입 재전투 3D 정상**(rendered true·degraded false·programs 불변, `_dist-death/retry.png`)·콘솔 error 0·request failure 0. *한계: 항로 노드 2탭 확정의 DOM 클릭 자동화가 스크립트에선 state 전환을 못 시켜 재진입은 dev 훅 경로로 검증 — 손 터치 재도전은 실기기 QA(2번)에서 함께 확인.*
4. ~~가독성 A/B~~ → **이사 실기 판정으로 대체(8/1, §G)**: 방향 확정 — "후미 모드는 전부 제대로 3D". 단 품질 기준 미달이던 구 소품 3D(기각분)는 폐기하고 **고품질 재구현**으로 진행.
5. AURORA 반응로 축소(10~20%)·상판 무늬 완화, B1 다수전 크기 감쇠 — 이사 확인 후
6. (별도 승인 시) 기본 URL 활성화 여부 결정

## G. 이사 실기 피드백 백로그 (8/1 실기기 테스트 직후 — "나중에 반영")

1. **아군 함선 차별화**: 순양함과 드론의 디자인을 다르게 — **드론은 더 단순화**(현재 둘이 비슷하게 읽힘). 위계: 기함(대·정교) > 순양함(중) > 드론(소·단순).
2. **후미 모드 전면 3D 화**: 기함·순양함·드론뿐 아니라 **발칸·레이저·유도미사일·드론 발사체·적·적 발사체·배지·크리스탈 등 전부** 3D 로. 
   - 구 소품 3D 가 기각된 원인(다이아 컨페티·정보 전달 부족)을 감안해, 이번에는 크리처 파이프라인 수준의 품질 기준(원본 그림 기반 형상·발광 설계·근접 판독성)으로 종별 재구현.
   - 레이저는 3D 빔 표현(단순 스트릭 대체) 설계 필요. 남은 적(터렛·위버·B3·보스 등)도 이 범위에 포함 — 종별 순차.

**신규 3D 제작은 §G 백로그 승인 순서에 따라 재개(코덱스 "중단" 판정은 이사 실기 판정으로 갱신됨). 단 품질 관문(검사실·IoU·실전 캡처·이사 확인)은 유지.**

### G-1 진행 기록 — 아군 드론·순양함 3D (8/1 구현 완료, 이사 확인 대기)

- **파이프라인**: 구 소품 기각 교훈대로 창작 조형 금지 — 원본 스프라이트 `assets/styleC/A1.png`(드론)·`A2.png`(순양함)를 알파 디지타이즈(64행 런, fixture `tests/fixtures/allies-planform.json`)해 반폭 프로파일 로프트. 모듈 상수=fixture 일치를 AL-00 테스트로 보증(단일 진실).
- **모델**: 드론 180tri(원본 코쿤+델타 실루엣, 콕핏·쌍발 엔진 glow만 — "단순화" 계약 ≤200tri) / 순양함 532tri(어깨 장갑함+측면 포드+함교+은 패널 trim+콕핏 세로창·쌍발 엔진 glow, ≤800tri). 위계: 기함(금 트림) > 순양함(은 패널) > 드론(무패널). 3점 가중 스무딩으로 디지타이즈 지그재그의 표면 밴딩 제거, flatShading 패널감, emissive 미광(실전 어두운 조명에서 아군 식별).
- **배선**: `Squad.escortsFor3D`(draw 동일 수식) → main 수집(projectPoint 정합) → renderer 인스턴스 스웜. t≥0.5에서 2D 본체 blit만 스킵(hideEscorts — HP바·플래시 링·무리 라벨 유지), 150% 이하는 기존 2D 그대로(실측 확인).
- **실측 교정 2건**: ① 검사실에서 타이틀 state가 overlay를 되살려 검사 대상을 덮음 → lab 모드 `#stage>*:not(#game3d)` 고정 숨김 ② 첫 배선에서 아군이 거꾸로 비행(엔진이 전방) → 지오 z 반전(노즈 +z=소실점, 감김·노멀 복구) 후 정상.
- **검증**: 855/855(AL-00~04 신설 포함), 실전 220% 실측 droneCount 2·cruiserCount 1·t 1(draws 13·tri 19,156 — 기함 단독 대비 +5 draw), dist 재빌드. 캡처: `docs/qa/chase3d-opus5-aurora/allies-lab-top.png`(검사실 위계 전시)·`allies-play-final.png`(실전 220%)·`allies-play-150-2d.png`(2D 폴백)·`allies-drone-zoom.png`(순양함 확대). 커밋 d57e0e2.
- **대기**: 이사 실기 확인(`?chase3d=1` 220%) → 다음 §G-2(무기 발사체 3종).

### G-1 재작업 기록 (8/1 저녁 — 이사 "2D 드론과 너무 다르고 기함 대비 단순" 반영, 커밋 2e1d955)

- **원인 진단 2건**: ① 참조 원본 오류 — 구 `styleC/A1.png`(게임이 안 쓰는 그림)를 디지타이즈했었음. 게임 2D 는 `art2-webp/styleC/A1.webp`(257×512, aspect 0.492 길쭉한 델타 인터셉터)·`A2.webp`(478×512)를 로드 ② 재질 격차 — 크리처는 절차 텍스처인데 아군만 민무늬 단색.
- **재구축**: planform 을 실제 webp 재디지타이즈(유도 규칙=행 최외곽, A2 는 날개+측면 나셀 한 장·틈은 텍스처 음영·포드 박스 폐지) + **원본 그림을 그대로 albedo 로**(정점 (x,z)→그림 (u,v) 투영 UV, CanvasTexture 로 투명부를 헐 색으로 채워 굽기, 종별 재질 분리). 드론 192tri·순양함 484tri.
- **결과**: 상면에서 2D 스프라이트와 동일한 패널·금 스파인·청록 라인이 3D 입체 위에 얹힘 — "2D 와 같은 배가 3D 로 서 있는" 목표 달성. 검사실 `allies-lab-top-tex.png` / 실전 220% `fleetlab-220-tex.png`(드론 49기+순양함 2척). 856/856·dist 재빌드.
- **확인 경로**: `?chase3d=1&fleetlab=1`(함대 완비 즉시 전투 — 정상 초반은 호위가 xN 라벨이라 관찰 불가, §G-1 이전부터의 기존 규칙) / 검사실 `?chase3d=1&chase3dLab=allies&labView=top`.

### G-2 진행 기록 — 무기 발사체 3종 3D (8/1 밤 구현 완료·이사 확인 대기, 커밋 cbe6573)

- **이사 승인 방식 그대로**: 2D 가 blit 하는 원본 그림(`nf2_proj_vulcan/homing/laser_base.webp`)을 납작 방추 로프트(각 72tri)에 planform UV 로 입히고, 합성도 2D 와 동일한 가산(AdditiveBlending — 'lighter'). 투명부는 가산 0 이라 알파 처리 자체가 불필요.
- **실측 교정 2건**: ① 진화색 instanceColor 틴트가 원본 주황 탄두를 초록으로 왜곡 → 발사체는 흰색(원색). 진화 구분은 후속(진화별 텍스처 3장) 과제로 명시 ② 레이저 랜스가 캔버스 여백까지 입혀져 뚱뚱 → 반폭 0.55 캡으로 그림 중앙(크리스탈)만.
- **레이저 처리 결정**: §G 백로그의 "3D 빔 설계" 대신 **원본 랜스 발사체 그대로**(2D 도 랜스 그림을 날림 — §G-1 교훈 '원본 우선'). 공명 레일만 2D 전용 렌더 유지.
- **배선**: 레이저 수집 추가(kind 'laser'), PROP_KEYS 9종(laser 캡 48), fleetlab 은 소품 3D 자동 ON. 856/856·dist 재빌드.
- **캡처**: `g2-vulcan-220.png`(주황 탄두 로켓 탄막)·`g2-laser-220.png`(청록 크리스탈 랜스)·`g2-homing-220.png`(은색 4핀 미사일). 모두 fleetlab 220% 실측.
- **대기**: 이사 실기 확인 → §G-3(배지·크리스탈·보급선).

### 방향 전환(8/2 이사): 2D 원본 제약 해제 — "3D 를 멋지게 만들어 2D 를 교체"

- 이후 신규·재작업 3D 는 원본 2D 계승 의무 없음(자유 디자인). 완성되면 역으로 2D 를 3D 렌더로 교체하는 방향.
- §G-3 자유 재디자인(커밋 e1e276e): crystal=MeshPhysicalMaterial 진짜 유리 굴절(transmission·ior 1.9·attenuation — three 공식 문서·Codrops 조사 반영, r160 지원 실측) / pod=밝은 흰 화물 컨테이너+주황 스트라이프(구속구 인상 해소) / coin=밝은 금 / pow=스타버스트+발광 후광+'POW' 텍스트 원판. 실측 g3r5-redesign.png.
- capsule 은 현행 섹터 무기 조합(reson) 모드에서 스폰되지 않는 레거시(무기 캡슐·전력 모듈 — S3 에서 정예 POW 강화로 대체) — 정밀화 후순위.

### G-3 진행 기록 — 픽업류 3D (8/1 밤 구현 완료·이사 확인 대기, 커밋 6e3d8fc)

- **같은 방식**: 파일이 있는 3종은 원본 그대로(크리스탈=C1.png **가산**으로 발광 halo 보존 / 보급선=C5.png / 캡슐=C2.png 알파 컷아웃), 파일이 없는 절차 벡터 2종(POW 배지·코인)은 **2D draw 와 동일한 모양·색을 캔버스에 구워** 텍스처로.
- **지오**: 세로 카드 프리즘(12tri) — 'spin' y 축 회전에서 앞뒤 그림+두께감, 알파가 실루엣대로 컷아웃. 구 파라메트릭(옥타·토러스 등— 소품 기각의 원인) 전면 폐지.
- **함정 예방**: 텍스처 재질 인스턴스색=흰색(§G-2 에서 배운 틴트 왜곡 함정).
- **실측**: 크리스탈(파일 텍스처 대표, `g3-pickups-220.png` — '+3' 라벨 유지)·코인(굽기 텍스처 대표, `g3-pickups2-220.png`). pod·pow·capsule 은 동일 파이프라인 공유 — 자연 스폰이 드물어 개별 실측은 이사 실기에서 확인. 856/856·dist 재빌드.
- **§G 진행 현황**: ①아군 ✔ ②무기 발사체 ✔ ③픽업 ✔ ④적탄 ✔(G-4-A)·터렛 ✔·위버 ✔(G-4-B/C) — 남은 것 B3(대형 크리처)·보스(B22·B7) 종별 순차.

### G-5 실모델(GLB) 파이프라인 기록 (8/5~8/6 — 이사 확정: "얼굴 보조광만 추가하고 전체 진행")

- **파이프라인**: Gemini 이미지 → VARCO 3D화(리메시 50K 필수 — 무리메시 원본은 UV 아일랜드로 단순화 불가) → `scripts/glb_diet.py`(리메시본=단순화 스킵+양자화+1K/512 텍스처, 종당 ~2MB) → `chase3d-glb.js` 로드.
- **투입 8종**: b1~b4(크리처·저격수)·b5(터렛)·b6(위버)·b17(재퍼)·b18(오비터). → 이후 b16(1d42212)·b19~b21(90c9611)로 **적 12종 전체 실모델 완성**.
- **얼굴 방향 확정(3중 시행착오)**: ①양자화 GLB 로드 실패(getComponent 부재→r160 getX/Y/Z/W 변환, interleaved 직접 순회=파편)가 빌보드 폴백을 불러 "차이 없음" 오판 유발 ②세움(rotX 0)+face(lookAt) 대면 ③rotY π(VARCO 얼굴=+z, face 는 -z 를 카메라로) — 확대 판독으로 눈·힘줄·부리 확정.
- **얼굴 보조광**: faceLight(카메라 쪽→전방 디렉셔널 1등) — face 대면이라 조명 하나로 전 종 얼굴 조도.
- **포털 빌드**: GLB 제외(50MB 예산) — 404 시 빌보드 자동 폴백. 실모델 포함은 포털 최적화 단계(사운드 재인코딩 등) 재결정.
- 진열장 `chase3dLab=models`(격자 8종·π 오프셋 흔들 — 진행 없이 평가). 실측 g5-face-crop·g5-facelight.png. 863/863.

### G-6 보스 6종+아군 2종 실모델 — 게임 전 개체 실 3D 완성 (8/6, 커밋 66a6ac8→16fee12)

- **투입**: 보스 6종(b7 하이브 퀸·b22 네온 아비터 len 414/max 830, b8~b11 중간보스 len 371/750, 전부 cap 1) + 아군 `ALLY3D` a1 드론(cap 60)·a2 순양함(cap 10). 이사 VARCO 구독 토큰으로 리메시 50K 일괄(종당 1.7~2.3MB). **적 12 + 보스 6 + 아군 2 = 전 전투 개체 실모델.**
- **보스 배선**: main 수집에 Boss/MidBoss 분기 — spriteId(또는 def.id)→ENEMY3D 키, 로스터 밖 보스는 2D 유지 가드. 이름 라벨은 HUD 계층 putLabel 로 보존(EN 분기 인라인). 실측 g6-boss-ally2.png(B8+라벨)·g6-b7.png(화면 압도).
- **아군 배선**: `makeGlbSwarm(key, def, fallback)` 인자화 — GLB 로드 실패 시 기존 디지타이즈 조형(createDrone/CruiserGeometry) 폴백. 포털 빌드는 GLB 제외라 자동 폴백 동작.
- **아군 방향 규약(실측 확정): rotX -π/2 + rotY π** — 첫 투입(rotX -π/2만)은 이사 실기에서 "드론 위아래 반전"(기수가 화면 아래·엔진광 위). rotY π 로 기수만 180° 돌려 +z(소실점) 정렬, 갑판은 카메라 쪽 유지. ⚠️rotX 부호 반전으로 고치면 기수는 맞지만 배 밑면이 보임 — 금지(16fee12 주석 명문화). 실측 g7-drone-fixed-crop.png(드론 49기·순양함 2척 전부 기수 위·엔진광 뒤).
- 863/863·포털 빌드 46.6MB. 남은 제작 후보=픽업류(크리스탈·보급 수송선 — Gemini 프롬프트 우선)·기함 AURORA(선택, 현행도 승인작). 보스 페이즈 연출(부위 파괴 등)은 실전 보스전 이사 확인 후 개별 조정.
- **충돌 섬광(8/6 이사 요청, 9d12d40)**: 적 몸체 충돌·적탄 명중(내구도 지불) 순간 기함 기수 HUD 섬광(백열 코어+주황 충격파 링+위쪽 부채꼴 스파크, 0.32s) — 신호=squad.flash 상승 에지(takeShot 이 세트, 로직 무수정·표시 전용), 3D(t≥0.5) 전용(2D 는 기존 링·burst 유지), 보호막 방어·reduced-motion 미발화. noteSwap/drawSwapFlash 패턴 복제(noteImpact/drawImpactFlash). 실측 g7-impact.png. 864/864.
- **VARCO 5종 프롬프트(8/6)**: 픽업 4종(P1 크리스탈·P2 보급수송선·P3 코인·P4 POW — 글자 금지)+A0 기함(현행 3D 크롭 첨부 리렌더, 실루엣 유지 지시)을 `E:\workspace\claude\neon-fleet\assets\3d\프롬프트_픽업기함_5종.md`로 전달. 참고 이미지 4장=`제작원본\참고_픽업기함\`. 완성 규약=p1~p4_50k.glb·a0_50k.glb. 기함은 A/B 비교 후 교체 원칙.

### G-7 픽업 4종 실모델 + 기함 A/B + 기함 등급 로드맵 (8/6, 커밋 742341c)

- **픽업 4종 투입**: 이사 VARCO 5종 수령(당일) → glb_diet(p1 1218·p2 1517·p3 2118·p4 2076·a0 2044KB) → `PICKUP3D`(chase3d-prop-defs, cap=PROP_CAPS 동일) → renderer 픽업 생성 GLB 우선(`makeGlbSwarm(k, PICKUP3D[k], 조형 폴백)`), capsule=레거시 조형 유지. 'spin' 배치·behindFlag(기함 뒤) 그대로. 실측 g7-pickups-real.png — 실전에서 수송선(장갑 컨테이너+창문)·크리스탈(내부 결 유리)·코인(문양 금화)·POW(스타버스트) 동시, 라벨(▲N·+N·POW) 보존.
- **⚠️진열장 1종 함정**: createGlbShowcase 격자가 3열 고정 공식이라 단독 진열(a0)이 왼쪽 칸(x=-1.5, scale 2 곱해 화면 밖)에 놓여 "draw 0/빈 캡처" — 종 수 기준 열 수(`COLS=min(3, keys)`) 중앙 정렬로 수정. 12종 진열은 결과 동일.
- **기함 A/B**: `chase3dLab=a0`(VARCO 단독 진열, 아군 규약 rotX -π/2+rotY π) ↔ `chase3dLab=aurora`(현행 절차 조형) — labView=top 비교판 g7-flagship-ab.png. VARCO 쪽도 금 반응로 링·금 스파인·델타·청록 엔진 정체성 재현 확인. **이사 판정 대기** — 승인 시 hero 기함 교체 배선(뱅크 롤은 그룹 회전으로 이식, 차지 글로우 연출은 별도 검토).
- **기함 등급 로드맵**: 기함=6등급(EMBER/FLARE/ARCLIGHT/AURORA/ZENITH/QUASAR — balance.js names, 2D 아트 A1~A6). 3D hero 는 현재 등급 무관 AURORA 단일 → 티어 스왑 배선 필요. T0/T1=드론(a1)·순양함(a2) 동일 아트 재사용, **신규 제작=T2/T4/T5** — 프롬프트 `프롬프트_기함티어_3종.md`(참고=A3/A5/A6 변환 3장, 완성명 t2/t4/t5_50k.glb) 전달, 이사 생성 대기.
- **기함 가림 교정(8/6 이사 "적·크리스탈 수송선이 기함 정면에 부딪히는 느낌이 아니라 위로 지나감", 2445459)**: 원인=근접 개체 깊이 클램프 하한 6 < 기함 시선깊이 실측 11.2(기수 13.4·함미 8.8, 전장 4.78) — z-buffer 가 큰 적·픽업을 항상 기함 앞에 그림. 교정=placeSwarm `behindFlag`(적 eswarm·픽업 spin 만): depth<FB(기함 원점깊이+FLAG_NOSE_CLEAR 2.8)면 [6,FB)→[FB,FB+1.5) 단조 리매핑 — 화면 위치(카메라 광선상 이동)·크기(scl 보정) 불변, 개체 간 앞뒤 순서 유지, 겹침 구간에서만 기함이 개체를 가려 "기수에 부딪히고 날개 뒤로 스치는" 그림. 아군(갑판 위 편대)·아군탄(기함에서 발사)·적탄(기함에 명중)은 기함 앞 유지. 테스트 HW-13 신설(+HW-10/11·B56-05 갱신) → 864/864. 실측 g7-occl-final.png(크리처가 기수 뒤로 파고듦·크리스탈 날개 뒤 반가림).

### G-18 레이저 굵기 재교정 — 기함이 안 보이던 원인 (8/8, 869/869)

- 이사 실기 "함미모드?" — 220% 인데 기함이 보이지 않는다는 지적. 원인은 **§G-15 에서 내가 레이저를 과교정**한 것이다.
- **실측**: 전장 3.4 · 굵기 0.5 → 폭이 **1.7 unit = 월드 28px**. 2D 빔 굵기(`beamW` 4~9px)의 **4배**다. 레이저는 연사라 볼트가 겹치는데, 그 굵기로 겹치면 기함 바로 위에 **흰 기둥**이 쌓여 함선을 통째로 가린다(재현 `g18_paused.png` — 3D 기함은 정상 렌더 중이었고 t=1·flagTier=0 이었는데 화면에서만 안 보였다).
- **교정**: 전장 3.4 → **1.70**, 굵기 0.5 → **0.17**(≈월드 4.8px, 2D beamW 대역), 최소 화면 전장 30 → 18px. 실측 `g18_cmp.png` — 기함이 드러나고 레이저는 소실점으로 뻗는 가는 광선이 된다.
- **교훈**: 발사체 크기는 "2D 에서 그 무기가 실제로 쓰는 굵기(beamW·스프라이트 폭)"를 기준으로 잡을 것. 눈대중 배수는 근거리에서 화면을 덮는다.

### G-17 위험물 4종 실모델 투입 + 포털 예산 재확보 (8/8, 869/869)

- 이사 VARCO 생성분(h1 운석·h2 잔해·h3 돌격기·h4 기뢰) 수령 → 개발본 다이어트(1.8~2.1MB)·검사실 진열 확인·실전 투입. 도형 폴백(주황 팔각형·원·삼각형)이 전부 실모델로 교체됐다. 실측 `g17_hazards_play.png`.
- **h4 재수령분 교체(정식 50,000 tri)**: 1차 수령분은 파일명만 50k 이고 실제 499,908 tri 라 ratio 0.10 으로 줄여 썼다. 이사 재생성분으로 교체 — 개발본 1,715KB / 포털 421KB. **수령 즉시 tri 수를 먼저 재는 절차를 표준화**한다(이름을 믿지 않는다).
- **포털 예산 재확보**: 위험물 4종 경량본 1.31MB 를 더하면 **50.87MB 로 게이트 초과**였다. 보스·기함은 화면에 크게 나오므로 손대지 않고, **화면에 100~200px 로 작게 그려지는 일반 적 12종만** 재다이어트(11K tri·256 tex) → 7.43 → 4.05MB(**-3.38MB**). 육안 확인 `h_portal_enemies.png` — 실루엣·무늬 손상 없음.
- **결과: 47.49MB / 50MB**(여유 2.5MB 회복), 파일 275개, ZIP 43.15MB. dist 실서빙 GLB 37종 200 OK.
- 869/869 유지(코드 변경 없음 — 모델·경량본 교체와 검사실 타깃 추가만).

### G-15/G-16 이사 실기 4건 교정 + 위험물 4종 배선 (8/8, 869/869)

- **(3) 섹터 종료에서 기함이 두 척**: `flythrough` 는 2D 기함이 화면 위로 빠져나가는 연출인데 3D 기함은 카메라에 고정돼 제자리에 남는다 → `flythrough`·`cutscene` 구간은 3D 레이어를 통째로 내린다(`cinematic` 게이트). 실측 `g16_cmp.png` 오른쪽 — 3D 기함 사라지고 2D 기함만 날아간다.
- **(4) 먼저 죽은 보스의 빈 붉은 칸**: 보스 HP 바는 배경(`rgba(255,61,113,0.25)`) 위에 채움을 그리는데, 죽은 보스는 hp 0 이라 **배경만 남아 붉은 사각형**이 됐다 → 살아있는 보스만 바에 넣는다(`liveBosses`).
- **(1) 발사체 2D↔3D 차이**
  - 발칸 색: 3D 가 원본 주황 로켓 텍스처를 입어 2D(백열 코어가 커 흰색으로 읽힘)와 어긋났다 → 발칸·레이저는 **텍스처 미적용**(`createProjMaterial(…, false)`) + 2D 탄색을 instanceColor 로 전달.
  - 레이저 "풍선": 전장 2.0·굵기 1.0 이라 통통했다 → **전장 3.4 · 굵기 0.5배**(가늘고 길게).
  - 유도탄 "너무 작다" → 0.86 → **1.5**.
  - "화면 끝까지 안 가고 중간에 사라짐": 2D 는 `KIND_SCALE.minRel` 로 탄의 최소 표시 크기를 보장하는데 3D 는 진짜 원근이라 바닥이 없었다 → **화면 최소 전장(`SHOT_MIN_PX`)** 도입.
- **(2) 도형으로 남은 위험물 4종**: 운석(Meteor)·잔해(Debris)·돌격기(Charger)·기뢰(Mine)는 **아트 파일이 아예 없다**(운석 스프라이트 `C4` 는 파일 부재 → 주황 팔각형 폴백). ENEMY3D 에 `h1~h4` 엔트리와 main 수집을 배선하고, **모델 준비 게이트**(`ctl.swarmReady`)를 넣어 GLB 가 없으면 3D 로 올리지 않고 기존 2D 를 유지한다(수집만 하고 모델이 없으면 개체가 통째로 사라지는 사고 방지). 프롬프트 `프롬프트_위험물_4종.md` 전달 — **이사 생성 대기**.
- 테스트 HW-17·HW-18 신설 → **869/869**. 포털 49.56MB/50MB.

### G-14 픽업 3D 상시 + 개발 플래그 폐기 (8/7, 867/867)

- 이사 지시 "픽업도 3D 로 켜줘". 기본 OFF 였던 근거는 **임시 조형 시절의 실기 판정("무기·크리스탈·수송함 엉망")**인데, §G-3/§G-7 에서 순수 조형 재설계 → VARCO 실모델 교체로 그 전제가 사라졌다.
- 크리스탈·보급 수송선·코인·POW·캡슐을 상시 3D 로. **정보 라벨(+N·▲N·POW·무기명)은 HUD 계층에 그대로** 남아 판독이 유지된다(실측 `g14_pickup_zoom.png` — 3D 결정 보석 + "+1", 컨테이너 + "▲3").
- **죽은 스위치는 남기지 않는다**: `chase3dProps` URL 플래그와 `propsEnabled` 옵션을 config·main·renderer에서 완전히 제거했다. 이제 3D 레이어에 표시 게이트는 없다(기함·적·아군·발사체·픽업 전부 상시).
- 포털 49.55MB/50MB, 867/867.

### G-13 발사체 상시 3D + "던지는 느낌" 원인 교정 (8/7, 867/867)

- **이사 지적**: "후미추적 모드에서 내 발사체가 곡선으로 날아간다. 레이저도 발칸도 유도탄도 포물선을 그리며 끝으로 발사되어 무기를 집어 던지는 느낌."
- **원인(코드 실측)**: 발사체는 3D 도입 이후에도 계속 2D 계층에서 그려졌는데, `chase-render.js`의 `drawProjected` 는 **translate+scale 만 하고 회전을 하지 않는다.** 즉 화면 어디에 있든 탄 그림이 "똑바로 선" 채로 놓인다. 소실점으로 수렴해야 할 탄이 축만 화면에 붙어 있으니 옆으로 미끄러지는(던져지는) 것처럼 읽힌다.
- **⚠️같이 확인한 반증**: 2.5D 투영 자체는 월드 직선을 화면 직선으로 정확히 보낸다(잔차 0px, 16점 최소제곱 실측). 즉 "투영이 휘게 만든다"는 가설은 **틀렸다** — 처음 세운 가정이었으나 측정으로 폐기했다. 화면에 남는 줄기의 곡선은 기함이 움직인 궤적 그 자체이고(월드에서 실제로 굽은 줄), 이는 어떤 투영에서도 같다.
- **조치**: 발사체 4종(발칸탄·적탄·미사일·레이저)을 **개발 플래그와 무관하게 상시 3D** 로 올리고(`SHOT_KEYS`, renderer 의 `shots`), 배치를 두 출처로 나눴다.
  - **화면 위치 = 2.5D 투영 좌표**(적·기함·픽업이 모두 이 좌표계라, 발사체만 월드로 놓으면 가로 퍼짐이 1.5배 커져 탄이 적을 빗나가 보인다 — 실측 396px vs 270px)
  - **깊이·자세 = 월드 좌표**(깊이는 `(squadY−worldY)×SCALE_Z + 카메라 거리`, 자세는 진행각으로 실제 회전) → 소실점으로 곧게 날아가고 거리만큼 작아진다.
- **⚠️축 규약 2건(실측으로 잡음)**: ①추적 카메라의 화면 오른쪽은 **월드 −X** 다(lookAt 기저) — 보정 없이 두면 왼쪽에서 쏜 탄이 오른쪽에 나타난다. ②절차 발사체 지오는 **노즈가 −z**(loftProjectile 주석) — 미사일 GLB 도 `rotY 0` 으로 같은 규약에 맞췄다. 최종 yaw = `π − wob`.
- **⚠️작업 중 사고 1건**: 렌더러를 블록 단위로 치환하다 `prewarm`·`placeSwarm`·`applyCam`·`pixelRatioFor` 를 통째로 지워 3D 가 통으로 죽었다(`prewarm is not defined`). `git checkout` 후 앵커 기반 부분 편집으로 재적용 — **큰 함수 사이를 인덱스로 잘라내는 치환 금지**.
- 픽업 3D 는 기존대로 개발 플래그(`chase3dProps=1`) 유지. 테스트 HW-16 신설 + HW-10/11/12 갱신 → **867/867**. 포털 49.56MB/50MB. 실측 `g13b_weapons.png`(3무기), `g13b_straight.png`(월드 직선→화면 직선).

### G-12 기함 무기 5종 실모델 + 차지 연출 배선 교정 (8/7, 866/866)

- **배경**: 이사 지시 "모든 기함 무기와 차지 때 나타나는 이펙트도 3D 로". VARCO 로 만들 수 있는 것(포탑·탄체·포구)만 실모델로 받고, 빛·빔·폭발은 코드가 담당한다는 구분을 먼저 정리해 프롬프트 `프롬프트_무기_5종.md` 전달 → 이사 5종 생성(w1~w5).
- **회전은 전부 진열장 실측**(`?chase3d=1&chase3dLab=weapons`): 30° 12칸 스윕 + w2 만 6° 정밀 스윕. 결과 `WEAPON3D` = w1 발칸 π / w2 레이저 **-π/4**(비축 포신) / w3 유도 0 / w4 미사일 π / w5 포구 0. 바운딩박스는 앞뒤를 못 가린다는 §G-8 교훈 그대로.
- **갑판 포탑(w1~w3)**: `mountRig`(flagRig 자식 → 뱅크 롤·흔들림 공유) + 무기 종당 InstancedMesh 1. 장착점은 2D `NORMALIZED_MOUNTS` 사본(`MOUNT_POINTS`, 테스트가 두 표의 일치를 잠금), 개수 규칙도 2D `drawWeaponRig` 과 동일(`min(len, 1+tier+max(0,lv-1))`). 현재 무기 1종만 켜므로 드로우 +1.
- **⚠️좌표 환산**: 장착점은 2D 스프라이트 폭 기준이라 3D 선체에 그대로 쓰면 얇은 날개 끝에 걸친다 → x×0.88 안쪽, y=기함 바운딩 상단×0.66(선체 곡면 위에 얹히되 뜨지 않는 높이). 실측 g12b_t4_bow.png 로 3무기 비교.
- **차지 포구(w5)**: chargeRig 자식이라 충전·발사 동안만 전개(상시 표시는 기함 실루엣을 바꾼다). **조리개를 전방(+z)으로 두면 함미 추적 카메라에는 뚫린 뒷면만 보여 검은 덩어리**가 된다(실측) → 리그에서 180° 돌려 조리개가 카메라를 향하고 집속 코어가 링 안에서 빛나게.
- **⚠️미사일(w4)**: 소품 경로(`chase3dProps=1`) 안이라 **기본 플레이에서는 아직 2D**다. 소품 3D 기본 OFF 는 이사 실기 피드백으로 정한 것이라 임의로 켜지 않았다 — 켤지 말지는 이사 판단.
- **§G-11 차지 연출 실플레이 미표시 교정**: 렌더러가 `sq.charging`·`sq.chargeFrac` 만 읽었는데 **실전 Squad 에는 그 필드가 없다**(검사용 합성 run 전용). 실제 상태는 `charge`(누적 초)·`chargeStage` → `charge>0` 이면 켜고 `BAL.charge.stageTime×maxStage` 로 정규화. 즉 §G-11 연출은 그동안 실게임에서 한 번도 뜨지 않았다.
- **기수 기준점도 등급 연동**: `NOSE_Z` 상수 2.6(T3 기준)이라 T4·T5 는 기수가 더 길어 코어·포구가 선체에 묻혔다 → 등급별 실측 바운딩(`box.max.z × 홀더배율 + 0.35`).
- **포털 경량본 5종 추가**: w3 은 43K tri·1.7MB 라 상자형(면이 넓어 UV 파손 위험 낮음)만 예외적으로 `-sa` 허용, w1/w2/w5 도 육안 확인 후 적용 — 5종 합계 **1.57MB**. 포털 빌드 47.97 → **49.55MB / 50MB**(여유 0.45MB). ⚠️다음 3D 추가는 예산 확보가 선행돼야 한다.
- 테스트 HW-15 신설 → **866/866**. 실측 w1~w5 진열·`g12b_t4_bow.png`(3무기 갑판 비교)·`g12f_cmp.png`(충전 vs 발사).

### G-10 배포판(CrazyGames 포털)에 실모델 3D 포함 (8/7, 커밋 673e745, 865/865)

- **배경**: 3D 는 전 개체 실모델이 됐지만 포털 빌드는 50MB 예산 때문에 GLB 를 통째로 제외해, 배포판은 2D 스프라이트로만 돌아갔다(추적 시점에서 아군이 위에서 본 그림). 이사 결정 "사운드 재인코딩해서 3D 넣어줘".
- **어디를 줄일지 실측이 먼저**: 사운드 28.50MB 중 **BGM 21곡이 27.96MB, 효과음 48개는 0.55MB**. 즉 용량은 전부 BGM — 효과음은 손댈 이유가 없었다. BGM 만 Vorbis q1(120→71kbps)로 재인코딩해 **17.09MB**(-11.4MB).
- **포털 전용 경량 GLB 28종**(`assets/3d/portal/`, 17.83MB): diffuse 512·JPEG q84~85, 메시 15~20K. **개발본은 손대지 않는다**(assets/3d/*.glb, 종당 ~2MB·50K tri) — 검사실·실기 확인은 계속 고품질로 본다. `glb_diet.py` 를 `--tex/--quality/--noaggr` 로 파라미터화해 두 벌을 같은 스크립트로 뽑는다.
- **⚠️`-sa`(aggressive simplify) 금지**: 리메시된 균일 토폴로지는 일반 단순화로 형태가 유지된다. -sa 는 UV 아일랜드를 무시해 "녹은 무늬"를 만든 전력이 있다(§G-5 실측) → `--noaggr` 기본.
- **빌드 배선(remap)**: 매니페스트에 소스→dist 재배치 맵을 도입해 `assets/3d/portal/x.glb` 를 dist 의 `assets/3d/x.glb` 로 내보낸다. **런타임 코드·경로 수정 0**, 경량본이 없으면 매니페스트에 안 들어가고 404 → 빌보드 폴백(기존 동작 유지). 복사·ZIP·텍스트검사 루프 모두 `distPath()` 경유로 통일.
- **부수 수정**: 타이틀 엠블럼(`branding/emblem.webp`) 404 — ui.js 가 `<img>` 로 직접 참조하는데 매니페스트에 없던 기존 누락. dist 실서빙 QA 로 발견해 함께 수정.
- **결과**: 압축 전 **47.96MB / 50MB**(3D 18.69 + 사운드 17.09), ZIP 43.24MB, 파일 266개. dist 실서빙 QA — GLB 28종 전부 200 OK, 404 0건, 3D 렌더 정상(기함 T2 + 드론 49 + 순양함 2, 818K tri). 865/865. 실측 g10-dist3d.png.
- ⚠️운영 함정: dist 서버가 폴더를 잡고 있으면 재빌드가 `EPERM`(rmSync) — 서버를 먼저 정지할 것. BGM 원본 백업 `E:/workspace/claude/neon-fleet-audio-backup-20260807/`.

### G-9 2D 스프라이트 26종 → 3D 실모델 렌더 교체 (8/7, 커밋 c875478, 865/865)

- **배경**: 3D 는 전 개체 실모델이 됐는데 2D(추적 미만 배율·전술 시점)는 구 아트라 같은 개체가 다르게 보였다. 이사 지시 "3D 이미지들도 2D 에 적용해서 일관성을 유지" → 8/2 방향 전환("3D 를 멋지게 만들어 2D 를 교체")의 완결.
- **파이프라인 2단**: ①`tools/sprite-render.html?run=1` — 실전과 같은 IBL·조명(faceLight 대응 정면광 포함)으로 GLB 를 **정사각 768 타이트** 렌더(개발 전용, 빌드 매니페스트가 명시 목록이라 tools/ 는 배포 제외) ②`scripts/sprite_finalize.py` — 채도·밝기·대비 보정 + 실루엣 외곽 발광 + `fit_to_reference`(원본 캔버스 규격·알파 bbox 채움비·중심 재현).
- **구도 계약**: 적·보스·픽업 = front(2D 원본이 "뿔 위·부리 아래" 정면 실루엣) / 기함·아군 = top + **18° 기울임**(순수 직하방은 함선이 뭉툭해 원본의 날렵함이 죽는다 — 실측).
- **⚠️규격 사고(재발 방지)**: 첫 시도에서 캔버스 크기를 추정으로 렌더 도구에 넣었다가 **20종이 원본과 어긋남**. 게임 blit 배율·히트박스 표시가 원본 종횡비에 묶여 있어(SHIP_ART_ASPECT = [257/512, 478/512, …]) 그대로면 개체 크기가 전부 틀어진다. → 렌더는 규격을 모르게 하고(정사각), 규격 정합은 파이썬이 **백업 원본을 기준으로** 수행하는 구조로 재작업. 재검증 불일치 0종.
- **⚠️마감 필수**: 3D 렌더를 그대로 넣으면 사실적 PBR 이라 2D 아트(어두운 우주에서 즉시 식별되도록 발광·채도를 과장한 그림)보다 어둡고 밋밋해 "예쁘지만 안 보이는" 스프라이트가 된다. 크리처=마젠타 후광, 아군·기함=청록, 크리스탈=강한 청록 발광으로 원본의 식별 신호를 계승.
- **기함 특이사항**: A1~A6 는 본체 위에 무기 리그·지휘 프레임 오버레이가 겹겹이 얹혀 화면상 차이가 거의 없다(실측 g9-flag-ab.png A/B 거의 동일). 부작용도 없어 일관성 목적으로 교체 유지.
- 교체 26종(적 12·보스 6·기함/아군 6·픽업 2). 원본 백업 `E:\workspace\claude\neon-fleet-2d-art-backup-20260807\`. 865/865, 포털 빌드 **46.62 → 41.65MB**. 실측 g9-sample-final·g9-play2d.png.

### G-8 기함 실모델 6등급 스왑 + 피격 파편·흔들림 (8/6~7, 커밋 c5fc1b1, 865/865)

- **기함 A/B 판정**: 이사 "B 로 하자" → VARCO 실모델 채택. 이어 **6등급 전체 실모델화**: `FLAG3D` 테이블(T0 a1·T1 a2 재사용 / T2 t2 ARCLIGHT·T3 a0 AURORA·T4 t4 ZENITH·T5 t5 QUASAR). renderer 에 `flagRig`(홀더 6 + 절차 AURORA) — `squad.tier` 로 가시성 스왑, 로드 전·실패는 절차 AURORA 폴백(포털 빌드=GLB 제외라 자동 폴백).
- **⚠️회전 규약 상항화(실측)**: GLB 원본 자세는 종별로 다르다 — 바운딩박스 최대축으로 판별할 것. **y 최대=서 있음**(a1 0.50/1.00/0.23, a2 동일) → rotX -π/2 필요. **z 최대=이미 누움**(t2 0.92/0.35/1.00, t4·t5 동형) → **rotX 금지**(적용 시 전장 1.67·높이 5.04 의 '세워진 벽'이 됨 — 첫 배선 실측으로 발견). a0 는 x 최대(넓적) → rotX -π/2 로 갑판을 위로.
- **등급 위엄**: `TIER_SCALE [0.62, 0.75, 0.88, 1.00, 1.13, 1.26]` = 2D SHIP_VISUAL_SIZE[34..140]/88 의 √ 압축. 원비(T5 1.59배)는 화면 초과로 구도 파괴 — 압축해야 T5 까지 들어온다(실측 g8-tier-scale.png).
- **피격 연출 교체(이사 "충돌 이펙트는 정말 별로다 — 부품이 떨어져 나가고 기체가 흔들리게")**: 9d12d40 의 HUD 섬광(noteImpact/drawImpactFlash)을 **완전 제거** → 3D 장면 내 장갑 파편 InstancedMesh(최대 16, 결정적 LCG — Math.random 금지 계약 유지) + `flagRig` 감쇠 진동 0.42s. 신호는 종전과 같은 `squad.flash` 상승 에지(게임 로직 무수정·보호막 방어 시 미발화·reduced-motion 미발화).
  - ⚠️파편 스폰 위치: 함체 내부(z 1.1~2.5)에서 시작하면 기함에 가려 보이지 않는다 — 기수 앞·갑판 위(z 1.6~2.8, y 0.5~1.4)에서 튀게 교정, 크기도 기함 전장 대비로 상향(s 1.1~2.2).
  - 카메라는 흔들지 않는다(멀미 방지) — 함체만 진동.
- 검사실 `chase3dLab=flags`(6등급 격자). ⚠️격자 진열은 **labView=front** 여야 한다 — top 뷰는 y축을 내려봐서 행이 깊이로 겹쳐 3종만 보임(실측).
- 865/865, 포털 빌드 46.62MB. 실측 g8-tier-grid·g8-tier-scale·g8-debris2-crop·g8-flags-front.png.

### G-4 적 발사체·터렛·위버 기록 (8/2 — 이사 "이 기준으로 §G-4 적 발사체와 적들도 진행해")

- **G-4-A 적탄 플라즈마(커밋 cc00584+0967eb8)**: createEnemyBulletParts 3파트 순수 조형 — 백열 코어 구(0.22 흰)+화염 외피 구(0.34, z 1.35 늘림, additive 0.5)+꼬리 콘(additive 0.42). 전 파트 흰색 지오 + instanceColor 로 적 종별 탄색 틴트(colored:true 유지). 전장 1.0. 실측 `g4-ebullet-220.png`.
- **G-4-B 터렛(chase3d-b5.js 신규)**: 자유 디자인 — 팔각 요새 로프트(스커트→장갑 드럼→돔)+전방 포탑 하우징+2연장 포신(+0.12 상향)+**돔 꼭대기 용광로 구**(원본 2D "중앙 주황 용광로"의 시점 일치 재해석)+발광 링·슬릿·머즐. 512tri(LOD2 340). buckets {hull,glow,core}.
- **터렛 전용 재질 포지(createTurretMaterials)**: B1 키틴 텍스처(forgeB1Hull) 재사용하되 **roughnessMap 제거·고정 러프 0.72·envMapIntensity 0.3·자주 틴트 0xe8c2f2** — 실측 교훈: B1 재질 그대로 쓰면 매끈한 대형 곡면에서 환경맵 파랑이 면 단위로 뭉쳐 종족 자주색이 죽는다(크리처는 유기 요철이 반사를 흩어서 무사했던 것).
- **flattenUV 기법**: Box/Cylinder 의 UV 를 좁은 평탄 영역으로 눌러(scale 0.05~0.10) 절차 텍스처 홈·심이 면 전체에 크게 감기는 "모눈종이 뭉침"을 제거 — 하우징·포신·위버 팔에 적용.
- **G-4-C 위버(chase3d-b6.js 신규)**: 자유 디자인 — **xy 평면의 별**(법선 -z=카메라): 6각 허브 드럼+6방 2절 낫 팔(접선 0.75rad 꺾임)+팔끝 발광 침+전면 눈. 402tri. 1차 시도 xz 수평 팔랑개비는 실전 정면 카메라에서 엣지만 보여 폐기(실측 교훈 — 화면 평면 개체는 xy 조형).
- **시점 교훈(추적 카메라=위에서 내려봄)**: 터렛 1차 "전면 하부 용광로+아래 기울인 포신"은 돔에 가려지고 단축으로 소멸 → 용광로를 돔 꼭대기로, 포신은 위로 들어 해결. 정규화 배율(k≈0.7)을 잊고 돌출량을 잡으면 파묻힘(2차 교훈).
- **배선**: renderer B5/B6_INSTANCE_MAX 8, makeSwarm(seed 5/6)+placeSwarm 'wob', b5Count/b6Count 진단. main 수집 Turret/Weaver 분기(비변이만, LEN 105/상한 210 — B4 동일 배율감, 위버 wob=t·1.7 살랑 롤), 검사실 b5/b6 타깃(가드 9종, 스튜디오 배율 2.8 — 정면 폭 넓은 조형은 4.2 에서 잘림). 테스트 +6(B56-01~06) → **862/862**.
- **실측**: 검사실 `g4-b5-lab-final.png`·`g4-b6-lab-final.png`, 실전(fleetlab QA 주입, hp 불려 관찰) `g4-b5b6-play.png` — b5:2·b6:2 카운트 확인. 터렛 스폰은 실전에서 후반 노드부터라 이사 실기는 진행 중 자연 등장으로 확인.

### G-3 재조형 기록 (8/2 — 이사 "2D 이미지 회전은 3D 가 아니다" 반영, 커밋 c5630f8)

- **8/2 순수 조형 재설계(커밋 47dfcd1)**: 이사 "크리스탈이 애매하게 깨지고 수송선 어설픔 — 2D 이어붙이지 말고 3D 완전 재작업. 적 발사체·적들도 마찬가지" → 픽업 그림 텍스처 전면 폐기. createPickupParts 멀티 파트(지오+재질 순수 조형): crystal=결정면 쌍뿔+백열 코어(104tri)/pod=팔각 드럼+리브 8+발광 창(160tri)/coin=금속 원반+금 코어/pow=육각+버튼/capsule=알약+발광 밴드. **원칙 확정: §G-4(적탄·적)도 그림 부착 금지, 순수 조형.** 실측 g3r2-final-220.png. 856/856.
- **8/2 품질 상향(커밋 5065486)**: 이사 "2000년대 초반 3D 그래픽 같다" 기각 → 원인=저폴리 프리미티브+단색 재질. 크리처급 파이프라인 적용: 소품용 절차 텍스처 포지(forgePropTex — perlin/fbm/heightToNormal, 결정적)+PBR. 크리스탈=결정면 로프트(지터·트위스트)+내부 결·백열 emissive 그라데이션+IBL 반사(과노출 1회 교정) / 수송선=챔퍼 팔각 셸+패널·리벳 노멀맵+리브·프레임·해치·발광 창 / 코인=문양 양각+금 PBR / POW=베벨 육각 / 캡슐=금속 캡+유리 몸통+발광 코어. 예산: 탄 ≤200 유지·픽업 파트합 ≤600(cap 8~48). 확대 실측 g3r3-crystal-zoom2.png.
- (기록) 1차 재조형: 카드 프리즘(얇은 박스 앞뒤 그림)은 판때기였음을 인정 — 전 픽업을 실제 조형으로: crystal=팔각 쌍뿔 보석 커트(세로 랜스형, 원본 그림 원통 감기, 96tri) / pod=두툼한 팔각 컨테이너(깊이 0.55) / coin=옆면 금테 원반 / pow=육각 프리즘 / capsule=팔각 프리즘. 재질·수집·배치 불변. 실측 g3r-*.png. 856/856.

### fleetlab 수정 기록 (8/2, 이사 실기 제보 2건 — 커밋 e9d9049)

- 증상: 막대 게이트 재등장 + (구버전) 모듈 선택 후 진행 불능. 원인: ①fleetlab 이 정상 진입의 installGate1(공명 reson) 미설치 → 게이트·캡슐·모듈 드래프트의 구형 규칙 부활(스킵 조건이 전부 !reson) ②devDirect 에서 enterSectorMap 조기 반환 → 노드 완료가 무효화되는 무한 정지.
- 수정: startFleetLab 에 installGate1+resonSetLoadout(정상 플레이 동일 상태), completeNode 는 devDirect 면 다음 열 노드로 직행(관찰 루프). 실측: 게이트 0·노드 자동 연결(track→flythrough→track)·보상 1회. 856/856.
- ⚠️일반 사용자 영향 없음 — 정상 플레이는 reson 이 항상 설치되고 devDirect 도 아님. fleetlab/bosslab 전용 경로 결함이었음.
