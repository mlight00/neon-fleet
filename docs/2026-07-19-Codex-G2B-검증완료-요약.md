# G2-B (함체 T0~T5 기능 변화, §7.3) — Codex 검증 완료 요약

- **일자**: 2026-07-19
- **브랜치**: `claude/neon-fleet-overhaul-phase-c-core-loop`
- **검증 방식**: codex-bridge (`-Review -Base 6e373c2`, read-only, gpt-5.6-sol) — G2-B 전체 diff를 매 라운드 재검토
- **최종 결과**: **5라운드 만에 클린 통과.** 지적 건수 3 → 4 → 3 → 1 → 0 으로 수렴 (전 라운드 P1 없음, 전부 P2)
- **최종 HEAD**: `028b6c6` (origin push, master 미병합·미배포)

## 무엇을 만들었나 (G2-B)

기존 `shipTraits`(발사 개성) 위에 §7.3 등급별 기능을 누적으로 얹음. `balance.gate2.hullFn` = 6등급 × `{move, resonPower, sideGuns, apex}`.

| 등급 | 기능 |
|---|---|
| T1 | 이동 반응 강화 (entities 팔로우 속도) |
| T2 | 공명 증폭 (spawnResonance·시커 충돌 피해 ×resonPower) |
| T3 | 이동 추가 (fleet 슬롯 실효과는 G2-C) |
| T4 | 측면 포대 (campaignHullFnTick 각도 사격) |
| T5 | **Apex** (적탄 소거 + 잡몹 즉사 + 보스 광역 펄스, 주기 8.5s) |

보스 피해는 검증된 양측 클램프를 경유하므로 측면포대·Apex가 활성이어도 지역 TTK 유지.

## 라운드별 Codex 지적 → 수정

### 1차 (커밋 `1d6e7e0`, P2×3)
- 시커빔 빌드에서 resonPower 미적용(spawnResonance 아닌 충돌 missileBonus 경유) → 충돌 시 ×resonPowerMult
- Apex가 T5 승급(1260s)에서 바로 켜짐 → apex 사건(1290s)에서만 해금
- Apex 처치가 `notifyEnemyKilled` 우회 → 중앙 킬 핸들러 라우팅(FLOW·프레임·키스톤·시커 표식 정합)

### 2차 (커밋 `1b03854`, P2×4)
- 함체 기능이 유기적 tier 변화(진화·강등)와 desync → `campaignHullFnTick`이 **매 프레임 현재 sq.tier에서 재적용**
- Apex 처치 적이 사망 후 1회 더 행동(접촉/발사) → **즉시 `w.entities.splice` 제거**
- 측면포대 `'vulcan'` 태그가 공명 충전·벌컨 통계 오염 → **`'sideGun'` 별도 소스**
- Apex 보스 펄스 피해 metrics 누락 → **`'apex'` 소스로 실효 피해 기록**

### 3차 (커밋 `0a00c19`, P2×3)
- 결과 화면이 새 버킷(sideGun·apex)을 총합엔 넣고 행은 미표시 → 비율 ≠ 100% → **ui.js 두 행 추가** (발칸 36·측면 9·에이펙스 36·공명 19 = 100% DOM 확인)
- Apex 잡몹 소거 피해 미집계 → avgDps(B7 HP 스케일 기준) 과소평가 → **소거한 실효 HP를 `'apex'`로 기록** (99999 overkill 원값 아님)
- 보호막 변이(shieldCharges) 적이 Apex 1발을 보호막으로만 흡수하고 생존 → **때리기 전 `shieldCharges = 0`으로 확정 소거**

### 4차 (커밋 `028b6c6`, P2×1)
- Apex 발동이 `apexUnlocked`(해금 사건)만 게이트 → T5 밑으로 강등돼도 영구 발동. hullFn은 Apex를 T5 전용으로 표시 → **이중 게이트(`apexUnlocked && hullFn[sq.tier].apex`)**로 강등 시 다른 T5 기능처럼 멈추고, 재승급 시 재개

### 5차 = 클린
> "The hull-tier abilities, resonance scaling, side-gun attribution, Apex gating, kill handling, and result metrics are consistently wired. The full test suite and campaign integration checks pass without regressions."

## 최종 검증 (본인 재측정)

- **자동 테스트 394개 전부 통과** (`node --test`), `node --check` clean, 콘솔 무오류
- **25분 헤드리스 integrationCheck PASS**: 6지역 보스 전부 등장·처치·B7 완주(clear), 지역 TTK **31.3 / 31.3 / 33.1 / 33 / 47.2 / 63.1초** — Apex 잡몹 피해가 avgDps에 반영돼 보스 HP가 올라가도 전부 목표 대역
- **결과 패널 DOM 실측**: 피해 비율 행 합 **100%** (sideGun·apex 포함)
- 회귀 방지 테스트 `gate2-wiring` G2-09·G2-10 신설 — 2·3·4차 수정 배선을 소스로 고정

## 교훈

- **새 피해 소스를 만들면 (a) 총합 분모 (b) 결과 UI 행 (c) 공명/무기 통계 분리 (d) DPS 스케일 기준을 전부 점검**해야 한다. 하나만 놓쳐도 비율 오차·공명 오충전·보스 스케일 왜곡으로 번진다(2·3차 지적의 공통 뿌리).
- **T5 전용 기능은 별도 영구 플래그가 아니라 현재 tier 파생으로 게이트**해야 강등과 정합(4차).
- 광역 즉사(Apex 등)는 **보호막류 흡수 변이를 명시적으로 무시**해야 "확정 소거" 계약을 지킨다(3차).
