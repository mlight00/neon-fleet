The new hull functionality is wired, but one resonance build receives no amplification, Apex begins before its scheduled event, and Apex kills bypass required kill-processing logic. These cause observable campaign behavior and progression errors.

Full review comments:

- [P2] Apply resonance power to seeker-beam hits ??E:\workspace\claude\neon-fleet\js\main.js:569-569
  When the campaign uses the laser+homing `seekerBeam` build, this multiplier has no effect because seeker beam never creates a projectile through `spawnResonance`; its amplification is applied separately through the fixed `missileBonus` in collision handling. Consequently T2/T4/T5 resonance upgrades do nothing for one of the three supported resonance builds, so the seeker-hit path should incorporate `resonPowerMult` as well.

- [P2] Gate Apex pulses on the scheduled event ??E:\workspace\claude\neon-fleet\js\main.js:862-862
  When the H5 tier event fires at 1260 seconds, this assignment immediately enables periodic pulses, with the first occurring about 8.5 seconds later, even though the director defines a distinct Apex unlock at `apexSec` (1290 seconds). The scheduled event therefore no longer gates Apex and merely forces an extra pulse; keep T5 eligibility separate or set `apexUnlocked` only when the Apex event is processed.

- [P2] Route Apex deaths through the central kill handler ??E:\workspace\claude\neon-fleet\js\main.js:928-928
  Whenever Apex kills regular enemies, directly calling `hitByBullet` bypasses `onEnemyKilled`/`world.notifyEnemyKilled`, unlike the other AoE kill paths. These deaths therefore grant no FLOW, frame or keystone kill progress, and do not call `resonEnemyRemoved`, which can leave a seeker mark pointing at a removed enemy; notify the central handler after an alive-to-dead transition.
