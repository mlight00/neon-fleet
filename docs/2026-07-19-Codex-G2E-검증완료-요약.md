# G2-E (지역별 적 구성 §7.5) — Codex 검증 완료 요약

- **일자**: 2026-07-19
- **브랜치**: `claude/neon-fleet-overhaul-phase-c-core-loop`
- **검증**: codex-bridge (`-Review -Base b9bd3eb`, read-only, gpt-5.6-sol)
- **결과**: **4라운드 만에 클린 통과** (지적 1 → 1 → 1 → 0, P1 없음)
- **최종 HEAD**: `e3c12a0` (origin push, master 미병합·미배포)

## 무엇을 만들었나 (G2-E)

§7.5 "적은 HP 벽이 아니라 빌드 시험지"대로, **지역마다 다른 적 역할 조합**으로 다른 빌드 측면을 시험한다.

| 지역 | 위협 테마 | 조합(pool) | 정예 |
|---|---|---|---|
| 1 COLD WAKE | 경량 군집 · 이동/광역 | creature, weaver | creature |
| 2 | 저격·포탑 · 우선 표적 | sniper, turret, weaver | turret |
| 3 FURNACE LINE | 장갑·방패 · 표적 순서 | shielder, charger, turret | turret |
| 4 BROKEN ARMADA | 모선·궤도 · 광역/전개 | carrier, orbiter, weaver | creature |
| 5 CHOIR VEIL | 전격·점멸 · 위치 선정 | zapper, blinker, bomber | creature |
| 6 CROWN CORE | 총력 혼성 · 모든 시험 | weaver, turret, shielder, orbiter, blinker | turret |

- `balance.gate2.regionThreat[6]`(pool·elite·label) + `eliteWaveSec`(정예 웨이브 주기 100초)
- `main.js` refillCoreLoopTrack: 캠페인 전투 스트림이 현재 지역 pool에서 적 선택(측정=결정론 순환, 플레이=시간 순환). **Gate 1 불변**(측정 weaver 고정 보존)
- campaign25Update: 주기적 **지역 정예 웨이브**(refillCoreLoopTrack(true)) — 정예 변이(★ 3.2×HP) 강제로 4~8초 처치 역할
- enterCampaignRegion: 지역 진입 시 위협 테마 한 줄 안내

## 라운드별 Codex 지적 → 수정

### 1차 (커밋 `55a9ee2`, P2×1)
- 캠페인이 `refillCoreLoopTrack(true)`를 안 불러 `regionThreat[].elite`가 도달 불가(죽은 설정) → **주기적 지역 정예 웨이브** 배선 + 정예 변이 강제

### 2차 (커밋 `1d99d14`, P2×1)
- 정예 변이가 `spriteScale`·`r`을 키우는데 sniper/shielder/carrier/zapper draw는 spriteScale 무시 → 히트박스/스프라이트 불일치 → 정예 타입을 **스케일 렌더 지원 타입(creature/turret/charger)**으로 제한 + 스폰 가드

### 3차 (커밋 `e3c12a0`, P2×1)
- charger는 확대된 `this.r`로 도형을 그리면서 캔버스도 `spriteScale`로 스케일 → **1.35² 이중 스케일**(몸통 vs 충돌반경 불일치) → 정예 지원 세트를 **{creature, turret}**로 축소(정확히 1회 스케일)

### 4차 = 클린
> "The regional enemy pools and periodic elite waves are consistently wired into the Gate 2 campaign while preserving Gate 1 behavior. Forced elite affixes are restricted to compatible entity renderers."

## 최종 검증 (본인 재측정)

- **자동 테스트 398개 통과**, `node --check` clean, 콘솔 무오류
- **25분 헤드리스 integrationCheck PASS (railStorm·seekerBeam·microMissile)**: 지역별 다양한 적(방패병·모선·정예 웨이브 포함)에도 지역 보스 TTK 전부 대역(예: 32.7/34.1/34.4/33.2/48.2/63.1), B7 완주 clear
- 회귀 방지 테스트 `gate2-wiring` G2-13(pool 스폰 가능·조합 다양·Gate 1 보존·정예 웨이브·스케일 정합) 신설

## 교훈

- **어떤 적 타입에 어떤 변이(affix)를 강제할 수 있는지는 그 타입의 draw가 스케일을 어떻게 처리하는지에 달렸다.** `AFFIX_KINDS.elite` 명단이 곧 "스케일 렌더 지원" 명단은 아니었다(charger는 이중 스케일) — 강제 변이는 실제 렌더 정합을 확인한 타입만.
- **설정값(regionThreat[].elite)이 코드 경로에서 실제로 호출되는지 확인**해야 죽은 설정을 피한다(1차).
