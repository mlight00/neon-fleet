[P3] 개발 런 조기 재시작 시 BGM이 처음부터 재생되지 않습니다. [`startSector()`](E:/workspace/claude/neon-fleet/js/main.js:217)는 `r.bgmRestart=true`를 설정하지만, [`enterSectorMap()`](E:/workspace/claude/neon-fleet/js/main.js:227)이 먼저 반환해 플래그를 소비하지 않습니다. 이후 [`enterNode()`](E:/workspace/claude/neon-fleet/js/main.js:262)는 `{ restart: true }` 없이 전투곡을 요청하고, 현재 같은 곡이면 [`playBgm()`](E:/workspace/claude/neon-fleet/js/audio.js:153)이 그대로 반환합니다. 따라서 CoreLoop/Campaign25가 첫 지역에서 조기 사망한 뒤 재시작하면 이전 런의 음악이 이어집니다. `enterNode()`가 `r.bgmRestart`를 `playBgmForSector`의 `restart` 옵션으로 전달하고 즉시 해제하도록 수정하는 것이 좋습니다.

그 외 요청하신 P2·P3는 해소됐습니다.

- BossLab 사망 시 `freeKeys=[]`이며, `firstDefeatUpgrade` 저장은 무료 카드의 `onPick`에서만 발생하므로 저장되지 않습니다.
- CoreLoop/Campaign25 사망은 각각 `finishCoreLoop`/`finishCampaign25`로 차단됩니다. 다른 `newExpedition` 개발 진입 경로도 없습니다.
- 개발 부트스트랩의 `map/node/done` 상태는 온전하며 `enterNode()`는 정확히 한 번 호출됩니다.
- 일반 캠페인과 엔드리스는 새 `run`에서 `devDirect=false`이므로 첫 자동 진입과 섹터 2+ 항로 화면 모두 영향을 받지 않습니다.
- 일반 첫 안내 흐름도 유지되고, 개발 진입은 `firstGuideSeen`을 변경하지 않습니다.

검증 결과: 전체 **509/509 통과**, 수정 파일 문법 검사 및 `git diff --check` 통과입니다.

---

## 후속 조치 (총비서, 2026-07-26)

Codex가 위에서 지적한 **BGM 후속 항목**까지 반영 완료.

- **원인**: 개발 부트스트랩이 `enterSectorMap`을 건너뛰면서(devDirect), `startSector`가 세팅한 `r.bgmRestart` 플래그를 아무도 소비하지 않음 → 개발 런(CoreLoop/Campaign25) 재시작 시 전투곡이 처음부터 재생되지 않음. **개발/측정 전용 사항이며 일반 사용자 섹터 모드에는 영향 없음**(일반 진입은 `enterSectorMap`이 그대로 플래그를 소비).
- **수정**: `enterNode`가 `playBgmForSector(r.sector, { restart: !!r.bgmRestart })`로 전달하고 즉시 `r.bgmRestart = false`로 소비(js/main.js:262). 일반 플레이에선 이미 false라 동작 불변(no-op), 개발 런에서만 전투곡 재시작.
- **회귀 방지 테스트**: FR-16(devDirect 기록·조기 반환)·FR-17(개발 3진입 devDirect:true·정상 진입 제외)·FR-18(enterNode의 bgmRestart 소비) 신규 3건 + BGM-S3 정규식 갱신.
- **최종 테스트**: 전체 **512/512 통과**(509 + 신규 3).

P2·P3 및 후속 BGM 항목까지 모두 해소. 이 변경은 `claude/neon-fleet-retention-p0` 브랜치에만 있으며, 운영(master·GitHub Pages) 반영은 작업지시서 지침에 따라 보류 상태.
