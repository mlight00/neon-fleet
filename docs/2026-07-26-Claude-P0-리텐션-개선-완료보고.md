# NEON FLEET P0 리텐션 개선 완료 보고

## 1. Git
- 기준 커밋: `7dace282c5fd119dec4bed939005c71f6995598d` (origin/master, 문서 기준과 일치)
- 작업 브랜치: `claude/neon-fleet-retention-p0`
- 완료 커밋: `1c7c4b1` (26d24ce→2e5894b→5ae5bf1→1c7c4b1, 4개)
- push 여부: 예 (작업 브랜치만, `origin/claude/neon-fleet-retention-p0`)
- master 병합 여부: 하지 않음 (origin/master 여전히 7dace28)
- GitHub Pages 배포 여부: 하지 않음
- 참고: 작성 시점 로컬 체크아웃은 오래된 `claude/neon-fleet-overhaul-phase-c-core-loop`(b0a92e0)였고,
  b0a92e0는 origin/master의 조상(이미 반영됨)이라 origin/master 위에 그 뒤 21개 커밋이 얹혀 있었다.
  기존 로컬 변경·미추적 파일은 건드리지 않았다(작업지시서 .md는 untracked로 그대로 유지).

## 2. 변경 파일
- `js/save.js` — SAVE_VERSION 3, firstDefeatUpgrade 기본값·마이그레이션 백필
- `js/logic.js` — firstDefeatUpgradeOptions 순수 자격 판정 + FIRST_DEFEAT_KEYS
- `js/main.js` — enterSectorMap 루트 자동 진입(P0-B), resetToTitleState(P0-C),
  endExpedition death 자격 계산·showLoseResult 정산/UI 분리·지급 콜백(P0-A)
- `js/ui.js` — showLose 무료 개조 카드/완료 문구(P0-A), showSectorMap 터치 2단계 확정(P0-D)
- `tests/` — first-defeat-upgrade / first-root-flow / title-state-reset / sector-map-touch 신규,
  save.test SAVE_VERSION 기대값 갱신
- `docs/qa/retention-p0-20260726/results.md` — QA 실측 로그

## 3. 구현 결과
- 첫 실제 사망 무료 긴급 개조: death 한정, drones/hull/dmg 중 1개 무료(코인 미차감),
  선택 키가 지급 완료 상태 겸함, 1회 한정, 완료 문구는 지급 직후에만.
- 섹터 1 첫 루트 자동 진입: 안내 시청자는 즉시, 미시청자는 안내 후. 첫 노드 이후·섹터2+·
  루트 여럿은 항로 화면 유지.
- 타이틀 전투 잔상 제거: resetToTitleState로 run=null 등 정리. draw 전투 렌더가 if(run) 안이라
  잔상 없음. 코인 재정산 없음.
- 모바일 항로 1탭 설명·2단 확정: 터치 첫 탭 선택+설명, 두 번째 탭/확정 버튼 진입.
  마우스·키보드 1회 확정 유지. onPick 1회 가드. 고정 정보영역·고유 접근 이름·44px 유지.

## 4. 저장 호환성
- SAVE_VERSION: 3
- 구 저장 백필: saveVersion!=3이면 firstDefeatUpgrade 포함 안전 백필, get()이 항상 DEFAULTS 병합 정규화
- 중복 지급 방지: firstDefeatUpgrade!=null이면 자격 없음 + 지급 콜백 재검사 + UI 카드 첫 클릭 즉시 비활성
- 전체 초기화: reset()이 firstDefeatUpgrade·up을 기본값으로(사운드·인트로 시청 여부만 보존)

## 5. 자동 테스트
- 실행 명령: `node --test tests/*.test.mjs`
- 전체: 509
- 통과: 509
- 실패: 0
- 추가한 테스트: 24 (기준선 485 → 509)

## 6. 브라우저 QA
| 화면/시나리오 | 결과 | 콘솔 오류 | 캡처 경로 |
|---|---|---:|---|
| 데스크톱 새 사용자(A) | 통과 | 0 | docs/qa/retention-p0-20260726/results.md |
| 데스크톱 첫 사망·무료 개조(B) | 통과 | 0 | 〃 |
| 데스크톱 끝내기(C) | 통과 | 0 | 〃 |
| 데스크톱 격납고→타이틀(D) | 통과 | 0 | 〃 |
| 모바일 항로 첫 탭·확정(E) | 통과 | 0 | 〃 |
- 스크린샷은 캔버스 rAF + QA 탭 document.hidden 한계로 이미지 캡처가 타임아웃 →
  살아있는 게임 상태(window.__NF)·localStorage·오버레이 DOM 실측으로 동일 사실을 증명.

## 7. 알려진 제한과 P1 후속 제안
- QA 증거가 이미지 스크린샷이 아니라 DOM·상태 실측 로그다(환경 한계, 사실 증명은 동등).
  실기기 스크린샷은 이사님 플레이 시 확보 권장.
- (P1, 코드 미변경) 무료 개조 완료 후 다음 판 결과 화면은 일반 표시로 돌아간다.
  향후 '이번 판 무료 개조를 아직 안 씀' 배지 등 유도 강화 여지.

## 8. Codex 검증 요청
- 검증 범위: `7dace28..claude/neon-fleet-retention-p0` (커밋 26d24ce·2e5894b·5ae5bf1·1c7c4b1)
- 특히 확인 요청:
  1) 무료 개조 중복 지급 불가(연속 클릭·새로고침·두 번째 사망·재렌더)와 코인 미차감
  2) endExpedition 정산 1회 유지(showLoseResult 재렌더가 재정산 안 함, r.settled)
  3) 터치/마우스/키보드 확정 경로가 서로 오인 없이 onPick 정확히 1회
  4) 자동 진입 가드가 섹터2+·갈림길·루트 여럿에서 오작동 없는지
