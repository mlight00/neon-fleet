The new boss-damage wrapper can desynchronize HP from the boss death flags and applies STAGGER side effects for damage rejected by the cap. These defects undermine the intended B22 timing and interaction guarantees.

Full review comments:

- [P1] Recompute death state after the final clamped HP ??E:\workspace\claude\neon-fleet\js\main.js:600-604
  When an uncapped hit would kill B22 but `loss > budget`, `rawHit` has already set `dead`/`dying`; restoring `boss.hp` here leaves a positive-HP boss marked dead, so `coreLoopUpdate` records an early clear. Conversely, when the enrage subtraction crosses zero, HP becomes nonpositive without invoking the superclass death transition, and the fight continues until another hit. Derive the death state from the final HP after both clamps.

- [P2] Feed B22 only damage accepted by the cap ??E:\workspace\claude\neon-fleet\js\main.js:596-596
  Once `_dmgSec` consumes the current one-second budget, this still calls `NeonArbiter.hitByBullet` with the full damage before reducing accepted loss to zero. `staggerFromDamage(dmg)` therefore runs for attacks that remove no HP, allowing high-rate or burst builds to fill STAGGER and trigger BREAK entirely from rejected damage. Apply the cap before boss-specific hit side effects or base STAGGER on the accepted loss.
