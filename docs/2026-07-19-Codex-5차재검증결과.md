A hit that triggers BREAK can receive a different damage multiplier than the wrapper budgeted, allowing actual boss damage to exceed the intended cap. This undermines the patch's core TTK-floor guarantee.

Review comment:

- [P1] Re-evaluate the multiplier after STAGGER transitions ??E:\workspace\claude\neon-fleet\js\main.js:596-596
  When the accepted hit itself fills STAGGER?봣or example, a stage-3 lance arriving at stagger 8?봳his reads `mult` as 1, but `NeonArbiter.hitByBullet` enters BREAK before applying damage and then multiplies that same hit by 1.25. Consequently `_dmgSec` records only `norm` while HP loses `1.25 * norm`, allowing every BREAK-triggering hit to bypass the per-second cap and potentially violate the configured minimum TTK.
