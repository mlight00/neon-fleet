The adaptive TTK mechanism does not cover direct charge damage and can collapse boss HP after a sparse sample. These paths can still place the boss fight well outside its stated 45??0 second contract.

Full review comments:

- [P1] Apply TTK clamps to every boss damage path ??E:\workspace\claude\neon-fleet\js\main.js:1040-1040
  During Gate 1 play, `Squad.fireLance` and `_fireEcho` damage bosses through direct `bo.hitByBullet(...)` calls in `entities.js`, bypassing this projectile-collision branch. Consequently neither the recalibrated `dpsCap` nor `_enrageMult` applies to charge damage, so charge-oriented play can still finish below the 47-second floor or exceed the 60-second ceiling after a noisy sample; move the clamps into a shared boss-damage path.

- [P1] Retain a fallback when the DPS sample is too low ??E:\workspace\claude\neon-fleet\js\main.js:644-648
  If the player lands little or no damage during the 3??1 second sample window?봣or example by moving out of line or holding a charge??bossDps` becomes zero or very low and `maxHp` collapses to the 875 HP floor. Since the following calculation still subtracts all damage dealt before the window, an early burst can reduce the recalibrated boss to 1 HP at second 11, violating the 45??0 second TTK target; reject insufficient samples or fall back to the pre-boss estimate.
