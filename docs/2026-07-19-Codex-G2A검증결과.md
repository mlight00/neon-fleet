The campaign foundation works on its measured happy path, but several runtime paths are incomplete: boss events can be lost, hull death bypasses campaign completion, and the configured intro and hull progression are not honored. Campaign play also omits essential survivability UI.

Full review comments:

- [P1] Preserve boss events while another boss is active ??E:\workspace\claude\neon-fleet\js\main.js:790-791
  If a regional boss is still alive when the next fixed `bossStart` event fires, this guard discards the new boss permanently even though `tickDirector` has already marked its key as fired. A low-damage run can therefore skip one of the six bosses and produce an invalid campaign; queue or reschedule the event rather than returning after it has been consumed.

- [P2] Finish campaign metrics when hull is depleted ??E:\workspace\claude\neon-fleet\js\main.js:750-751
  When campaign hull reaches zero, this callback only records the metric; the existing loss branch special-cases `r.coreLoop` but not `r.campaign25`, so it calls `endExpedition()` without setting `campaign25.resultShown` or attaching `regionResults`. Consequently `__nfCampaign25.run()` continues until `maxSeconds` and returns an incomplete snapshot, while play mode shows the unrelated expedition result path.

- [P2] Keep the first minute free of shooter waves ??E:\workspace\claude\neon-fleet\js\main.js:770-772
  At campaign startup this immediately calls `refillCoreLoopTrack()`, which reads `r.coreLoop` and therefore treats a campaign run as dense measurement mode, adding two rows of weavers. They enter within seconds, violating `gate2.introSec`'s 0??0 second no-shooter tutorial contract in both campaign modes.

- [P2] Start measurement runs at H0 ??E:\workspace\claude\neon-fleet\js\main.js:758-760
  The URL entry always starts campaign mode as `measure`, and this branch initializes the visible flagship at tier 2. Since each scheduled hull event increments `sq.tier`, the H1 event produces H3, H5 is reached by the third event, and the last two events cannot advance the ship, invalidating the configured H1?밐5 progression and its measurements.

- [P2] Render the survivability HUD for campaign runs ??E:\workspace\claude\neon-fleet\js\main.js:745-745
  This installs the hull, wing, resonance, and frame systems for campaign play, but the rendering path still gates `drawCoreLoopHud` on `r.coreLoop` only. A campaign started in `play` mode therefore provides no hull gauge, secondary-weapon/resonance status, or campaign timer despite hull being the actual life resource.
