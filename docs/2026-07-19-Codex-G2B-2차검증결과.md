The new hull abilities can become desynchronized from the actual hull tier, and Apex-killed enemies may still act once after death. Side-gun source tagging and untracked Apex damage also distort resonance behavior and campaign metrics.

Full review comments:

- [P2] Reapply hull functions on every tier transition ??E:\workspace\claude\neon-fleet\js\main.js:860-862
  In campaign runs, rewards and gates can still change `sq.tier` through `Squad.checkEvolution()` or `onDronesDepleted()` independently of the scheduled hull events. Because these copied fields are updated only at campaign start and director-driven upgrades, an organic promotion receives none of its new functions until the next event, while a downgrade retains higher-tier movement, resonance, and side guns; synchronize them whenever the actual tier changes or derive them directly from `sq.tier`.

- [P2] Prevent Apex-killed enemies from updating afterward ??E:\workspace\claude\neon-fleet\js\main.js:930-930
  When an Apex pulse kills enemies, this runs before the generic `for (const e of w.entities) e.update(dt, w)` loop, which does not skip dead entities. Consequently a killed creature can still apply contact damage, and dead firing enemies can emit a final volley after the supposed screen clear; remove these entities immediately or ensure dead entities are skipped before their update.

- [P2] Keep side guns out of Vulcan weapon processing ??E:\workspace\claude\neon-fleet\js\main.js:914-914
  For T4+ runs using `railStorm` or `microMissile`, tagging each hull-mounted side-gun shot as `vulcan` makes `tallyBulletDamage()` feed those hits into `resonOnHit`, so the new hull feature unintentionally charges those resonances. It also reports Vulcan damage even for a laser+homing loadout; use a distinct or non-weapon source identifier unless side batteries are explicitly meant to behave as an equipped Vulcan.

- [P2] Record applied Apex damage in run metrics ??E:\workspace\claude\neon-fleet\js\main.js:933-933
  Every T5 boss pulse calls `hitByBullet` directly without recording the actual HP loss, so the campaign result's damage contribution omits Apex damage even though repeated pulses can account for a substantial portion of the B7 fight. Capture HP before and after the clamped hit and record that applied amount under an Apex-specific source.
