The measured happy path may complete, but the supported play path uses measurement-only wave behavior and exits into the wrong result/retry flow. Campaign metrics also omit milestones that visibly occur.

Full review comments:

- [P2] Use campaign state when refilling play-mode waves ??E:\workspace\claude\neon-fleet\js\main.js:876-876
  When `window.__nfCampaign25.start(..., 'play')` is used, `refillCoreLoopTrack()` still reads only `r.coreLoop`; for a campaign this is undefined, so every refill selects `dense = true` and `t = 0`. Play mode therefore permanently receives measurement-density waves and never reaches the play density ramp or reward-crystal branch, making the supported campaign play path substantially harder and preventing its intended growth.

- [P2] Render a campaign-specific result flow ??E:\workspace\claude\neon-fleet\js\main.js:922-922
  Whenever a play-mode campaign finishes, this reuses `showCoreLoopResult`, whose panel is hard-coded as an ??遺꾟?B22 result and whose retry callbacks call `startCoreLoop()`. Consequently both result buttons leave the 25-minute campaign and start Gate 1, while the six regional results are not presented; this path needs campaign-specific labels and restart callbacks.

- [P2] Record campaign milestones in run metrics ??E:\workspace\claude\neon-fleet\js\main.js:829-830
  The campaign equips the wing without calling `cl.metrics.secondWeapon(t)`, and the resonance and frame handlers likewise never record their corresponding milestones. Thus every `__nfCampaign25` snapshot reports `secondWeaponSec`, `firstResonanceSec`, and `framePickSec` as null even though those scheduled events occurred, making the campaign measurement output inaccurate.
