# 네온 함대 — 작업 상태 (2026-08-11)

## 🔴 진행 중: §G-34 (코덱스 5차 검토 반영) — 905/905, 미커밋

지시서 `E:/workspace/claude/neon-fleet-codex-검수요청/클로드에게_줄_G34_수정작업지시서.md`
검토서 `.../Codex_G33_5차_검토_결과.md`

### ⚠️코덱스가 뒤집은 전제 (내 §G-32/33 설계가 틀렸다)
- **A-5[치명]**: 소실점을 화면 밖으로 옮겨도 **2D 의 "화면 상단 선"이 되지 않는다.** 원근 카메라는 소실점이 안이든 밖이든 평행선이 한 점으로 수렴한다. y=-25 로 숨기면 사용자가 그 점을 못 볼 뿐이다.
  → **A′4 는 정답이 아니다.** 진짜 해법은 "화면 위치를 결정하는 **단일 projection**"을 정하고 3D 메시를 그 화면점의 광선 위에 놓는 것.
- **A-3[중대]**: A′4 는 t=0.5(3D 하드 컷 시작)에서 소실점 y=**+15.25** — 아직 화면 안. t≈0.68 이후에야 밖으로 나간다.
- **A-2[중대]**: `shotDepth` 가 전환 중에도 `CAMERA.chase.dist` 고정값을 쓴다. 실제 카메라는 high↔chase 보간 → t=0.5 에서 **2.75unit 차이**.
- **B-2[치명]**: 음수 `horizonYFrac` 은 2D 상단을 보존하는 게 아니라 **월드 상단을 화면 밖으로 숨긴다**. finite top plane 은 world y=0 에 둬야 한다.
- **C-1[치명]**: `slope` 에서만 zoom 빼면 입력 역함수가 즉시 깨진다(`_project` 의 `lateral * zoom` 과 불일치).
- **E-1[중대]**: 데드존 144 는 **기함이 월드 0/480 까지 간다는 잘못된 전제**. 실제 여백 `m = laneMargin(30) + width*0.4` ≈ 35~64px → `deadHalf = 192 − m` ≈ **128~157**(동적).
- **G-1[중대]**: `SHOT_ORIGIN` 이 정확한 화면 보정이 아니다 — generic 2.5D **11.44px 과보정**, `drawLaserStreak` **보정 0**, 3D 수집은 sidecar 를 **아예 안 읽음**.
- **G-2[중대]**: 판정 컬럼 잔광이 기함 x=420 에서 **104.4px 어긋남**, 폭도 63px(고정) vs 투영 30.49px.

### ✅ 이번에 끝낸 것
- **§9 서버 동시성(G-5[중대])** — `exists`→`os.replace` 경쟁 제거. no-overwrite 는 최종 이름을 **`O_CREAT|O_EXCL` 로 원자 예약**한 뒤 임시파일→`os.replace`. 실패 시 예약 되돌림. `request_queue_size=64`(동시 연결 거절 실측 대응).
  - 신규 `tests/g34-server-race.test.mjs` 2종: 같은 이름 6개 동시 → **정확히 1개 200 · 5개 409**, 다른 이름 4개 → 전부 200, `.part` 잔여 0.
  - ⚠️**Windows 는 열린 파일을 `os.replace` 못 한다** — `--overwrite` 동시 요청은 일부 실패가 정상. 계약은 "전부 200"이 아니라 **손상 0 · `.part` 0**.
- **§10-1 주석 정정(G-7)** — `chase3d-prop-defs.js` 의 §G-30 주석이 폐기된 실패안(3.20·0.17)을 가리키고 있었다 → 최종값 **전장 2.10 · 폭비 0.259 · 곱 0.5439** 로 수정.

### ✅ P0-1 착수분 (§4-4 · A-2[중대] 해결)
- **`shotDepth` 고정 dist 제거** — `placeShots` 가 `CAMERA.chase.dist`(9.9) 고정값을 쓰던 것을
  **이번 프레임 카메라의 원점 시선 깊이**로 교체. `applyCam` 이 매 프레임 `ctl._camOriginDepth` 를 확정한다
  (예전엔 `followPx` 가 있을 때만 계산했다).
  - 실측 차이: t=0 −0.081 / t=0.49 +0.418 / **t=0.5 +0.432** / t=0.68 +0.710 / **t=1 +1.302** unit
  - 브라우저 실측: zoom 1.8→10.332, 2.0/2.2→11.202 로 매 프레임 확정(zoom 1.6 은 t<0.5 라 3D 미표시 = 정상)
- 신규 `tests/g34-projection.test.mjs` 3종
  - `G34-CAMERA-DEPTH`: 고정값과 실제가 **모든 t 에서 다름**을 수치로 고정 + t 단조 증가
  - `G34-CAMERA-DEPTH-CLAMP`: 살아있는 탄 범위에서 `SHOT_DEPTH_MAX(80)` 미도달(Codex A-1 성질 유지)
  - `G34-HORIZON-FINITE`: **`horizonYFrac >= 0`** 계약(음수 horizon 금지 — B-2 치명)
- ⚠️`HW-16` 의 소스 문자열 대조가 또 깨졌다(내가 고친 그 줄) → **동작 기반**으로 교체.
  같은 패턴이 §G-27·§G-30 에 이어 세 번째다 — 남은 문자열 대조(§10-2)를 서둘러 없애야 한다.

### ✅ P0-1 §4-1 단일 투영 계약 (Codex D-2 해결)
- **한 프레임에 투영이 두 번 만들어지고 있었다** — `update` 시작(`input.tick` 직전)과 `draw` 에서 각각.
  같은 프레임 안에서 **입력 역변환은 "이동 전 함대", 화면 그림은 "이동 후 함대"** 기준이었다.
  카메라가 팬하기 시작하면 그 차이가 곧바로 조준 오차가 된다.
- → **`update` 끝(월드 갱신 후)에서 1회만 확정**하고, `draw`·3D 수집·followX·HUD·edge warning 이 그 스냅샷을 공유.
  입력 역변환은 다음 프레임에 **"사용자가 방금 본 그 화면"** 의 투영을 쓴다(물리적으로 맞는 순서).
  `update` 시작에는 첫 프레임 부트스트랩만 남겼다.
- 신규 테스트 3종
  - `G34-PROJ-ROUNDTRIP`: **1,350 케이스**(3뷰포트 × 6줌 × 5blend × 3기함위치 × 5화면점) 왕복 오차 **≤1px**
  - `G34-PROJ-SINGLE-SNAPSHOT`: 투영 생성이 순수함(공유의 전제)
  - `G34-PROJ-FRAME-ONCE`: 생성 호출 1곳(+부트스트랩) · draw 는 공유 · 확정은 `input.tick` 뒤
- ⚠️**소스 문자열 대조가 또 깨졌다 — 이번이 네 번째**(`AS-04`: `syncCameraAnchor`↔`input.tick` 인접 160자).
  주석을 넣었을 뿐 계약은 그대로였다 → **호출 순서 비교**로 교체.
  §G-27·§G-30·§G-34(HW-16)에 이어 같은 패턴. **§10-2(남은 3건 교체)를 우선 처리해야 한다.**
- ⚠️브라우저 pane 이 숨김이면 `requestAnimationFrame` 이 멈춰 **프레임 갱신 검증이 불가**하다.
  모듈 로드·UI·버튼은 정상 확인. **실기 조작 확인은 이사님 필요**(조준이 손에 붙는지).

### ⏭ 다음(우선순위, 지시서 §4~§8)
1. **P0-1 단일 투영 계약(§4)** — world 중심·2D 스프라이트·3D `sx/sy`·레이저·차지빔·edge warning·입력 역변환이 **한 projection snapshot** 공유. finite top plane = world y=0(음수 horizon 금지).
2. **P0-2 camera-centered 가로식(§5)** — `chaseX = screenCenterX + (worldX − cameraWorldX) * lateral(depth)`, `playerSlope = lerp(zoom, nearLateral, chaseBlend)`, slope/intercept 를 **한 번 계산해 렌더·역변환 공유**. 동적 deadHalf, hysteresis 4~8px, dt 기반 스무딩, 카메라 상태 수명 경계.
3. **P0-4 `SHOT_ORIGIN` 전 경로(§7)** — sidecar 가 월드 dx 대신 **포구 world 좌표**를 주고, 각 경로가 현재 projection 으로 두 점을 투영해 screen delta 계산.
4. **P0-3 입력·경고(§6)** — edge-scroll 계약 명시, warning 누락 종(Sniper/Turret/Weaver/Zapper/Shielder/BroodCarrier) 보완.
5. §8 lockedSprite·판정 링, §3 실패 재현 나머지(G34-ORIGIN/LANCE-COLUMN/CAMERA-TRANSITION/DEADZONE).

⚠️**제출 gate 0 = AI 생성 사운드·이미지 상업 라이선스 증빙**(자산별 출처·서비스/요금제·생성일·약관 버전). 미확인 상태에서 CrazyGames 제출 제안 금지.

## ⏸ 다음 작업(설계 확정·미착수): 카메라 데드존 + 좌우 시야 확대

이사 아이디어(첨부 그림): **화면 영역을 고정하지 말고, 기함이 좌/우 경계를 넘으면 그쪽 가려진 영역이 점진적으로 드러나게.**

### 왜 필요한가 (실측)
- 220% 3D 에서 근거리에 보이는 월드 폭 = **175px / 480px = 36%**. 2D 에 보이던 적의 **64% 가 화면 밖**.
- 원인은 원근이 아니라 **좌우 확대**: `slope = zoom(2.2) × nearLateral(1.25) = 2.75` → 480 ÷ 2.75 = 175.
- ⚠️원거리는 반대다 — 먼 곳(y=120)은 월드 전체가 화면 131~398 에 압축돼 **다 보인다**. 정작 위협적인 근거리가 안 보인다.
- ⚠️`followFrac` 을 0→1 로 바꿔도 **보이는 폭은 175px 고정**(창이 이동만 한다). 데드존만으로는 해결 못 한다.
- 기함 폭이 월드 47~92px → **시야에 기함 2~3대**뿐. 회피 기동 한 번이면 데드존을 무조건 넘는다.

### 확정한 설계 (내가 판단, 이사 위임)
1. **시야**: `slope` 에서 zoom 제거 → 2.75 → **1.25**. 보이는 월드 175 → **384px(80%)**.
   줌 220% 의 "다가간 느낌"은 깊이·크기 원근이 이미 담당한다. 좌우까지 2.2배 하면 시야만 잃는다.
2. **데드존 반폭 = 144px 월드**(시야의 37.5%, 화면 180px). 역산 근거:
   카메라 이동 여유 = 480 − 384 = 96px → 기함이 월드 끝(0)에 닿을 때 카메라도 한계에 닿으려면 240 − 96 = 144.
   결과: 기함이 **시야 가장자리 12.5%** 에 들어설 때 팬 시작, 벽에 닿을 즈음 끝까지 열림. 중앙 75% 는 완전 고정.
3. 카메라 x 를 **상태로 보관**하고 매 프레임 갱신(데드존은 상태 의존 — `createChaseProjection` 이 순수 함수라 인자로 받아야 한다).
   `C ∈ [192, 288]` 클램프(월드 밖 빈 공간은 안 보여준다) + 지수 스무딩.

### ⚠️착수 전 주의
- `proj.C0`·`proj.slope` 는 **입력 좌표 변환**(`screenToWorldXAtPlayer`, 마우스→월드)과 3D 배치(`placeOnRay` 의 화면 x)가 함께 쓴다. 카메라를 바꾸면 **셋이 같이 움직여야** 한다.
- 게임 판정은 월드 좌표라 무관하지만, **조준 입력이 어긋나면 즉시 체감**된다 — 왕복 변환 테스트 필수.

## ✅ 함께 확정된 카메라 안 (A′4, 미적용)
소실점을 화면 밖으로 보내는 안. `height 11 · lookY −3 · lookZ 15 · dist 12 · fov 52` → 아래각 27.4°(fov 반각 26° 초과) → **소실점 화면 밖**, 기함 화면 539~725(하단 여유 75px).
- 근거: **2D 는 평행 투영이라 화면 끝이 "선"**, 3D 원근은 무한대가 "점". 소실점이 화면 안에 있는 한 둘은 화해 불가(이사 통찰).
- 소실점을 화면 밖으로 밀면 화면 안에서는 모든 전방 직선이 화면 끝까지 뻗어 2D 와 같은 인상이 된다.
- ⚠️거리(dist)를 늘리면 아래각이 줄어 **소실점이 도로 화면 안으로 들어온다**. 기함을 화면 위로 올리는 손잡이는 **lookY 낮추기**(아래각도 함께 커져 두 목표가 같은 방향).
- 비교 렌더: `_shots/g32_vanishing_plans.png`(현재/A/B/C/D) · `_shots/g33_A_variants.png`(A′ 6종)
- **적용 시 `PROJECTION.horizonYFrac`(현재 0.15 = 화면 y 120)도 3D 소실점에 맞춰야 한다** — 지금 64px 어긋나 있다.

## 진행 중: §G-28 코덱스 4차 검수 수정 (901/901, 미커밋)
지시서 `E:/workspace/claude/neon-fleet-codex-검수요청/클로드에게_줄_G28_수정작업지시서.md`

### 완료 (P0 4건 중 3건)
- **P0-1 wing 좌표 복원** — ⚠️**내가 §G-23 에서 절대 계약을 위반했다.** `BAL.gate1.loadout.hardpointX.wing` 을
  시각 지적(레이저가 선체 밖) 때문에 22→8 로 바꿨는데, 이 값은 `entities._spawnWeaponShots` 의
  `this.x + hx + m.x`(레이저·발칸·미사일)와 `main.spawnResonance` 의 레일 시작 x — **게임 좌표**다.
  → 22 복원. 시각 문제는 렌더 전용으로 해결: 포구 섬광을 선체 위 마운트로 당기고,
  `SHOT_ORIGIN` WeakMap sidecar + `shotOriginDX()` 로 **탄의 첫 26px 만** 포구→실제 위치로 보간.
  Bullet/HomingMissile 의 draw 를 `_drawBody` 로 감싸 translate 만 적용(게임 x 불변).
  실측: 모든 등급에서 발사점이 선체 밖이었다(T0 +14px·T2 +12px·T4 +11px·T5 +5px).
- **P0-2 차지 빔** — `hide()` 가 `clearTransientFx()` 호출(_lanceT·_prevCharge·_shakeT·visible·opacity 정리).
  빔 시작점을 `muzzleAnchor`(chargeRig 자식) **월드 좌표**로 → 흔들림·롤·등급별 기수 반영.
  피격/흔들림 계산을 빔 배치 **앞**으로 이동(한 프레임 지연 제거). 중첩 배열 루프 제거(무할당).
  frustumCulled=false. 공유 lanceGeo 1회 dispose. NDC x 클램프. info().lance 진단 신설.
  **실측: 54조합(tier0~5 × bank3 × followX3) 포구오차 0.000px · 상단도달 100% · 복귀 잔상 0프레임 ·
  context loss 후 2D 고정 · dispose 2회 예외 0 · page error 0.**
- **P0-4 개발 서버 봉쇄** — 127.0.0.1 기본, 난수 토큰, urlparse 정확 매칭, 8MiB 상한(드레인 후 413),
  **경로 성분 거절**(basename 무마 금지 — `../evil.png` 가 실제로 저장됐다), magic↔확장자, 덮어쓰기 금지,
  CORS 제거, 임시파일→원자적 이동. **보안 테스트 12/12.**

### 완료 (P0 전건 + P1 3건)
- **P0-3 초진화 2D 색** — 템페스트·랜스 전용 스프라이트 신설(`PROJ_VULCAN_TEMPEST/LANCE`).
  `weaponProjectileSpriteId(weapon, evo, superId)` 로 확장(우선순위=색 함수와 동일), 호출부 3곳 배선.
  ⚠️**제작기가 js 색 상수를 직접 읽는다** — 하드코딩 제거. 구울 때 쓴 색을 `vulcan_colors.json` 에 남기고
  `G28-EVOCOLOR` 가 상수와 대조 → 상수만 바꾸고 이미지를 안 구우면 즉시 실패.
  실측(스프라이트 원본): 5종 색상차 base 5.7° / needle 9.8° / storm 3.0° / tempest 1.9° / lance 3.5°.
  ⚠️게임 크기(9~14px)로 축소해 재면 황동 금속이 섞여 오차가 커진다 — **원본 캔버스에서 채도 상위 15%** 로 재야 정확.
- **P1-2 POW 라벨 복원 + 표현 고정** — 스프라이트 위에 작은 'POW'(테두리 포함) 복원.
  `ART_LOCK` WeakMap 으로 개체별 **첫 렌더 시점의 표현을 고정** — 로딩 중 코드도형→실모델 변신 제거.
- **P1-3 512px 축소** — 논리크기×2 기준으로 재산정(코인96·잔해224·나머지128). 발광 반경도 비례 축소.
  **567KB → 79KB(1/7)**, 빌드 48.37 → **47.94MB**.
- **P1-1 실루엣 vs 판정** — 실측으로 3종 확인(크리스탈 좌우빈띠 24px·잔해 20.5px·수송선 판정밖 7px).
  원본 비율상 폭·높이 동시 충족 불가 → 크리스탈 2.8r→2.4r, 수송선 2.6r→2.15r 로 조이고
  `drawHitRing()` 으로 **실제 판정 반경**을 점선 링 표시(충돌원 계약 불변). 잔해는 실제 판정이 r×0.82 라 링도 거기에.

### §G-29 차지빔 방향 되돌림 (이사 재지적 "다시 직선으로 나가는데?")
- ⚠️**§G-25 에서 내가 방향을 잘못 바꿨다.** "2/3 까지밖에 안 나간다"는 지적에 빔을 **화면 최상단**으로 보냈는데,
  그러면 포구와 끝점의 화면 x 가 같아져 **기함이 어디 있든 화면 수직 직선** — §G-23 이사 판정("소실점으로 뻗는 3D 가 맞다")을 어긴다.
- 부족했던 건 **길이**였지 방향이 아니었다 → 방향을 기함 전방(+z)으로 되돌리고(flagRig 회전 반영),
  길이 300→**380**(far 400 한계), 원통→**원뿔**(균일 두께는 끝이 뭉툭하게 잘려 "멈췄다"로 읽힌다).
- 실측: 기함 이탈 0/+180/−180 → 시작X 240/388/92 로 흩어지고 **끝Y 197.73 한 점 수렴** = 원근 유지.
- ⚠️**교훈: "짧다"는 지적에 방향을 바꾸지 마라 — 길이·두께·끝처리부터 본다.**
- 비교 렌더 `_shots/g29_lance_compare.png` · 903/903 · 빌드 47.94MB

### §G-30 차지빔 사거리 + 레이저 전장 (이사 3지적)
- ⚠️**차지빔 한계는 길이가 아니라 카메라 `far=400`이었다.** far 1600·빔 1400 → 끝 화면Y 197.7→**188.0**(소실점 184.3 에 3.7px).
  씬 개체는 깊이 80 이내라 far 확장이 가려짐 순서에 영향 없음.
- ⚠️**레이저는 방향이 이미 맞았다** — 월드 +z 면 원근 투영이 자동으로 소실점에 모은다(실측). 문제는 **화면 전장 26px(=점)**.
  → `SHOT_MIN_PX.laser` 26→**52**, `SHOT_LEN.laser` 1.70→2.10 → 원거리 52px(2D `laserBoltLength` 42~74 대역).
- ⚠️**전장을 늘리면 폭도 커진다**(shotScale: 폭 = 전장 × SHOT_W). 3.20 으로 올렸다가 폭 1.88배 → §G-18 "흰 기둥이 기함을 덮음" 재발(테스트가 잡았다).
  → `SHOT_W.laser` 0.32→**0.259** 로 월드 폭 0.544 그대로 유지.
- 취약 테스트 3건(HW-17·HW-19·G22-SIZE)의 정규식 대조를 **값 기반**으로 교체 — 수치 튜닝마다 깨졌다.
- 903/903 · 빌드 47.94MB · ZIP 43.42MB

### §G-31 차지랜스 관통범위 표시 (이사 A안)
- ⚠️**판정은 되고 있었다** — `fireLance` 는 거리 제한 없음(`e.y < this.y && |e.x-this.x| <= halfW+e.r`). 안 보였을 뿐.
- ⚠️**빔을 연장하면 안 된다** — 소실점 지난 직선은 반대편으로 꺾인다(실측 끝 x241.5 → 상단 x167.6).
- → 잔광 = **판정 컬럼**(기함 x, 폭 halfW×2=63px, y 0~빔끝, 옅은 띠+중앙심). 실제 ChargeLance.halfW 를 읽어 그림.
- 빔(원근·사선) + 띠(판정·수직) 병존 — 발사체와 사거리는 다른 정보.
- 렌더 `_shots/g31_lance_column.png` · 903/903 · 빌드 47.95MB

### 남은 것
- **P1-4** 모바일 실기기 성능(p50/p95/p99) — 실기기 없음, 데스크톱 측정으로 대체 예정(보고서에 한계 명시)
- §5 나머지 테스트 실행 기반 전환(G24-CORE·G24-TIP3D·G27-EBULLET), §6 3뷰포트 브라우저 검증, §7 통합 보고서
- 903/903 통과 · 빌드 **47.94MB/50MB** · ZIP 43.42MB · 287파일 · `git diff --check` 오류 0

## 방금 완료: §G-26·27 2D 위험물·픽업 8종 실모델 교체 + 적탄 3겹 (894/894, 미커밋)
- 이사 지적 "2d 모드에 남아있는 대충 만든 이미지들 모두 새로 만든 이미지로 교체해줘."
- **전수 조사**: entities.js draw 를 스프라이트/코드도형으로 분류 → 코드 도형 5종(잔해·돌격기·기뢰·코인·파워) + 구 벡터/PNG 3종(운석·크리스탈·수송선)이 남아 있었다. 3D 에는 실모델(h1~h4·p1~p4)이 **이미 다 있었다**.
- 파이프라인: 각도 대조판(정면/25°/45°) → 개체별 각도 선택 → 512² 투명 렌더 → 채도·발광 마감 → webp → sprites.js 등록 → draw 교체.
  - 제작기 `E:/workspace/claude/neon-fleet/art/props2d/finalize_props.py`
- ⚠️**상태 표시는 전부 남겼다**(그림만 바꾸고 정보를 잃으면 안 된다): 잔해 붉은 경고 맥동 / 기뢰 무장 여부·폭발반경 링 / 돌격기 예고선·HP바 / 파워 맥동. 옛 코드 도형은 **폴백으로 보존**.
- ⚠️**잔해 경고를 원형 후광으로 깔았더니 "빨간 공"이 됐다** — 세로로 긴 구조물이라 원이 본체보다 컸다. → **실루엣 그림자(shadowBlur)** 로 교체하니 모양을 따라간다.
- 빌드 **48.37MB/50MB** · ZIP 43.85MB · 285파일. 대조판 `_shots/g26_before_after.png`
- 보고서 `docs/2026-08-09-Claude-G26-위험물픽업-실모델교체-완료보고서.md`
- **§G-27 적탄도 3겹(이사 지시)**: 3D 플라즈마 구조(코어 0.22 / 셸 0.34×1.35 / 꼬리 0.18×0.55)를 2D 코드로 옮김. 색 원+흰 점 → **꼬리+셸+백열 코어**. 진행축 회전이라 **탄이 어디로 가는지 보인다**(회피 가능성 개선).
  - ⚠️**3D 비율 그대로 쓰면 "올챙이"** — 2D 는 판정 원(r)이 곧 읽는 크기다. 꼬리 1.45→1.15·셸 1.35→1.2 로 조여 r 안쪽에.
  - ⚠️**검은 배경 위 가산합성은 색을 알파만큼 어둡게 만든다**(62%, 주황→갈색). 셸은 불투명, **꼬리만** 가산합성.
  - ⚠️적탄은 수십 개 동시 — shadowBlur 금지. 타원 2 + 원 1 뿐. 894테스트(G27-EBULLET 추가).
- **남은 것**: 전력모듈·캡슐은 3D 모델 없음(전력모듈=번개 아이콘이라 기능적 OK, 캡슐=현 모드 미스폰 레거시).

## 방금 완료: §G-25 차지 빔 사거리 교정 (893/893, 미커밋)
- 이사 지적 "함미모드일 때 차지샷이 다른 발사체의 2/3 지점까지밖에 안 나간다."
- **길이가 아니라 방향 문제였다.** 빔을 기함 로컬 +z 로만 뻗으면 원근상 **소실점으로 수렴**한다.
  - 실측(480×800·t=1): 기함 노즈 y=561 · 빔 끝(len 110) y=227 · **소실점 y=184** · 발사체 도달 y=0
  - 334/561 = **0.60** ← 이사가 본 "2/3". 길이를 400 으로 늘려도 y=197 로 30px 만 올라간다(소실점이 상한).
  - 발사체가 화면 상단에 놓이는 3D 지점은 **(0, 23.8, 77.4)** — y 가 위로 올라간다(추적 카메라가 내려다보므로).
- 조치: 빔도 발사체와 **같은 광선 수학**(`placeOnRay`)으로 그 지점을 향하게. 깊이 상수 공유(`SHOT_DEPTH_MAX=80`).
  - ⚠️그래서 빔은 `flagRig`(기함 로컬)가 아니라 **씬 직속**이어야 한다 — 월드 좌표 직접 배치.
  - 집속 연출(`chargeRig.visible = chg > 0`)과 빔 표시 조건 분리. dispose 에 차지 메시 3종 정리 추가.
- 실측 비교판 `E:/workspace/claude/neon-fleet/art/_shots/g25_lance_reach.png` (BEFORE 화면 1/3 → AFTER 최상단)
- 보고서 `docs/2026-08-09-Claude-G25-차지빔사거리-완료보고서.md`

## 방금 완료: §G-24 발칸탄 2D 교체 + 백열 심 비례화 + 3D 진화색 (892/892, 미커밋)
- 이사 지적 "3D엔 새 탄인데 **2D는 예전 이미지**" → 원인이 **두 겹**이었다.
  1. 2D 스프라이트가 옛 로켓 그림 그대로였다.
  2. **더 큰 원인**: 백열 심이 고정 **2.8px × 세로 92%** 인데 발칸탄 화면 폭은 **3~8px**(실측 base lv1 6.4px·needle 1.1px). 심이 탄의 **40~100%** 를 덮어 **어떤 그림을 넣어도 흰 막대**로 보였다.
- 조치 ①: 이사 v1 원본 렌더 → 2D 3종 신규(알파컷 + 시계 90° + 진화별 굵기·발광색). **긴 변 320 고정**이라 화면 길이는 불변, 굵기만 기존 폭 비율에 맞춤(base .447→.444 · storm .538→.525).
- 조치 ②: `VULCAN_CORE` 를 **탄 폭 비례**(wRel .24 / wMin .9 / wMax 2.8) + **탄두 국한**(hRel .30, yRel −.10). 팁의 진화색이 심 위에 남는다.
- **⚠️교훈: 아트를 바꿨는데 화면이 그대로면 "그 위에 덧그리는 코드"를 먼저 의심할 것.**
- **⚠️검증법: Browser pane 이 숨김(rAF 정지)이어도 실제 `Bullet.draw()` 를 오프스크린 캔버스로 실행 → dataURL → PNG 저장하면 육안 확인 가능** (`E:/workspace/claude/neon-fleet/art/v1-2d/ingame_bullet_draw.png`).
- 제작기·백업: `E:/workspace/claude/neon-fleet/art/v1-2d/` (make_vulcan_2d.py · backup_old_*.webp)
- 빌드 47.96MB/50MB · ZIP 43.44MB · 279 파일. 보고서 `docs/2026-08-09-Claude-G24-발칸탄2D교체-완료보고서.md`
- **3D 진화색도 맞춤(이사 지시)**: 실모델 본체를 틴트하면 금속이 물드니, **탄저에 가산 발광체 하나**만 얹고 그 파트만 진화색으로. 색은 게임의 `b.color` 를 그대로 받아 2D 와 같은 값(#3ff5e0 / #5cc8ff / #ff8c1a) + 초진화 색까지 자동.
  - ⚠️**노즈에 얹으면 안 보인다** — 추적 카메라는 탄을 **뒤에서** 본다(yaw = π − wob). 실측에서 탄두 발광은 3색 전부 몸체에 가렸다. 탄저면(z 0.52·반경 0.075)이라야 황동 링 안쪽이 색으로 찬다. 실제 예광탄도 탄저가 탄다.
  - v1 탄 **앞뒤 확정**: z별 최대 반경 −0.5쪽 0.034 → +0.5쪽 0.094 → **−z 가 노즈**, `rotY: 0` 이 맞았다(미결 해소).
  - 드론 예광탄은 `corePart` 규칙으로 발광이 숨는다(2D 에 진화색·백열심이 없는 것과 일치). 실측 `_shots/g24_3d_final.png`
- **🛠️새 검증 도구**: `_tools/nocache-server.py` 에 `POST /__save` 추가 + 스레딩 전환 → 페이지가 오프스크린 렌더를 올리면 `E:/workspace/claude/neon-fleet/art/_shots/` 에 PNG 로 떨어진다. **pane 이 숨김이어도 육안 검증 가능.** (단일 스레드였을 땐 게임 keep-alive 가 소켓을 쥐어 POST 가 영영 대기했다.)

## (기록) §G-23 발칸 재설계 모델 투입 + 실기 지적 2건 (미커밋)
- v1 탄(1,498tri) · w1r 포탑(49,995tri) 3D 투입. **앞뒤 방향·회전각은 바운딩박스로 판별 불가** → `rotY: 0`/`π` 로 넣어 둠, 실기 확인 필요.
- 차지샷: 함미모드에서 광선이 둘로 보이던 문제 → **3D 빔 복원 + main 에서 ChargeLance 를 3D 스탬프로 2D 숨김**(중앙으로 모이는 게 맞다는 이사 판정 반영).
- 보조무기 레이저 발사 위치가 기함 바깥 → `hardpointX.wing` **22 → 8**(FLARE 선체 반폭 30인데 22+9=31로 넘어갔다).
- **⚠️`python -m http.server` 는 Cache-Control 을 안 보내 브라우저가 옛 ES 모듈을 계속 실행**(적용 안 된 것처럼 보임) → `_tools/nocache-server.py` 사용(8345 개발본 · 8347 배포본).

## 방금 완료: §G-20 2D↔3D 정합 3건 + 발칸 재설계 프롬프트 (870/870)
- **드론 탄 색 불일치**: 2D 는 종류별로 색이 다르다(예광탄=청록 / 발칸=스프라이트+백열코어라 **흰색** / 레이저=b.color). 3D 는 전부 b.color 하나로 칠했다. **⚠️발칸 b.color 는 청록인데 2D 는 그 색을 안 쓴다** → 3D 만 청록이 됐던 것. 종류별 색 + 예광탄 크기 0.55 배(2D 위계).
- **레이저 안 보임**: §G-18 굵기 0.17 이 과했다 → **0.32**(월드 ≈9px = 2D beamW×2 대역), 최소 화면 전장 26px.
- **차지 원형**: 3D 가 기함을 그리는 구간에서는 2D 원·게이지 링을 안 그린다(3D 집속 코어가 담당). **⚡단계 표시는 유지.**
- **발칸 재설계 프롬프트 전달**: `E:/workspace/claude/neon-fleet/assets/3d/프롬프트_발칸_재설계_2종.md` (w1r 6열 회전총열 포탑 + v1 소구경 예광탄) — **이사 생성 대기**. 받으면 3D 투입 + 2D 스프라이트도 같은 모델로 교체.
- 포털 47.61MB/50MB. 실측 `g20b_cmp.png`.

## (기록) §G-19 배포판이 3D 를 한 번도 안 켜던 문제 (1b22a6c, 869/869)
- 이사 지적 "배포본이 이상해" → **배포판은 3D 가 아예 안 켜져 있었다.** 점등 조건이 `?chase3d=1` 필수인데 배포판은 index.html 을 **쿼리 없이** 연다.
- 즉 §G-10 에서 GLB 20MB 를 넣고 무기·픽업·위험물까지 다 채웠는데 **배포판은 그 자산을 한 번도 안 쓰고 2D 로만 돌았다.**
- **⚠️내 검증 구멍**: QA 를 늘 `?chase3d=1` 로 했고, 배포본은 "GLB 가 200 으로 서빙되는가"까지만 봤다. **자산 전달 ≠ 기능 점등** — 앞으로 배포본은 쿼리 없이 열어 t·flagTier 를 찍어 확인한다.
- 교정: `chase3dEnabled` **기본 ON**, 해제는 `?chase3d=0`. 검사실·검증장면은 여전히 `chase3d=1` 명시 필요.
- 검증: dist 를 쿼리 없이 열어 t=1·flagTier=0·rendered=true, 실측 `g19_dist_3d.png`.

## (기록) §G-18 레이저 굵기 재교정 — "함미모드?" 원인 (eeffeed, 869/869)
- 이사 지적 "함미모드?"(220%인데 기함이 안 보임) → **3D 기함은 정상이었고**(t=1·flagTier=0) **레이저가 기함을 덮고 있었다**.
- 원인=**내 과교정**(§G-15): 전장 3.4·굵기 0.5 → 폭 1.7unit=월드 28px 로 **2D 빔 굵기(beamW 4~9px)의 4배**. 연사로 겹치니 기함 위에 흰 기둥이 쌓임.
- 교정: 전장 **1.70**·굵기 **0.17**(≈월드 4.8px)·최소 화면 전장 18px. 실측 `g18_cmp.png`.
- **교훈: 발사체 크기는 2D 가 실제 쓰는 굵기(beamW·스프라이트 폭) 기준으로 잡는다. 눈대중 배수 금지.**

## (기록) §G-17 위험물 4종 실모델 투입 (4db4feb·9f72e4c, 869/869)
- 이사 VARCO 생성분 h1 운석·h2 잔해·h3 돌격기·h4 기뢰 투입 — **도형 폴백이 전부 실모델로 교체**. 실측 `g17_hazards_play.png`.
- **h4 재수령(정식 50,000 tri) 교체 완료** — 1차 수령분은 이름만 50k 이고 실제 49만 tri 였다. **모델 수령 시 tri 수를 먼저 재는 절차**를 표준으로.
- **포털 예산**: 위험물 4종을 더하면 50.87MB 로 초과 → 보스·기함은 그대로 두고 **작게 그려지는 일반 적 12종만** 재다이어트(11K tri·256 tex) 7.43→4.05MB. 육안 확인 `h_portal_enemies.png` 손상 없음.
- **결과 47.49MB / 50MB**(여유 2.5MB 회복), dist GLB 37종 200 OK.
- 검사실: `?chase3d=1&chase3dLab=hazards&labView=front` 로 4종 진열 확인 가능.

## (기록) §G-15/§G-16 이사 실기 4건 (120bd5e, 869/869)
- **(3) 섹터 종료 기함 두 척**: flythrough=2D 기함이 화면 위로 빠지는 연출인데 3D 기함은 카메라 고정이라 남았다 → `flythrough`·`cutscene` 은 3D 통째로 소등. 실측 `g16_cmp.png`.
- **(4) 죽은 보스 붉은 칸**: HP 바 배경만 남아서 생긴 것 → 살아있는 보스만 바에 표시(`liveBosses`).
- **(1) 발사체**: 발칸=원본 주황 텍스처를 벗기고 **2D 탄색 그대로**(흰빛) / 레이저=전장 3.4·굵기 0.5배(가늘고 길게) / 유도탄 0.86→1.5 / **화면 최소 크기 하한(SHOT_MIN_PX)** 도입 — 2D 의 minRel 에 해당하는 바닥이 3D 엔 없어 중간에서 사라져 보였다.
- **(2) 위험물 4종은 아트 파일 자체가 없다**(운석 스프라이트 C4 부재 → 팔각형 폴백). h1~h4 배선 + **모델 준비 게이트**(없으면 2D 유지, 사라지지 않음) 완료. 프롬프트=`E:\workspace\claude
- **(2) 위험물 4종은 아트 파일 자체가 없다**(운석 스프라이트 C4 부재 → 팔각형 폴백). h1~h4 배선 + **모델 준비 게이트**(없으면 2D 유지, 사라지지 않음) 완료. 프롬프트=`E:/workspace/claude/neon-fleet/assets/3d/프롬프트_위험물_4종.md` → **이사 생성 대기**.
- 포털 49.56MB/50MB.

## (기록) §G-14 픽업 3D 상시 (10253fd, 867/867)
- 이사 지시 "픽업도 3D 로 켜줘" → 크리스탈·보급수송선·코인·POW·캡슐 상시 3D. **라벨(+N·▲N·POW)은 HUD 로 유지** — 판독 안 잃는다.
- 기본 OFF 근거였던 "엉망" 판정은 **임시 조형 시절 얘기**였고, §G-3/§G-7 재설계·VARCO 실모델로 전제가 사라져 켜는 게 맞다.
- **죽은 스위치 제거**: `chase3dProps` URL 플래그·`propsEnabled` 옵션을 config·main·renderer 에서 완전 삭제. 이제 3D 표시 게이트는 없다(기함·적·아군·발사체·픽업 전부 상시).
- 실측 `g14_pickup_zoom.png`(3D 결정 보석 +1 / 컨테이너 ▲3). 포털 49.55MB/50MB.

## (기록) §G-13 발사체 상시 3D — "무기를 집어 던지는 느낌" 교정 (867/867)
- **원인**: 발사체는 3D 도입 후에도 2D 계층에서 그려졌고, `chase-render.js` 의 `drawProjected` 는 **회전을 하지 않는다**(translate+scale 뿐). 화면 어디에 있든 탄 그림이 "똑바로 선" 채라 소실점으로 가야 할 탄이 옆으로 미끄러져 보였다.
- **⚠️내 첫 가정은 틀렸다**: "2.5D 투영이 직선을 휘게 한다" → 실측 결과 **잔차 0px**(16점 최소제곱). 화면에 남는 곡선은 기함이 움직인 궤적 그 자체다(월드에서 실제로 굽은 줄). 투영 탓이 아니다.
- **조치**: 발칸탄·적탄·미사일·레이저 4종을 **상시 3D**(개발 플래그 무관)로. 배치는 **화면 위치=2.5D 좌표**(적·기함과 정합 — 발사체만 월드로 놓으면 가로 퍼짐 1.5배로 적을 빗나가 보임, 실측 396 vs 270px) + **깊이·자세=월드 좌표**(진행 방향으로 실제 회전, 거리만큼 축소).
- **⚠️축 규약 2건**: ①추적 카메라의 화면 오른쪽 = 월드 **−X**(보정 없으면 좌우 거울상) ②절차 발사체 노즈 = **−z**(미사일 GLB 도 rotY 0 통일) → 최종 yaw = `π − wob`.
- **⚠️사고 1건**: 렌더러 블록 치환으로 `prewarm/placeSwarm/applyCam/pixelRatioFor` 를 통째로 삭제해 3D 가 죽었다. git checkout 후 앵커 부분편집으로 재적용. **큰 함수 사이를 인덱스로 잘라내는 치환 금지.**
- 픽업 3D 는 기존대로 `chase3dProps=1` 뒤. 포털 49.56MB/50MB. 실측 `g13b_weapons.png`·`g13b_straight.png`.

## (기록) §G-12 기함 무기 5종 실모델 + 차지 연출 배선 교정 (f588c0c, 866/866)
- **갑판 포탑 3종**(w1 발칸·w2 레이저·w3 유도): flagRig 자식 `mountRig` + 무기 종당 InstancedMesh 1 — 현재 무기만 켜서 드로우 +1. 장착점·개수는 2D `drawWeaponRig` 과 같은 규칙(`MOUNT_POINTS` = ships.js NORMALIZED_MOUNTS 사본, 테스트가 일치 잠금). **무기를 바꾸면 갑판 포탑이 바뀐다** — 실측 `g12b_t4_bow.png`.
- **회전은 전부 진열장 실측**(`?chase3d=1&chase3dLab=weapons`, 30° 스윕 + w2 만 6° 정밀): w1 π / **w2 -π/4**(비축 포신) / w3 0 / w4 π / w5 0. ⚠️바운딩박스로는 앞뒤 판별 불가(§G-8 교훈).
- **차지 포구(w5)**: 충전·발사 동안만 기수에 전개. ⚠️**조리개를 전방(+z)으로 두면 함미 카메라엔 뚫린 뒷면=검은 덩어리** → 180° 돌려 조리개가 카메라를 향하고 코어가 링 안에서 빛나게(실측 `g12f_cmp.png`).
- **⚠️§G-11 차지 연출이 실플레이에서 한 번도 안 떴다**: 렌더러가 `sq.charging/chargeFrac` 만 읽었는데 그건 **검사용 합성 run 전용 필드** — 실전 Squad 는 `charge`(초)·`chargeStage`. `charge>0` + `BAL.charge.stageTime×maxStage` 정규화로 교정. 기수 기준점 `NOSE_Z` 도 상수 2.6(T3) → 등급별 실측 바운딩(T4·T5 에서 코어가 선체에 묻혔음).
- **⚠️미사일(w4)은 아직 소품 플래그 뒤**(`chase3dProps=1`): 소품 3D 기본 OFF 는 이사 실기 피드백으로 정한 것이라 임의로 켜지 않았다 — **켤지 여부 이사 판단 대기**.
- **포털 빌드 47.97 → 49.55MB / 50MB**(무기 경량본 5종 1.57MB 추가, 여유 0.45MB). ⚠️**다음 3D 추가는 용량 확보가 선행**돼야 한다. w3 등은 상자형이라 예외적으로 `-sa` 허용 후 육안 확인.
- 확인 경로: 개발본 `http://127.0.0.1:8129/index.html?chase3d=1` / 배포판 `http://127.0.0.1:8347/` / 무기 진열 `...?chase3d=1&chase3dLab=weapons&labView=front`.

## (기록) §G-11 차지 연출 3D + 무기 VARCO 프롬프트 (cbe654b, 865/865)
- **차지 연출 3D**(이사 "차지 이펙트도 3D"): 기수 앞 집속 코어+후광(chargeFrac 비례) → 발사 순간 빔 0.34s(3단 충전=금색·굵기 1.5배). **⚠회귀 수정 겸** — 실모델 스왈(§G-8) 이후 절차 AURORA 의 emissive 펄스가 model.group 숨김으로 사라졌던 것을 flagRig 별도 연출로 재구성(기함 종류 무관). 실측 g11-charge/g11-lance.png.
- **무기 VARCO 프롬프트 전달**(이사 "varco3d 에서 만들 수 있으면"): `E:/workspace/claude/neon-fleet/assets/3d/프롬프트_무기_5종.md` — W1 발칸·W2 레이저·W3 유도발사대 포탑 + W4 미사일 탄체 + W5 차지 포구. **이사 생성 대기**.
  - **VARCO 가능/불가 구분**: 단단한 물체(포탑·탄체·포구)=가능 / 빛·빔·폭발·화염=불가(코드 담당, 이미 완성).
  - 필요 이유: 3D 함미 시점은 2D 기함을 통째로 안 그려(flagshipAlpha 0) **무기를 바꿔도 기함 겉모습이 동일**. 포탑이 생기면 무기 선택이 보인다.
- ⚠**테스트 함정**: dist 서버(8347)가 폴더를 잡고 있으면 빌드 게이트 테스트 7건이 EPERM 으로 실패 — 코드 문제 아니다. 서버 정지 후 재실행.
- 참고(이사 질문): **fleetlab 은 개발 직행(devDirect) 이라 경로 선택 화면을 건너뛴다**(enterSectorMap 조기 반환) — 정상 플레이(타이틀→출격)에서는 그대로 나온다.

## (기록): §G-10 배포판(포털)에 실모델 3D 포함 (673e745, 865/865, 47.96MB/50MB)
- 이사 결정 "사운드 재인코딩해서 3D 넣어줘" → **BGM 21곡 Vorbis q1 재인코딩**(120→71kbps): 사운드 28.50 → **17.09MB**(-11.4MB). ⚠️효과음 48개는 0.55MB뿐 — 용량은 전부 BGM이었다(실측 후 대상 확정).
- **포털 전용 경량 GLB 28종**(`assets/3d/portal/`, 17.83MB): 텍스처 512·JPEG q84~85·메시 15~20K. **개발본(assets/3d/*.glb ~2MB)은 품질 그대로 — 두 벌 운영.**
- ⚠️`gltfpack -sa` 금지(`--noaggr`): 리메시 균일 토폴로지는 일반 단순화로 형태 유지, -sa 는 UV 아일랜드 무시로 "녹은 무늬"를 만든 전력(§G-5).
- 빌드 배선: 매니페스트 **remap** 도입 — `assets/3d/portal/x.glb` → dist 의 `assets/3d/x.glb`. 런타임 코드·경로 수정 0, 경량본 없으면 404→빌보드 폴백(기존 동작).
- **타이틀 엠블럼 404 수정**(기존 누락): ui.js 가 `<img>` 로 직접 참조하는데 매니페스트에 없었다 — dist 실서빙 QA로 발견.
- 검증: dist 실서빙 GLB 28종 200 OK·404 0건·3D 렌더 정상(기함 T2+드론 49+순양함 2), ZIP 43.24MB. 실측 `g10-dist3d.png`.
- ⚠️백업: BGM 원본 `E:/workspace/claude/neon-fleet-audio-backup-20260807/`. ⚠️dist 서버가 폴더를 잡고 있으면 재빌드가 EPERM — 서버 먼저 정지.

## (기록) §G-9 2D 스프라이트 26종 → 3D 실모델 렌더 교체 (c875478, 865/865)
- 이사 지시 "3D 이미지들도 2D 에 적용해 일관성 유지" → **적 12 + 보스 6 + 기함/아군 6(A1~A6) + 픽업 2** 전부 3D 모델 렌더로 교체. 원본 백업=`E:\workspace\claude\neon-fleet-2d-art-backup-20260807\`.
- 파이프라인: `tools/sprite-render.html?run=1`(정사각 768 타이트 렌더, 실전과 같은 IBL·조명) → `scripts/sprite_finalize.py`(채도·밝기 보정 + 외곽 발광 + **원본 캔버스 규격·채움비·중심 재현**).
- **⚠️규격 계약**: 게임 blit 배율이 원본 종횡비에 묶여 있다(SHIP_ART_ASPECT 등) — 캔버스 크기를 추정으로 넣었다가 20종이 어긋난 사고 1회. 렌더는 정사각, 규격 정합은 파이썬(fit_to_reference)이 전담하는 구조로 확정.
- **⚠️3D 렌더는 그대로 쓰면 안 된다**: 사실적 PBR 이라 2D 아트(발광·채도 과장)보다 어두워 "예쁘지만 안 보이는" 스프라이트가 됨 → 마감 단계 필수.
- 기함(A1~A6)은 무기 리그·프레임 오버레이가 본체를 덮어 화면상 차이가 거의 없다(실측 g9-flag-ab.png) — 부작용 없어 일관성 목적으로 교체 유지.
- **⚠️외곽 발광 폐기(이사 실기 "이상한데?", 83b42e7)**: 어두운 배경 식별성을 노려 실루엣 외곽에 후광을 깔았는데 **작게 그려지는 개체(드론 18px)는 후광이 본체를 덮어** 뿌옇게 뭉갬. 원본 2D 도 halo 없이 그림 자체 발광으로 식별 — 채도·밝기만이 정답. 26종 재마감(glow=None). 실측 g9-glow-fix.png.
- **⚠️T2 기함만 교체가 안 먹던 함정(이사 "기함 3d 이미지 적용이 안된다", 819bdda)**: T2(ARCLIGHT)는 `shipSprite` 가 A3 가 아니라 **`H2_BASE_FRAME`**(assets/art2-webp/ships/frames/H2_base_aligned.webp, 533×768)을 쓰고 A3 는 폴백일 뿐 — 게다가 그 위에 무기별 외장 `H2_ASSAULT/H2_CARRIER` 가 덮인다. → ①H2_base_aligned 를 t2 실모델 렌더로 교체 ②`drawHullFrame` 외장 오버레이 폐지(`HULL_FRAME_OVERLAY=false`, 6등급 중 T2 특례라 통일). ⚠️무기별 함체 외장 변화 연출은 사라짐(구분은 포대 마운트·HUD 유지). **교훈: 아트 교체 전 스프라이트를 실제로 고르는 코드 경로(shipSprite/오버레이)를 먼저 읽을 것 — 파일명 추정 금지.**
- 포털 빌드 46.62 → **41.40MB**(새 스프라이트가 더 가벼움). 실측 g9-sample-final·g9-play2d·g9-h2-final-ab.png.

## (기록) §G-8 기함 실모델 6등급 스왑 + 피격 파편·흔들림 (c5fc1b1, 865/865)
- **기함 6등급 전부 실모델**(이사 "B 로 하자"): T0/T1=a1/a2 재사용, T2 ARCLIGHT·T4 ZENITH·T5 QUASAR 신규, T3 AURORA=a0. `FLAG3D` 테이블 + flagRig(홀더 6 + 절차 AURORA 폴백).
- **⚠️회전 규약(실측 확정)**: 서 있는 원본(a1/a2/a0)=rotX -π/2, **누운 원본(t2/t4/t5)=rotX 금지**(적용하면 전장 1.67·높이 5.04 세워진 벽). 바운딩박스 최대축으로 판별할 것.
- TIER_SCALE [0.62..1.26] = 2D SHIP_VISUAL_SIZE 비의 √ 압축(원비 1.59배는 T5 화면 초과). 검사실 `chase3dLab=flags`(6등급 격자, **labView=front 필수** — top 은 행이 깊이로 겹침).
- **피격 연출 교체**(이사 "충돌 이펙트는 정말 별로다"): 섬광 완전 제거 → **파편 16(결정적 LCG)+함체 감쇠 진동 0.42s**. 파편은 기수 앞·갑판 위 스폰(함체 내부 스폰=가려서 안 보임 — 실측 교정). 카메라는 안 흔든다(멀미 방지).
- 실측 `g8-tier-grid.png`(6등급)·`g8-tier-scale.png`(위엄 차등)·`g8-debris2-crop.png`(파편). 포털 빌드 46.62MB.

## (기록) §G-6 보스 6종+아군 2종 실모델 = 게임 전 개체 실 3D 완성 (66a6ac8→16fee12)
- **적 12 + 보스 6(b7/b22 len 414·b8~b11 len 371, cap 1) + 아군 드론 60·순양함 10(ALLY3D)** 전부 VARCO 리메시 50K 실모델.
- main Boss/MidBoss 수집 분기(spriteId→ENEMY3D 키, 로스터 밖=2D 유지) + 보스 이름 라벨 HUD 보존. makeGlbSwarm(key,def,fallback)=아군 로드 실패 시 디지타이즈 조형 폴백.
- **아군 방향 규약(실측 확정): rotX -π/2 + rotY π** — 이사 지적(8/6) "드론 위아래 반전" 교정: rotY π 로 기수만 180°(+z 소실점), ⚠️rotX 부호 반전은 배 밑면 노출이라 금지. 실측 `g7-drone-fixed-crop.png`.
- **기함 가림 교정(8/6 이사 "적·수송선이 정면에 부딪히는 게 아니라 위로 지나감", 2445459)**: 근접 깊이 클램프 6 < 기함 시선깊이 11.2 가 원인 — placeSwarm behindFlag 로 적·픽업 깊이를 기함 기수 뒤([6,FB)→[FB,FB+1.5) 단조 리매핑, 화면 위치·크기 불변)로 밀어 겹침 구간에서 기함이 가림. 아군·탄은 기함 앞 유지. HW-13, 864/864. 실측 `g7-occl-final-crop.png`. 이사 판정(8/6): "아까보다는 괜찮다" — 수용, 추가 미세조정 여지.
- **충돌 섬광(8/6 이사 요청, 9d12d40)**: 적 몸체 충돌·적탄 명중(내구도 지불) 순간 기함 기수에 백열+주황 충격파+스파크 0.32s — 신호=squad.flash 상승 에지(로직 무수정), 3D HUD 계층 전용, 보호막 방어·reduced-motion 미발화. 실측 `g7-impact-crop.png`(내구도 100→78 충돌 프레임).
- **§G-7 픽업 4종 실모델 투입(8/6, 742341c)**: 이사 VARCO 5종 수령 → PICKUP3D(crystal/pod/coin/pow, cap=PROP_CAPS) — renderer 픽업 생성 GLB 우선·조형 폴백, capsule 레거시 제외. 실측 `g7-pickups-real.png`(실전 4종 동시). ⚠️진열장 1종 함정: createGlbShowcase 격자가 3열 고정이라 단독 진열이 화면 밖(x=-1.5·scale 2) — 종 수 기준 중앙 정렬로 수정.
- **기함 VARCO A/B 대기(8/6)**: `chase3dLab=a0`(VARCO, 아군 규약 rotX -π/2+rotY π) vs `chase3dLab=aurora`(현행) — 비교판 `g7-flagship-ab.png` 이사 전송, **판정 대기**(승인 시 hero 기함 교체 배선).
- **기함 등급 구조 확인**: 기함=6등급(T0 EMBER~T5 QUASAR, balance.js names). 3D는 현재 등급 무관 AURORA 단일 → **티어 스왑 배선 필요**. T0/T1=드론(a1)·순양함(a2)과 동일 아트라 재사용, 신규 제작=T2 ARCLIGHT·T4 ZENITH·T5 QUASAR — **프롬프트 전달** `E:\workspace\claude\neon-fleet\assets\3d\프롬프트_기함티어_3종.md`(참고 3장 변환 완료, 완성명 t2/t4/t5_50k.glb). **이사 생성 대기** → 도착 시 티어 스왑 구현.
- 864/864, 포털 빌드 46.6MB(GLB 제외→빌보드 폴백). 보스 페이즈 연출은 실전 보스전에서 이사 확인 후 조정.

## (기록) §G-5 실모델 기준 확정(이사 승인) — 2d01199
- 실모델 8종(b1~b6·b17·b18) + face 대면(항상 정면) + rotY π(얼굴 -z) + 얼굴 보조광. → 이후 b16(1d42212)·b19~b21(90c9611)로 적 12종 완성.
- 확인: fleetlab(실전) / `chase3dLab=models&labView=front`(진열장 — 일반 적만, 보스·아군 제외).
- ⚠️양자화 GLB=r160 getX 변환 필수(파편·로드실패 함정 2건 — 통합 보고서 §G-5).

## 방금 완료: 적 12종 전부 Gemini 렌더 빌보드 통합 (f163667, 이사 확인 대기)
- 이사가 10종 렌더 제공(`E:\workspace\claude\neon-fleet\assets\3d\B*.png`) → 일괄 전처리(b19 워터마크 스팟 제거 포함) → **적 12종 전체**(b1~b6·b16~b21)가 이사 제작 렌더로 표시.
- 구조: ENEMY3D 단일 진실 테이블(chase3d-prop-defs) — renderer eswarm 루프·main put3D 헬퍼. 조형 스웜(b1/b2/b4) 퇴역. Blinker 는 appearT≥0.9 만 3D(텔레포트 페이드=2D 연출 유지).
- 검증: 863/863, fleetlab 실측 9종 동시(`docs/qa/chase3d-opus5-aurora/g4-all-batch1.png`·`batch2.png`), dist 12종 포함. 로컬 커밋만(푸시 금지).
- **이사 확인: fleetlab 정상 진행만으로 전 종 등장** — 터널 URL 은 세션마다 갱신(status 최신 참고).

## (기록 2026-08-03) 터렛·위버 = 이사 제작(Gemini) 렌더 빌보드 교체 (a3a199e)
- 이사가 절차 조형 기각("정말 별로다") → **Gemini 프롬프트 제작 → 이사가 렌더 생성 → 게임 반영** 새 파이프라인 확립.
- 원본: `E:\workspace\claude\neon-fleet\assets\3d\터렛.png·위버.png` → 전처리(검정 배경 제거=본체 코어 불투명+발광 luminance 페이드, 워터마크 자동 제거, 512² webp) → `assets/3d/b5_gen.webp·b6_gen.webp`.
- `js/chase3d-billboards.js` 신규: 카메라 대면 판(빌보드) — 터렛·위버는 회전·측면 노출이 없어 판 티 없음. renderer b5/b6 스웜 교체(placeSwarm 계약 유지). 빌드 매니페스트 자동 동기. 863/863.
- 실측: fleetlab 220% b5:2·b6:2, `docs/qa/chase3d-opus5-aurora/g4-billboard-play.png`.
- ⚠️QA 교훈: pane rAF 정지 상태에선 카메라 보간이 안 돎 → `__NF.setZoomTarget(2.2)+stepCamera(300)` 수동 펌프 필수(zoom 1이면 3D 수집 자체가 0).

## (기록) §G-4 절차 조형판 — 빌보드로 대체됨
- **G-4-A 적탄**(cc00584+0967eb8): 플라즈마 3파트 순수 조형(백열 코어+화염 외피+꼬리), instanceColor 로 적 종별 탄색.
- **G-4-B 터렛**(330beea, `js/chase3d-b5.js`): 자유 디자인 — 팔각 요새 로프트+2연장 포신+**돔 꼭대기 용광로**+발광 링·슬릿·머즐. 512tri.
  전용 재질 포지: B1 키틴 텍스처 재사용 + 고정 러프 0.72·반사 0.3·자주 틴트(평면 반사 뭉침 교정 — 실측 교훈).
- **G-4-C 위버**(330beea, `js/chase3d-b6.js`): **xy 평면 6방 낫 별**+전면 눈(정면 카메라에 별 실루엣이 활짝 — xz 수평 1차안은 엣지만 보여 폐기). 402tri.
- 배선: renderer 스웜 cap 8·main 수집(비변이만, LEN 105)·검사실 b5/b6 타깃(배율 2.8). 테스트 +6 → **862/862**. dist 재빌드. 커밋 로컬만(푸시 금지).
- 캡처: docs/qa/chase3d-opus5-aurora/g4-b5-lab-final.png · g4-b6-lab-final.png · g4-b5b6-play.png · g4-ebullet-220.png

## 이사 확인 방법
- **실전: `http://localhost:8346/?chase3d=1&fleetlab=1`** — 220% 추적. 적탄은 상시, 터렛·위버는 후반 노드에 자연 등장(진행하며 확인).
- 검사실: `?chase3d=1&chase3dLab=b5`(터렛) / `?chase3dLab=b6`(위버) / `chase3dLab=pickups`(픽업 진열) / `chase3dLab=allies`(아군)

## 다음(이사 승인 순서)
1. ~~§G-1 아군~~ ~~§G-2 발사체~~ ~~§G-3 픽업~~ ~~§G-4 적탄·터렛·위버~~ ~~§G-5 적 12종 실모델~~ ~~§G-6 보스 6종+아군 2종~~ 완료
2. 남은 제작 후보(이사 여유 토큰): **픽업류**(크리스탈·보급 수송선 — Gemini 프롬프트 우선)·**기함 AURORA**(선택, 현행도 승인작)
3. §F 잔여: ①chase3dHeroTest 결정 장면 ⑤반응로 축소(이사 확인 후) / 포털 실모델 포함 여부(예산 재확보 시)

## 서버(백그라운드)
- `node scratch_nocache_srv.mjs "E:/workspace/claude/neon-fleet/worktrees/stage1-analytics"` (8346, 루트 인자 필수)
- `node scratch_capsrv2.mjs` (9098 캡처 저장)
- 외부 테스트 필요 시: `cloudflared tunnel --protocol http2 --url http://localhost:8346`

## QA 캡처 절차(pane 백그라운드)
- 검사실: `__NF3D.lab.step(1)` ×40 + `#game3d.toDataURL` 같은 태스크 → capsrv POST.
- 실전: `__NF.startPlay()` → world 준비 대기 → 필요 개체 주입(`hp=maxHp=999999`로 격추 방지) → `__NF.step(150)` → 3계층 합성(toDataURL).


---

# §G-39 · G-40 · G-41 (2026-08-12~13) — 이사님 실기 승인 완료

## 한 일

**§G-39 적 피해 피드백** — 3D 함미모드에서 적을 쏘면 붉어지고, 맞는 순간 번쩍 + 불꽃.
발칸·레이저는 전에 피격 반응이 **아예 없었다**(미사일만 있었음). 2D 에도 같은 번쩍임·불꽃 추가.
설계 → Codex 3차 검수(반려 → 조건부승인 → 최종승인) → SDD 10태스크(전부 리뷰 통과, 수정 3회).

**§G-40 차지빔 폭** — 3D 빔 폭이 2D 와 무관한 고정 상수였다. 2D 값을 넘겨 쓰도록 배선.
최종: **판정 = 2D 그림 = 3D 그림** 한 숫자로 통일.

**§G-41 발사체 아트 선로딩** — 출격 직후 레이저가 폴백 도형으로 나오던 문제.
§G-38 의 INSTANT_ART 에 **발사체를 빠뜨린** 내 누락이었다.

## 이사님이 정한 값 (전부 실기 판단 — 계산 아님)

| 값 | 파일 | 현재 | 이력 |
|---|---|---|---|
| 피격 번쩍 밝기 | `js/damage-feedback.js` `FEEDBACK.boost` | **3.744** | 2.4 → 3.12(+30%) → 3.744(+20%) |
| 차지빔 기본 폭 | `js/balance.js` `charge.widthShipMult` | **1.40** | 1.05 → 0.70 → 1.40 |
| 차지빔 판정 배수 | `js/balance.js` `charge.hitWidthMult` | **1.2** | 신설. = 그림 배수(0.7+0.5) |
| 3D 빔 폭 배수 | `js/chase3d-config.js` `LANCE_3D_WIDTH_MULT` | **1.0** | 2.0 → 1.0 |
| 적 3D 고도 보정 | `js/chase3d-config.js` `WORLD_OBJ_VIS_DROP` | **0.78** | 0 → 0.26 → 0.52 → 0.78 |

⚠️**빔 폭 조정은 `widthShipMult` 하나만 만져라.** 판정·2D·3D 가 함께 따라온다.
 `LANCE_3D_WIDTH_MULT` 를 1 에서 벗어나게 하면 판정↔그림 일치가 깨진다
 (`tests/g40-lance-width.test.mjs` 의 `G40-HIT-MATCHES-3D-DRAW` 가 잡는다 — 자기검증 확인함).

## 반드시 지켜야 할 계약

- **전역 `Math.random()` 신규 소비 0.** 치명타 판정이 같은 RNG 를 쓴다 —
  연출이 난수를 먹으면 전투 결과가 달라진다. hit-spark 는 전용 LCG(`effects.hitSpark`).
  실측: hitSpark 50회 → 전역 0회 / burst 8입자 → 정확히 24회 유지.
- **2D 몸체 플래시는 body clip 안에서만.** `e.draw()` 는 체력바·보호막 호·충전 예고선도 함께 그린다.
  clip 없이 재호출하면 체력바까지 번쩍여 판독이 왜곡된다(픽셀 검증: 체력바 변화 0/552).
- **피격 시계는 게임 누적 시간.** `main.js` 의 update guard **안**에서만 `advance(dt)`.
  벽시계를 쓰면 일시정지·결과화면에서 플래시가 혼자 사라진다.
- **입자 예산**: 전체 600 / 일반 480(hit-spark 120) / 고우선 예약 120. cap 도달 시 **신규 생략**
  (기존 입자를 지우지 않는다 — 보스 폭발이 발칸 스파크에 밀리면 안 된다).

## 알려진 남은 것

- **Blinker(`js/entities.js:3864`) · 광폭화 Boss(`js/bosses.js:397`)** 는 몸체 blit 직전
  `globalAlpha` 를 **절대값**으로 덮어써 플래시 강도가 무시된다(항상 최대 밝기, 감쇠 안 보임).
  이사님 "괜찮다" 판단으로 보류. 근본 해결은 그 클래스들이 `*=`(상대곱)를 쓰는 것.
- **FZ-3 문자 예산 6자**(`tests/freeze-guard.test.mjs`). `frame()` 을 건드리면 거의 확실히 넘는다.
  구조 기반 매칭으로 바꾸는 소태스크 권장.
- **벤더 `three.module.js` FileLoader 버그**(Node 전용): 상대경로를 받으면 in-flight 레지스트리가
  영구 오염돼 같은 프로세스의 두 번째 `createChase3D` 가 위험. 테스트 파일을 나눠 회피 중.
- **실플레이 입력 검증 불가**: pane 이 숨겨지면 rAF 가 멈춘다. 프레임 수동 구동으로 렌더만 확인 가능.

## 산출물

- 설계: `docs/superpowers/specs/2026-08-12-3d-damage-feedback-design.md`
- 계획: `docs/superpowers/plans/2026-08-12-g39-damage-feedback.md`
- QA: `docs/qa/g39/README.md` · `qa-results.json` · `glb-hp-flash-grid.png`
- SDD 대장: `.superpowers/sdd/2026-08-12-g39-damage-feedback/progress.md`
- Codex 검수 3건: `E:\workspace\claude
eon-fleet\codex-검수\Codex_G39_*.md`

## 상태

- 자동 테스트 **1,048 / 1,048 통과**(패키지 8개는 프리뷰 서버 종료 상태에서만 통과)
- **전부 미커밋 · 푸시 없음** (변경 94개)
- ⚠️빌드 순서: **서버 중지 → 빌드 → 테스트 → 서버 재시작**. 서버가 dist 를 잡으면 빌드가 EPERM 으로 죽는다.


---

# §G39-R1 (2026-08-13) — 코덱스 반려 6건 표적 수정

**결과**: 자동 테스트 **1,083 / 1,083 통과**(G39 대상 45 → 85개). 전부 미커밋(변경 100개).
보고서: `E:\workspace\claude
eon-fleet\codex-검수\G39-R1-수정완료보고서_20260813.md`

## 고친 것 (전부 실재한 결함이었다 — 코드에서 직접 확인 후 착수)

| # | 결함 | 수정 |
|---|---|---|
| P0 | 랜스·메아리·시즈·연쇄·APEX **5개 경로가 3D 피격 표시를 안 남김** | 공용 어댑터 `hitWithFeedback` |
| P0 | 입자 상한에서 `break` → **전역 RNG 소비가 24회→0회** (전투 결과 변동) | 난수는 소비하고 `push` 만 생략 |
| P1 | 고우선 예약이 production 에서 **한 번도 안 쓰임** | 보스·진화·승급·재건 9곳 배선 |
| P1 | 색 장애 시 **직전 색이 얼어붙음**(부분 적용 포함) | `restoreSwarmWhite` — 버퍼 직접 흰색 |
| P1 | put3D↔hero 테스트가 **끊겨 있음** | `writeEnemySlot` seam(위치+색 한 호출) |
| P1 | 사망 보스 재그리기가 **RNG 2회 추가 소비** | `dead`/`dying` 을 기록 대상에서 제외 |

## 핵심 계약 (깨면 안 되는 것)

- ⚠️**`burst()` 는 예산 밖에서도 난수를 소비한다.** 전역 RNG 는 치명타·분산·스폰과 같은 스트림이라
  소비량이 바뀌면 전투 결과가 달라진다. `continue` 를 `break` 로 되돌리지 마라.
- ⚠️**`writeEnemySlot` 은 위치와 색을 한 호출로 쓴다.** 일부러 묶은 것이다 — 색만 지우는
  조용한 회귀가 성립하지 않게 하려고. 쪼개지 마라.
- ⚠️**사망 타격은 플래시하지 않는다.** 사망 폭발이 담당한다(설계 적용표). `drawDying` 이 draw 마다
  전역 난수 2회를 쓰기 때문이기도 하다 — 전수 조사 결과 **draw 계열 중 유일**하다.
- ⚠️**`EFFECT_BUDGET.high(120)` 는 코드가 읽지 않는 설명용 값**이다. 고우선 실제 상한은 `total`(600).

## 코덱스에 되질의한 것 2건 (답 대기)

1. **§5 mutation 요구** — "put3D 색 기록 제거 시 테스트 실패"는 **실행 불가능**하다
   (main.js export 0개, put3D 는 내부 함수, `__NF3D.step` 은 legacy 경로).
   대신 색+위치를 한 호출로 묶어 **그 mutation 자체가 표현 불가능**하게 만들었다. 이 대안이 요건을
   충족하는지 질의. put3D 를 모듈로 분리하는 것은 R1 범위를 넘는다고 봄.
2. **§6 범위** — 코덱스가 `Boss.drawDying` 만 지목했으나, 같은 논리면 살아 있는 개체도 위험하다.
   entities.js·bosses.js draw 계열 전수 조사 결과 **drawDying 이 유일**함을 확인해 근거로 남김.

## QA 재생성 (3.744 / 2.184 한 실행)

- `docs/qa/g39/` 3파일 전부 재작성. 격자 PNG 984×1156(기함 제외 — 1차는 겹쳐 있었다)
- ⭐**blocker 2개 닫힘**: 저사양(mobile·lod1) 체력틴트·번쩍 실측 + 저사양 성능 실측
- 측정법 수정: "알파>40 전체 평균"(배경·기함 희석) → **차분 마스크로 본체만** (px 13,001 → 4,583)
- **h2(잔해)는 production 피해 대상 아님** 명시(indestructible). h1 운석은 대상(maxHp 17)
- 성능: 색 갱신 비용이 **잡음(0.134ms) 아래**. 순수 색 계산 프레임당 0.0058ms

## 이사님 실기 확인 1건

**사망 타격의 번쩍임이 사라졌다.** 죽는 순간에는 폭발만 나온다. 어색하면 사망 폭발 자체를
밝히는 방향으로 대체 가능.


---

# §G39-R2 · §G-42 (2026-08-13) — 코덱스 최종 승인

**판정: 승인 — G39 종료 가능, R3 불필요.**
결과: `E:\workspace\claude
eon-fleet\codex-검수\Codex_G39-R2_최종재검수결과.md`
보고서: 같은 폴더 `G39-R2-수정완료보고서_20260813.md` (§8 에 승인 후 반영 기록)

**테스트 1,099 / 1,099 통과. 변경 102개 전부 미커밋.**

## R2 에서 고친 것 (코덱스 R1 반려 2건)

| 결함 | 내용 |
|---|---|
| **P0 stale flash** | 죽기 **직전** 비치명타 기록이 사망 후에도 읽혀 `drawDying` 이 2번 → 전역 RNG 2→4회 |
| **P1 put3D 링크** | `writeEnemySlot` 호출 한 줄을 지워도 테스트가 다 통과했다 |

⚠️**P0 를 R1 이 놓친 이유 = 테스트 설계 결함.** `G39R1-FATAL-BOSS-*` 세 개가 전부
"**첫 타격이 곧 치명타**"인 새 보스만 검사했다. 실제 전투는 여러 번 때린 뒤 죽인다 —
**가장 흔한 실사용 흐름을 테스트가 안 다뤘다.** 앞으로 경계 테스트를 쓸 때 이 축을 먼저 본다.

## §G-42 보조무기 좌우 (이사 제보 → 승인 → 반영)

"레이저 보조무기 빔이 기함 우측에 형성된다" → `hardpointX.wing(22)` 을 **항상 +** 로만 더한 것이 원인.
레이저·발칸·유도탄·공명 레일 **전부** 해당. 2D·3D 가 같은 건 `shotRenderX()` 한 함수를 쓰기 때문.

**수정: 크기 22 는 그대로, 부호만 발사마다 뒤집는다.** 공용 헬퍼 `alternateHardpointX()`.

⚠️**"밸런스 무변경"이라고 쓰지 마라.** 피해량·발사율·탄 수·관통은 그대로지만 **탄도 분포가 바뀌었다**
— 한 열이 두 열로 갈라져 개별 교전의 명중·충돌 경로가 달라진다.
⚠️§G-28 P0-1 정확한 표현: "게임 좌표 불변"이 아니라 **"크기 22 불변, 부호 정책은 승인하에 개정"**.

## 새로 못 박은 계약

- `readDamageFlash` 도 `isDamageFlashTarget` 를 본다 — **기록 차단과 판독 차단이 같은 기준**이어야 한다
- `G42-WING-MIRROR` — 평균 0 이 아니라 **정렬한 좌/우 오프셋이 원소별 거울쌍**인지 본다
  (평균만으로는 대칭 증명이 안 된다. 왼쪽 −10 두 발 + 오른쪽 +20 한 발도 평균 0)
- `EFFECT_BUDGET.highReserve = total − normal` 파생값 — 강제 상한이 아니다

## ⚠️코덱스도 틀릴 수 있다 (이사 지시 2026-08-13)

이사 지시: "코덱스가 무조건 정답은 아니다. 논리적으로 맞다고 생각하면 되질의해라."

이번 회차 실적:
- R1 지적 6건 → 전부 코드에서 직접 확인 후 착수(전부 실재)
- **되질의 2건** 중 A(put3D mutation 실행 불가) = 불충족 판정 수용, B(RNG 전수조사) = 방법 미비 수용
- **코덱스가 못 본 것 1건 자체 발견**: `EFFECT_BUDGET.high` 죽은 필드 → 인정받음
- **코덱스 근거 데이터 오류 1건 발견**: 최종 재검수 §3-2 의 마운트 예시(±31·±14·±42, 6·8마운트)는
  `SHIP_DEFS_RAW.mounts` 에서 계산한 값인데, **런타임은 `NORMALIZED_MOUNTS`(1,2,3,4,5,5개)를 쓴다.**
  결론(거울쌍)은 맞았고 우리가 실측으로 독립 확인. 근거 배열만 다름
- 부수: `SHIP_DEFS_RAW[].mounts` 는 `SHIP_DEFS` 생성 시 덮이는 **죽은 데이터**(별건)

## 미착수 (승인 후 남긴 것)

- `put3D` 를 실행 가능한 수집 모듈로 분리 → 정적 정규식을 실행 테스트로 교체.
  코덱스도 "향후 자연스럽게 분리되는 시점"을 조건으로 달았다. 지금 하면 승인 상태를 건드리는 리팩터링
- `main.js` 호출부 실행 검증(일반탄·연쇄·APEX·공명 레일) — export 0개라 Node 로 못 띄운다

---

# 폴더 정리 (2026-08-13) — 7개 → 1개

이사 지시로 `E:\workspace\claude` 에 흩어져 있던 네온함대 관련 폴더 7개를 하나로 모았다.

```text
E:\workspace\claude\neon-fleet\
├─ repo\           git 본체 (2.2GB)  ← 옛 neon-fleet
├─ worktrees\      작업 코드 + _tools  ← 옛 neon-fleet-worktrees
│  └─ stage1-analytics\   ← 지금 작업 중인 곳(미커밋 102개)
├─ art\            아트 원본 + _shots(브라우저 캡처 저장소)
├─ codex-검수\     검수요청·결과 52개  ← 옛 neon-fleet-codex-검수요청
├─ review\         2026-07 문서 검토
└─ backups\        2d-art-20260807 / audio-20260807
```

⚠️**옛 경로는 전부 사라졌다.** `neon-fleet-worktrees`·`neon-fleet-art`·`neon-fleet-codex-검수요청`
같은 이름으로 접근하면 실패한다. 메모리·문서에 남은 옛 경로를 보면 위 표로 바꿔 읽어라.

## 이동 시 고친 것 (같은 볼륨이라 파일 내용은 무손실)

| 대상 | 조치 |
|---|---|
| git worktree 양방향 포인터 | `git worktree repair` — 본체·워크트리 양쪽 실행 |
| `.claude/launch.json` | 3곳 |
| `_tools/nocache-server.py` `SHOT_DIR` | 1곳 (브라우저 캡처 저장 위치) |
| `tests/g28-contract.test.mjs` 주석 | 1곳 |

## 이동 후 검증

```text
git worktree list      본체 3e178bc / 워크트리 d987c27 정상
미커밋                 102개 그대로 보존
전체 테스트            1099 / 1099 통과 (패키지 8개 포함)
원본↔dist 해시         4개 파일 전부 일치
프리뷰 서버            8347 HTTP 200 (새 경로로 재시작)
```

⚠️**서버 기동 명령이 바뀌었다.** 앞으로는 이 경로를 쓴다.
`python E:/workspace/claude/neon-fleet/worktrees/_tools/nocache-server.py 8347 E:/workspace/claude/neon-fleet/worktrees/stage1-analytics/dist/crazygames-basic --token=nf-dev-local --overwrite`
