# 네온 함대 — 작업 상태 (2026-08-16)

배포 `master: 7af0196` · 전체 **1,158개 통과** · https://mlight00.github.io/neon-fleet/
브랜치 `claude/neon-fleet-analytics-stage1` — master 와 동일(fast-forward 배포)

---

## 🔴 대기 중: §G-47 보스 페이즈 — 이사님 결정 5건

사양서 `docs/2026-08-14-G47-보스-페이즈-사양서.md`

**규칙과 무관한 부분은 전부 끝났다. 배선만 남았다.**

| 단계 | 상태 |
|---|---|
| 1. 데이터 표 + 순수 판정 (`js/boss-phases.js`) | ✅ |
| 2·3. 페이즈 배선 | ⏸ **승인 대기** |
| 4. 전환 연출 (`ctl.phaseBurst`) | ✅ 미배선 |
| 5·6. B8 검증 → 9종 확대 | ⏸ 승인 후 |
| 7. 테스트 11개 | ✅ |

⚠️테스트 2개가 **미배선 상태 자체를 고정**한다. 승인 없이 켜지면 실패한다.
승인 후 배선할 때 그 둘을 함께 뒤집는 것이 "승인됐다"는 기록이 된다.

### 정해 주셔야 할 것

| # | 안건 | 제 제안 |
|---|---|---|
| 1 | 페이즈 수 | 3단계 |
| 2 | 임계 체력 | 66% / 33% |
| 3 | 전환 시 짧은 무적(0.4초) | 넣는다 |
| 4 | 적용 범위 | **B8 하나로 먼저** |
| 5 | 보스전 총 길이 | 유지 / 늘림 / 줄임 |

### 조사에서 나온 핵심

- **페이즈는 이미 있었다** — `bosses.js:99` 체력 50% 광폭화
- ⚠️**그런데 바꾸는 게 사실상 속도뿐이다.** 전수 4곳(발사간격 ×0.6 · 흔들림 폭·빈도 ·
  위험컬럼 +1). 공격의 **종류**는 그대로라 크레센트 보스는 광폭화해도 크레센트만 빨리 쏜다
- **보스별 고유 패턴이 이미 9종 있다** — 페이즈마다 다른 걸 쓰면 새 공격 없이 3배가 된다
- ⚠️지금 광폭화는 **아무 신호가 없다.** 변화를 인지조차 못 하므로 전환 연출이 필수

---

## ✅ 이번 세션에 끝낸 것

### §G-45 저FPS 품질 강등 (이사 제보 → 실기 확인 완료)

증상: 함미모드에서 기함이 2D 로 굳어 다시 3D 가 안 됨. 스테이지 3 부근.
원인: FPS 가드가 `chase3d` 를 dispose 하고 `_c3dPhase=4` 로 잠갔다. 재시도 경로가 없다.
수정: **끄기 전에 낮춘다** — `degradeProfile()` 사다리
`데스크톱 1.5 → 1.0 → 0.85+LOD1 → 0.7 → 2D`

### §G-46 적 피격·격파 파편 (6차까지)

사양서 `docs/2026-08-14-G46-적-피격격파-3D연출-사양서.md`

- 피격: `consumeHitSpark` 상승 에지 → `placeSwarm` 루프에서 방출(좌표 계산 0)
- 격파: `onEnemyKilled` → `killBurst` 큐 → `frame()` 에서 3D 변환
  ⚠️일반 적은 죽는 프레임에 splice 되므로 목록을 훑는 방식으로는 못 잡는다
- 생체 파편(붉은 타원체) · 피사체별 색(운석 암석 / 보급선 청록 / 크리스탈 청록)
- **품질 사다리 연동** — 파편이 먼저 줄고 → 해상도 → 마지막에 2D
- 기함이 내려가면 제트·포탑·폴백 선체까지 전부 정리(잔상 제거)

### §G-46 4차 충전 이펙트 입체감

구 2개는 어느 각도에서도 원이라 납작하다 → **기울어진 토러스 2개를 반대로 회전**.

---

## ⭐가장 중요한 발견 — 3D를 실측할 수 있다

**`__NF3D.step()` 은 rAF 없이 프레임을 돌린다.** Browser pane 이 숨겨져 있어도 된다.
그동안 "창이 숨겨져 검증 불가"라고 적어 온 것은 **틀린 전제**였다(메모리 정정 완료).

```js
window.__NF3D.init();                 // 지연 로드 강제, phase()===3 까지 대기
window.__NF3D.step(n, zoom);          // n프레임 구동
const ctl = window.__NF3D.ctl();
ctl.killBurst(sx, sy, px, key);       // 파편 직접 청구
ctl.scene.scene.traverse(...)         // instanceColor·행렬 판독
N.run().squad.dead = true;            // 사망·컷신 상황 재현
```

### 이 방법으로만 잡힌 결함 4개 (전부 테스트 통과 중이었다)

1. 파편 12개가 **전부 같은 색** — 방출당 색을 한 번만 뽑아 복사
2. 기함 사망 후 제트·포탑·폴백 선체 **잔상** — 이사님 "십자 빛" 제보의 정체
3. 전환 세기가 **개수로는 구분 불가** — 프레임 상한 12 에 전부 붙음
4. 전환 1단계·2단계 **크기 동일** — 배수를 단계와 무관하게 고정

### ⚠️함정 두 가지 (여러 번 오진했다)

- **조상 체인을 끝까지 확인해라.** `o.parent.visible` 만 보면 숨겨진 리그의 자식이
  "보이는 것"으로 잡힌다
- **캐시가 두 겹이다.** CDN 엣지 + 브라우저 HTTP 캐시. `curl` 확인은 **평문 URL**로 하고,
  브라우저는 `fetch(u, {cache:'reload'})` 후 다시 열어야 새 모듈이 잡힌다

---

## 다음에 할 일

1. 이사님 결정 5건 → §G-47 배선 (B8 하나부터)
2. 십자 빛 잔상이 실기에서 사라졌는지 확인 (코드상 잔여 0 실측 완료)
3. 후속 과제: 명중 지점 정밀화 · 보스 격파 연출 · 잔해(Debris) 파편(maxHp 없어 제외됨)

---

# 2026-08-18 세션 — §G-47 배선·2차 · 자체분석 · 관찰테스트 준비

**HEAD `0357228` · master 동일 · 전체 테스트 1,169개 통과**

## 1. §G-47 보스 페이즈 — 배선 + 실기 1차 반영

이사 승인(8/18) 후 **B8 하나에만** 적용. 나머지 9종은 종전 광폭화 그대로.

### 1차 실기 평가 (이사)
전환 인지 "그런가보다" · 연출 "약함" · **2·3단계 대응 "비슷"** ·
과밀 "가끔" · 보스전 "길어짐" · 발사 "더 빨라도 됨" · 결론 "조절 후 확대"

### ⚠️진단 — "비슷"의 직접 원인
세 페이즈가 **같은 탄**을 각도만 바꿔 쐈다. 피하는 방법이 안 바뀌니 대응도 안 바뀐다.
`EnemyShot` 은 `homing`·`shape`·`r` 을 **이미 지원**하고 있었다. 표가 안 썼을 뿐이다.

### 2차 수정 (배포 완료)
- 임계 66/33 → **70/30**, 3단계 발사 1.7 → **1.8배** (이사 지정)
- `shot` 신설 — 페이즈마다 탄 종류: P1 needle r7 / P2 **orb r7 +유도1.5** / P3 **ring r11 +유도0.7**
- `countMult` 신설 — 후반 탄 수 **감소** (0.85 / 0.7)
- 전환 연출: 플래시 0.18 → **0.34**, 후광 2겹, 파편 22개

### ⚠️여기서 배운 것 세 가지
1. **shape 은 그 보스 기본과 달라야 한다.** B8 기본이 `needle` 인데 페이즈에도 needle 을 줬더니
   유도가 붙어도 **눈으로는 같은 탄**이었다. 실행해 보고 orb/ring 으로 교체했다.
2. **케이스가 크기를 덮어쓴다.** `ring`/`spiral` 이 `{ ...fanOpts, r: 6 }` 로 하드코딩해
   3단계 `r11` 지정이 `r6` 으로 나갔다 → `phR()` 로 페이즈 값 우선.
3. **표만 검사하는 테스트는 배선이 끊겨도 통과한다.** `countMult` 돌연변이가 1차에 안 잡혔다.
   **실제 발사 수**를 세도록 고쳐서 잡았다.

과밀·길어짐은 같은 원인(탄이 많으면 피하느라 못 쏜다).
→ 원칙: **같은 양을 다른 종류로.** 종류를 늘리면서 수까지 늘리지 않는다.

## 2. 평가 도구 — 온라인화

이사 지시 "온라인에서 체크할 수 있게".

| 용도 | 주소 |
|---|---|
| 보스 페이즈 평가 | `eval/boss-phase.html` |
| **3인 관찰 테스트(관찰자용)** | `eval/pilot.html` |
| 자체분석 읽기용 | `eval/self-analysis.html` |

공통: localStorage 자동 저장 · 진행도 · **결과를 붙여넣기 좋은 마크다운으로 복사**
(클립보드 실패 시 새 창 폴백).

⚠️**문항 수를 HTML 에 하드코딩하지 마라.** `0 / 12` 로 적어 뒀는데 실제는 13개였다(라이브에서 발견).

## 3. 외부 관점 자체 분석 (이사 요청)

현직 개발자 비평("AI가 낮춘 것은 문턱이지 기준이 아니다")의 잣대를 우리 게임에 적용.
→ `docs/2026-08-18-바이브코딩-관점-자체분석.md`

**핵심 수치: 361 커밋 · 1,169 테스트 · 외부 플레이어 0명.**

- 커밋 분류: 버그29% · 표현연출28% · 게임성25% · 문서18% · 테스트5%
  전체는 균형이나 **최근 25개 중 18개가 표현·연출**
- '재미의 동사'(선택-결과·위험-보상·학습-숙련·긴장-해소)는 **설계돼 있다**.
  다만 "플레이어에게 그렇게 느껴지는가"를 확인한 것은 **0개**
- ⚠️작업자 책임 명시: 아무도 해 본 적 없는 게임의 파편 색을 4차까지 다듬으면서
  그 사실을 한 번도 짚지 않았다

## 4. 3인 관찰 테스트 준비 — 완료 (실행만 남음)

### ⚠️준비 중 찾은 실제 문제
**저장 기록이 있으면 인트로·첫 안내가 안 뜬다**(`main.js:965`·`3858` — `introSeen`·`firstGuideSeen`).
그 둘이 테스트 핵심인데 관찰자 브라우저를 그냥 쓰면 관찰 대상이 통째로 사라진다.
→ **참가자마다 시크릿 창**. 사전 점검 1번으로 못박음.

`eval/pilot.html` 한 장에: 사전 점검 5 · 진행자 대본 · 관찰 12항목(3명 탭) · 사후 질문 6 · 3명 일괄 복사.
8/14 인쇄본에 없던 **보스 페이즈 관찰 항목**을 추가했다.

### 부수 확인
게임 부팅 정상 · 콘솔 오류 0 · 서버 응답 50~250ms.
⚠️개발 브라우저에서 잰 로딩 9.66초는 **샌드박스 문제**였다(curl 반복으로 확인). 사이트 회귀 아님.

---

## 다음에 할 일

1. ⏳**3인 관찰 테스트 실행** — 이사님 섭외 대기. 결과 오면 항목별 대조 → `docs/pilot/결과_*.md`
   - 진행 차단·판독 불가·조작 불능은 **1명만 겪어도** 수정. 난이도·취향은 여러 명일 때만
2. 보스 페이즈 2차 실기 재평가 (`eval/boss-phase.html`, ⚠️이전 답 남아 있으니 **초기화** 먼저)
3. 페이즈 확대 여부 판단 — B8 검증 후 나머지 9종
4. 후속 보류: 명중 지점 정밀화 · 보스 격파 연출 · 잔해(Debris) 파편(maxHp 없음) · 지속형 빔
5. ⚠️미해결: 영문 375개 미번역 → 영어권 게시 보류 중
